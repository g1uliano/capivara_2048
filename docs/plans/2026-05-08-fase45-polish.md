# Fase 4.5 — Polimento de Jogo e IAP Completo: Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementar 5 melhorias: (1) Bomba 3 requer ≥ 5 peças + aviso visual, (2) Shop overlay com IAP real, (3) Itens avulsos da loja principal com IAP real, (4) Texto "Ok" pós-recompensa dobrada, (5) Haptic feedback graduado em level-up, milestones e game over.

**Architecture:** Mudanças cirúrgicas em camadas existentes. Novo helper `deliverIAPItems` (DRY entre 3 callers de compra). Extensão de `haptic_utils.dart` com enum de intensidade. Nenhum pacote novo. `FakeIAPService` e `FakeAdService` restritos a flavor `tst`. Produtos unitários IAP definidos como `ShopPackage` em `shop_data.dart`.

**Tech Stack:** Flutter/Dart, Riverpod, `HapticFeedback` (flutter/services), `in_app_purchase`, flutter_test

**Spec:** `docs/specs/2026-05-08-fase45-polish-design.md`

---

## Mapa de arquivos

| Arquivo | Ação |
|---------|------|
| `lib/presentation/widgets/daily_reward_overlay.dart` | Modificar — texto condicional `'Ok'`/`'Não, obrigado'` |
| `lib/presentation/widgets/inventory_bar.dart` | Modificar — `forceDisabled` + `onTapWhenDisabled` para Bomba 3 |
| `lib/core/utils/haptic_utils.dart` | Modificar — enum `HapticIntensity` + assinatura `Ref` |
| `lib/presentation/controllers/game_notifier.dart` | Modificar — 3 triggers de haptic em `move()` |
| `lib/data/shop_data.dart` | Modificar — `kShopUnitPackages` + `kUnitPackageByType` |
| `lib/core/utils/iap_delivery.dart` | **Criar** — `deliverIAPItems` helper compartilhado |
| `lib/presentation/widgets/shop_unit_item_card.dart` | Modificar — fluxo IAP completo (era bypass direto) |
| `lib/presentation/widgets/shop_overlay.dart` | Modificar — fluxo IAP completo (era bypass direto) |
| `lib/presentation/screens/shop_screen.dart` | Modificar — remover `_deliverItemsLocally`, usar helper |
| `docs/IAP.md` | **Criar** — documentação de configuração IAP |

---

## Task 1 — Feature 4: Texto "Ok" pós-recompensa dobrada

**Files:**
- Modify: `lib/presentation/widgets/daily_reward_overlay.dart`

- [ ] **Step 1.1: Localizar o TextButton de dismiss**

Abrir `lib/presentation/widgets/daily_reward_overlay.dart` e localizar:
```dart
TextButton(
  onPressed: widget.onDismiss,
  child: Text(
    'Não, obrigado',
    style: GoogleFonts.fredoka(color: Colors.white70, fontSize: 15),
  ),
),
```

- [ ] **Step 1.2: Tornar o texto condicional**

Substituir `'Não, obrigado'` por expressão condicional:

```dart
TextButton(
  onPressed: widget.onDismiss,
  child: Text(
    _doubled ? 'Ok' : 'Não, obrigado',
    style: GoogleFonts.fredoka(color: Colors.white70, fontSize: 15),
  ),
),
```

- [ ] **Step 1.3: Verificar análise estática**

```bash
flutter analyze lib/presentation/widgets/daily_reward_overlay.dart
```

Esperado: `No issues found!`

- [ ] **Step 1.4: Commit**

```bash
git add lib/presentation/widgets/daily_reward_overlay.dart
git commit -m "fix(ui): show 'Ok' instead of 'Não, obrigado' after doubling daily reward"
```

---

## Task 2 — Feature 1: Bomba 3 requer ≥ 5 peças

**Files:**
- Modify: `lib/presentation/widgets/inventory_bar.dart`
- Modify: `test/presentation/widgets/inventory_bar_test.dart` (criar se não existir)

- [ ] **Step 2.1: Escrever teste que falha**

Verificar se `test/presentation/widgets/inventory_bar_test.dart` existe:
```bash
ls test/presentation/widgets/inventory_bar_test.dart 2>/dev/null || echo "não existe"
```

Se não existir, criar. Adicionar (ou acrescentar ao arquivo existente) o grupo:

```dart
// test/presentation/widgets/inventory_bar_test.dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:capivara_2048/data/models/game_state.dart';
import 'package:capivara_2048/data/models/tile.dart';
import 'package:capivara_2048/data/models/inventory.dart';
import 'package:capivara_2048/domain/inventory/inventory_notifier.dart';
import 'package:capivara_2048/presentation/controllers/game_notifier.dart';
import 'package:capivara_2048/presentation/widgets/inventory_bar.dart';

// Helper: cria tabuleiro 4x4 com N tiles não-nulos
List<List<Tile?>> _board(int tileCount) {
  final board = List.generate(4, (_) => List<Tile?>.filled(4, null));
  var placed = 0;
  for (var r = 0; r < 4 && placed < tileCount; r++) {
    for (var c = 0; c < 4 && placed < tileCount; c++) {
      board[r][c] = Tile(id: 'tile_${placed}', level: 1, row: r, col: c);
      placed++;
    }
  }
  return board;
}

// Helper: GameState mínimo para testes
GameState _gameState(int tileCount) => GameState(
  board: _board(tileCount),
  score: 0,
  highScore: 0,
  isGameOver: false,
  hasWon: false,
);

// Helper: cria ProviderContainer com overrides para inventory + game
ProviderContainer _container({
  required int bomb3Count,
  required int tileCount,
}) {
  return ProviderContainer(overrides: [
    inventoryProvider.overrideWith((_) => Inventory(
      bomb2: 0, bomb3: bomb3Count, undo1: 0, undo3: 0,
    )),
    gameProvider.overrideWith((_) => _gameState(tileCount) as GameState),
  ]);
}

void main() {
  group('InventoryBar — Bomba 3 tileCount guard', () {
    testWidgets('Bomba 3 forceDisabled quando bomb3 > 0 e < 5 peças', (tester) async {
      final container = _container(bomb3Count: 1, tileCount: 4);
      addTearDown(container.dispose);
      await tester.pumpWidget(
        UncontrolledProviderScope(
          container: container,
          child: const MaterialApp(home: Scaffold(body: InventoryBar())),
        ),
      );
      // Deve renderizar sem crash e o botão Bomba 3 estar desabilitado
      expect(find.byKey(const Key('inventory_bomb3')), findsOneWidget);
    });

    testWidgets('Bomba 3 habilitada quando bomb3 > 0 e >= 5 peças', (tester) async {
      final container = _container(bomb3Count: 1, tileCount: 5);
      addTearDown(container.dispose);
      await tester.pumpWidget(
        UncontrolledProviderScope(
          container: container,
          child: const MaterialApp(home: Scaffold(body: InventoryBar())),
        ),
      );
      expect(find.byKey(const Key('inventory_bomb3')), findsOneWidget);
    });
  });
}
```

```bash
flutter test test/presentation/widgets/inventory_bar_test.dart --no-pub
```

Esperado: pode falhar por override de `gameProvider` — ajustar conforme necessário. O importante é garantir que o teste existe antes de implementar.

- [ ] **Step 2.2: Adicionar leitura de `tileCount` em `inventory_bar.dart`**

No método `build` de `InventoryBar`, após as linhas de `undoStackLen`, adicionar:

```dart
final tileCount = ref.watch(
  gameProvider.select((s) =>
      s.board.expand((row) => row).whereType<Tile>().length),
);
```

Adicionar import de `Tile` no topo do arquivo se não existir:
```dart
import '../../data/models/tile.dart';
```

- [ ] **Step 2.3: Adicionar `forceDisabled` e `onTapWhenDisabled` no botão Bomba 3**

Localizar o `InventoryItemButton` com `key: const Key('inventory_bomb3')` e substituir:

```dart
InventoryItemButton(
  key: const Key('inventory_bomb3'),
  label: 'Bomba 3',
  icon: Icons.auto_fix_high,
  pngPath: 'assets/images/inventory/bomb_3.png',
  count: inventory.bomb3,
  size: iconSize,
  onPressed: inventory.bomb3 > 0 && tileCount >= 5 ? useBomb3 : null,
  forceDisabled: inventory.bomb3 > 0 && tileCount < 5,
  onTapWhenDisabled: () => showCannotUseItemDialog(
    context: context,
    message: 'São necessárias pelo menos 5 peças no tabuleiro para usar a Bomba 3.',
    pngPath: 'assets/images/inventory/bomb_3.png',
  ),
  onTapWhenEmpty: inventory.bomb3 == 0 && onTapWhenEmpty != null
      ? () => onTapWhenEmpty!(ItemType.bomb3)
      : null,
  shouldPulse: pulsingItems.contains(ItemType.bomb3),
),
```

- [ ] **Step 2.4: Verificar análise estática**

```bash
flutter analyze lib/presentation/widgets/inventory_bar.dart
```

Esperado: `No issues found!`

- [ ] **Step 2.5: Rodar testes**

```bash
flutter test test/presentation/widgets/inventory_bar_test.dart --no-pub
```

Esperado: All tests passed.

- [ ] **Step 2.6: Commit**

```bash
git add lib/presentation/widgets/inventory_bar.dart test/presentation/widgets/inventory_bar_test.dart
git commit -m "feat(game): disable Bomb3 when fewer than 5 pieces on board + warning dialog"
```

---

## Task 3 — Feature 5: Haptic feedback graduado

**Files:**
- Modify: `lib/core/utils/haptic_utils.dart`
- Modify: `lib/presentation/controllers/game_notifier.dart`
- Create: `test/core/utils/haptic_utils_test.dart`

- [ ] **Step 3.1: Escrever testes para `haptic_utils.dart`**

```dart
// test/core/utils/haptic_utils_test.dart
import 'package:flutter/services.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:capivara_2048/core/utils/haptic_utils.dart';
import 'package:capivara_2048/presentation/controllers/settings_notifier.dart';

void main() {
  group('maybeHaptic', () {
    final List<MethodCall> log = [];

    setUp(() {
      log.clear();
      TestDefaultBinaryMessengerBinding.instance.defaultBinaryMessenger
          .setMockMethodCallHandler(
        SystemChannels.platform,
        (call) async {
          if (call.method == 'HapticFeedback.vibrate') log.add(call);
          return null;
        },
      );
    });

    tearDown(() {
      TestDefaultBinaryMessengerBinding.instance.defaultBinaryMessenger
          .setMockMethodCallHandler(SystemChannels.platform, null);
    });

    test('não dispara quando hapticEnabled é false', () {
      final container = ProviderContainer(overrides: [
        settingsProvider.overrideWith((_) => const SettingsState(hapticEnabled: false)),
      ]);
      addTearDown(container.dispose);
      maybeHaptic(container, intensity: HapticIntensity.heavy);
      expect(log, isEmpty);
    });

    test('dispara lightImpact quando intensity é light e habilitado', () {
      final container = ProviderContainer(overrides: [
        settingsProvider.overrideWith((_) => const SettingsState(hapticEnabled: true)),
      ]);
      addTearDown(container.dispose);
      maybeHaptic(container, intensity: HapticIntensity.light);
      expect(log.length, 1);
      expect(log.first.arguments, 'HapticFeedbackType.lightImpact');
    });

    test('dispara heavyImpact quando intensity é heavy e habilitado', () {
      final container = ProviderContainer(overrides: [
        settingsProvider.overrideWith((_) => const SettingsState(hapticEnabled: true)),
      ]);
      addTearDown(container.dispose);
      maybeHaptic(container, intensity: HapticIntensity.heavy);
      expect(log.length, 1);
      expect(log.first.arguments, 'HapticFeedbackType.heavyImpact');
    });
  });
}
```

```bash
flutter test test/core/utils/haptic_utils_test.dart --no-pub
```

Esperado: FAIL (enum `HapticIntensity` não existe ainda).

- [ ] **Step 3.2: Atualizar `haptic_utils.dart`**

Substituir o conteúdo completo do arquivo:

```dart
// lib/core/utils/haptic_utils.dart
import 'package:flutter/services.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import '../../presentation/controllers/settings_notifier.dart';

enum HapticIntensity { light, medium, heavy }

/// Dispara haptic respeitando o toggle de vibração nas Configurações.
/// Aceita [Ref] e [WidgetRef] (WidgetRef extends Ref no Riverpod).
void maybeHaptic(Ref ref, {HapticIntensity intensity = HapticIntensity.light}) {
  if (!ref.read(settingsProvider).hapticEnabled) return;
  switch (intensity) {
    case HapticIntensity.light:
      HapticFeedback.lightImpact();
    case HapticIntensity.medium:
      HapticFeedback.mediumImpact();
    case HapticIntensity.heavy:
      HapticFeedback.heavyImpact();
  }
}
```

- [ ] **Step 3.3: Rodar testes de haptic_utils**

```bash
flutter test test/core/utils/haptic_utils_test.dart --no-pub
```

Esperado: All tests passed.

- [ ] **Step 3.4: Verificar call sites existentes de `maybeHaptic`**

```bash
grep -rn "maybeHaptic" lib/ --include="*.dart"
```

Verificar que todos os call sites passam `WidgetRef` — compatível com `Ref` (subtipo). Nenhuma mudança necessária nos call sites existentes.

- [ ] **Step 3.5: Adicionar triggers de haptic em `GameNotifier.move()`**

Em `lib/presentation/controllers/game_notifier.dart`, adicionar import no topo se não existir:

```dart
import '../../core/utils/haptic_utils.dart';
```

No método `move()`, localizar o bloco que atualiza `state` com `justLost`/`after`:

```dart
state = justLost
    ? after.copyWith(isAwaitingGameOverResolution: true)
    : after.isGameOver
    ? after
    : after.copyWith(isContinuingWithItem: false);
```

Logo **após** esse bloco (e antes do `if (state.maxLevel > before.maxLevel)`), adicionar:

```dart
// Haptic feedback graduado
if (after.maxLevel > before.maxLevel) {
  maybeHaptic(ref, intensity: HapticIntensity.light);
}
if (after.pendingMilestone != null && before.pendingMilestone == null) {
  maybeHaptic(ref, intensity: HapticIntensity.medium);
}
if (after.isGameOver && !before.isGameOver) {
  maybeHaptic(ref, intensity: HapticIntensity.heavy);
}
```

> **Nota:** usar `before` (capturado antes do move) em vez de `state` (que já foi atualizado), para comparar estados antes/depois.

- [ ] **Step 3.6: Verificar análise estática**

```bash
flutter analyze lib/core/utils/haptic_utils.dart lib/presentation/controllers/game_notifier.dart
```

Esperado: `No issues found!`

- [ ] **Step 3.7: Commit**

```bash
git add lib/core/utils/haptic_utils.dart lib/presentation/controllers/game_notifier.dart test/core/utils/haptic_utils_test.dart
git commit -m "feat(haptic): graduated vibration for level-up, milestones and game over"
```

---

## Task 4 — Features 2+3: Produtos unitários IAP + helper compartilhado

**Files:**
- Modify: `lib/data/shop_data.dart`
- Create: `lib/core/utils/iap_delivery.dart`
- Create: `test/data/shop_data_test.dart`

- [ ] **Step 4.1: Escrever testes que falham**

```dart
// test/data/shop_data_test.dart
import 'package:flutter_test/flutter_test.dart';
import 'package:capivara_2048/data/models/item_type.dart';
import 'package:capivara_2048/data/shop_data.dart';

void main() {
  group('kShopUnitPackages', () {
    test('tem exatamente 4 produtos unitários', () {
      expect(kShopUnitPackages.length, 4);
    });

    test('IDs são únicos e seguem convenção u_*', () {
      final ids = kShopUnitPackages.map((p) => p.id).toList();
      expect(ids, containsAll(['u_bomb3', 'u_undo3', 'u_bomb2', 'u_undo1']));
      expect(ids.toSet().length, 4); // sem duplicatas
    });

    test('cada produto tem exatamente 1 unidade do item correspondente', () {
      final bomb3 = kShopUnitPackages.firstWhere((p) => p.id == 'u_bomb3');
      expect(bomb3.contents.bomb3, 1);
      expect(bomb3.contents.bomb2, 0);
      expect(bomb3.contents.undo1, 0);
      expect(bomb3.contents.undo3, 0);
      expect(bomb3.contents.lives, 0);

      final undo1 = kShopUnitPackages.firstWhere((p) => p.id == 'u_undo1');
      expect(undo1.contents.undo1, 1);
    });

    test('giftContents são zeros para todos os unitários', () {
      for (final pkg in kShopUnitPackages) {
        expect(pkg.giftContents.bomb2, 0, reason: '${pkg.id} giftContents.bomb2');
        expect(pkg.giftContents.bomb3, 0, reason: '${pkg.id} giftContents.bomb3');
        expect(pkg.giftContents.undo1, 0, reason: '${pkg.id} giftContents.undo1');
        expect(pkg.giftContents.undo3, 0, reason: '${pkg.id} giftContents.undo3');
        expect(pkg.giftContents.lives, 0, reason: '${pkg.id} giftContents.lives');
      }
    });
  });

  group('kUnitPackageByType', () {
    test('mapeia todos os 4 ItemType', () {
      expect(kUnitPackageByType.keys, containsAll(ItemType.values));
    });

    test('bomb3 mapeia para pacote u_bomb3', () {
      expect(kUnitPackageByType[ItemType.bomb3]!.id, 'u_bomb3');
    });

    test('undo1 mapeia para pacote u_undo1', () {
      expect(kUnitPackageByType[ItemType.undo1]!.id, 'u_undo1');
    });
  });
}
```

```bash
flutter test test/data/shop_data_test.dart --no-pub
```

Esperado: FAIL (`kShopUnitPackages` não existe).

- [ ] **Step 4.2: Adicionar produtos unitários em `shop_data.dart`**

Ao final de `lib/data/shop_data.dart`, após `kItemUnitPrices` e `packageIdsContaining`, adicionar:

```dart
const List<ShopPackage> kShopUnitPackages = [
  ShopPackage(
    id: 'u_bomb3',
    name: '1× Bomba 3',
    description: '1 bomba que remove 3 peças à sua escolha',
    originalPrice: 1.99,
    currentPrice: 1.99,
    discountPercent: 0,
    contents: RewardBundle(lives: 0, bomb2: 0, bomb3: 1, undo1: 0, undo3: 0),
    giftContents: RewardBundle(lives: 0, bomb2: 0, bomb3: 0, undo1: 0, undo3: 0),
  ),
  ShopPackage(
    id: 'u_undo3',
    name: '1× Desfazer 3',
    description: '1 desfazer de 3 jogadas',
    originalPrice: 0.99,
    currentPrice: 0.99,
    discountPercent: 0,
    contents: RewardBundle(lives: 0, bomb2: 0, bomb3: 0, undo1: 0, undo3: 1),
    giftContents: RewardBundle(lives: 0, bomb2: 0, bomb3: 0, undo1: 0, undo3: 0),
  ),
  ShopPackage(
    id: 'u_bomb2',
    name: '1× Bomba 2',
    description: '1 bomba que remove 2 peças adjacentes',
    originalPrice: 1.19,
    currentPrice: 1.19,
    discountPercent: 0,
    contents: RewardBundle(lives: 0, bomb2: 1, bomb3: 0, undo1: 0, undo3: 0),
    giftContents: RewardBundle(lives: 0, bomb2: 0, bomb3: 0, undo1: 0, undo3: 0),
  ),
  ShopPackage(
    id: 'u_undo1',
    name: '1× Desfazer 1',
    description: '1 desfazer de 1 jogada',
    originalPrice: 0.49,
    currentPrice: 0.49,
    discountPercent: 0,
    contents: RewardBundle(lives: 0, bomb2: 0, bomb3: 0, undo1: 1, undo3: 0),
    giftContents: RewardBundle(lives: 0, bomb2: 0, bomb3: 0, undo1: 0, undo3: 0),
  ),
];

/// Mapa de conveniência: ItemType → ShopPackage unitário.
/// Usar para compra avulsa via IAP em ShopUnitItemCard e ShopOverlay.
final Map<ItemType, ShopPackage> kUnitPackageByType = {
  ItemType.bomb3: kShopUnitPackages[0],
  ItemType.undo3: kShopUnitPackages[1],
  ItemType.bomb2: kShopUnitPackages[2],
  ItemType.undo1: kShopUnitPackages[3],
};
```

- [ ] **Step 4.3: Rodar testes de shop_data**

```bash
flutter test test/data/shop_data_test.dart --no-pub
```

Esperado: All tests passed.

- [ ] **Step 4.4: Criar `iap_delivery.dart`**

```dart
// lib/core/utils/iap_delivery.dart
import 'dart:async';

import 'package:flutter_riverpod/flutter_riverpod.dart';

import '../../data/models/item_type.dart';
import '../../data/models/shop_package.dart';
import '../../domain/inventory/inventory_notifier.dart';
import '../../domain/lives/lives_notifier.dart';

/// Entrega local os itens de um [ShopPackage] após IAP bem-sucedido.
///
/// Usado por [ShopScreen], [ShopUnitItemCard] e [ShopOverlay] para evitar
/// duplicação. Não gera share codes — cada caller lida com isso.
void deliverIAPItems(WidgetRef ref, ShopPackage package) {
  final c = package.contents;
  if (c.lives > 0) {
    unawaited(ref.read(livesProvider.notifier).addPurchased(c.lives));
  }
  if (c.bomb2 > 0) {
    unawaited(ref.read(inventoryProvider.notifier).add(ItemType.bomb2, c.bomb2));
  }
  if (c.bomb3 > 0) {
    unawaited(ref.read(inventoryProvider.notifier).add(ItemType.bomb3, c.bomb3));
  }
  if (c.undo1 > 0) {
    unawaited(ref.read(inventoryProvider.notifier).add(ItemType.undo1, c.undo1));
  }
  if (c.undo3 > 0) {
    unawaited(ref.read(inventoryProvider.notifier).add(ItemType.undo3, c.undo3));
  }
}
```

- [ ] **Step 4.5: Verificar análise estática**

```bash
flutter analyze lib/data/shop_data.dart lib/core/utils/iap_delivery.dart
```

Esperado: `No issues found!`

- [ ] **Step 4.6: Commit**

```bash
git add lib/data/shop_data.dart lib/core/utils/iap_delivery.dart test/data/shop_data_test.dart
git commit -m "feat(iap): add unit product packages + deliverIAPItems helper"
```

---

## Task 5 — Feature 3: `ShopUnitItemCard` com IAP real

**Files:**
- Modify: `lib/presentation/widgets/shop_unit_item_card.dart`

- [ ] **Step 5.1: Adicionar imports necessários**

Em `lib/presentation/widgets/shop_unit_item_card.dart`, substituir os imports existentes por:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

import '../../core/utils/iap_delivery.dart';
import '../../data/models/item_type.dart';
import '../../data/shop_data.dart';
import '../../domain/shop/iap_service.dart';
import '../widgets/iap_confirmation_sheet.dart';
import '../widgets/purchase_success_sheet.dart';
```

> **Nota:** verificar se `iap_confirmation_sheet.dart` e `purchase_success_sheet.dart` estão em `lib/presentation/widgets/` ou em `lib/presentation/screens/`. Ajustar o caminho do import conforme a estrutura real. Usar `grep -rn "class IAPConfirmationSheet" lib/` para confirmar.

- [ ] **Step 5.2: Reescrever o método `_buy`**

Substituir o método `_buy` completo:

```dart
Future<void> _buy(BuildContext context, WidgetRef ref) async {
  final package = kUnitPackageByType[item]!;

  // 1. Exibir confirmation sheet (mesma da loja principal)
  final confirmed = await IAPConfirmationSheet.show(context, package);
  if (!confirmed || !context.mounted) return;

  // 2. Chamar IAP (real em dev/prd via IAPServiceImpl; fake em tst via FakeIAPService)
  final iapService = ref.read(iapServiceProvider);
  final result = await iapService.buyPackage(package);
  if (!context.mounted) return;

  if (result.success) {
    // Entregar itens localmente (necessário em dev/tst; em prd é feito server-side também)
    deliverIAPItems(ref, package);
    if (result.shareCode != null && context.mounted) {
      await PurchaseSuccessSheet.show(context, result.shareCode!);
    }
  } else if (result.error != null) {
    ScaffoldMessenger.of(context).showSnackBar(
      SnackBar(content: Text('Erro na compra: ${result.error}')),
    );
  }
  // cancelled (error == null && !success) → não fazer nada
}
```

- [ ] **Step 5.3: Remover import não mais necessário**

Remover o import de `inventory_notifier.dart` se não for mais usado em nenhuma outra parte do arquivo:

```bash
grep -n "inventoryProvider\|inventory_notifier" lib/presentation/widgets/shop_unit_item_card.dart
```

Se não aparecer nenhum uso restante, remover o import.

- [ ] **Step 5.4: Verificar análise estática**

```bash
flutter analyze lib/presentation/widgets/shop_unit_item_card.dart
```

Esperado: `No issues found!`

- [ ] **Step 5.5: Commit**

```bash
git add lib/presentation/widgets/shop_unit_item_card.dart
git commit -m "feat(shop): wire unit item purchases through real IAP (dev/prd)"
```

---

## Task 6 — Feature 2: `ShopOverlay` com IAP real

**Files:**
- Modify: `lib/presentation/widgets/shop_overlay.dart`

- [ ] **Step 6.1: Adicionar imports necessários**

Em `lib/presentation/widgets/shop_overlay.dart`, adicionar ao bloco de imports:

```dart
import '../../core/utils/iap_delivery.dart';
import '../../domain/shop/iap_service.dart';
import 'iap_confirmation_sheet.dart';
import 'purchase_success_sheet.dart';
```

> Verificar caminho de `iap_confirmation_sheet.dart` e `purchase_success_sheet.dart` igual ao Step 5.1.

- [ ] **Step 6.2: Reescrever `_onBuy`**

Localizar o método `_onBuy` atual:

```dart
Future<void> _onBuy(ShopPackage package) async {
  final confirmed = await showDialog<bool>(...);
  if (confirmed != true || !mounted) return;
  final c = package.contents;
  if (c.lives > 0) unawaited(...);
  // ... entrega direta
  widget.onItemPurchased(widget.itemType);
}
```

Substituir completamente por:

```dart
Future<void> _onBuy(ShopPackage package) async {
  // 1. Confirmation sheet
  final confirmed = await IAPConfirmationSheet.show(context, package);
  if (!confirmed || !mounted) return;

  // 2. IAP real
  final iapService = ref.read(iapServiceProvider);
  final result = await iapService.buyPackage(package);
  if (!mounted) return;

  if (result.success) {
    deliverIAPItems(ref, package);
    if (result.shareCode != null && mounted) {
      await PurchaseSuccessSheet.show(context, result.shareCode!);
    }
    widget.onItemPurchased(widget.itemType);
  } else if (result.error != null) {
    ScaffoldMessenger.of(context).showSnackBar(
      SnackBar(content: Text('Erro na compra: ${result.error}')),
    );
  }
  // cancelled → não fazer nada
}
```

- [ ] **Step 6.3: Remover imports não mais necessários**

```bash
grep -n "inventoryProvider\|livesProvider\|inventory_notifier\|lives_notifier" lib/presentation/widgets/shop_overlay.dart
```

Remover imports de `inventory_notifier.dart` e `lives_notifier.dart` se não houver mais usos no arquivo.

- [ ] **Step 6.4: Verificar análise estática**

```bash
flutter analyze lib/presentation/widgets/shop_overlay.dart
```

Esperado: `No issues found!`

- [ ] **Step 6.5: Commit**

```bash
git add lib/presentation/widgets/shop_overlay.dart
git commit -m "feat(shop): wire overlay purchases through real IAP (dev/prd)"
```

---

## Task 7 — Limpeza `ShopScreen` + criar `docs/IAP.md`

**Files:**
- Modify: `lib/presentation/screens/shop_screen.dart`
- Create: `docs/IAP.md`

- [ ] **Step 7.1: Substituir `_deliverItemsLocally` por `deliverIAPItems` em `ShopScreen`**

Em `lib/presentation/screens/shop_screen.dart`:

1. Adicionar import do helper:
```dart
import '../../core/utils/iap_delivery.dart';
```

2. Localizar o método `_onBuy` e substituir a chamada `_deliverItemsLocally(ref, package)` por `deliverIAPItems(ref, package)`:

```dart
// Antes:
if (flavor != 'prd') {
  _deliverItemsLocally(ref, package);
}

// Depois:
deliverIAPItems(ref, package);
```

> **Nota sobre share codes:** `_deliverItemsLocally` também adicionava um share code fake ao `shareCodesProvider`. Esse comportamento é específico do flavor `dev` e pode ser mantido inline em `_onBuy` para não perder a funcionalidade:
> ```dart
> if (result.success && result.shareCode != null) {
>   deliverIAPItems(ref, package);
>   // Em dev: gerar share code local para testar o fluxo de presente
>   const flavor = String.fromEnvironment('FLAVOR', defaultValue: 'dev');
>   if (flavor != 'prd') {
>     unawaited(ref.read(shareCodesProvider.notifier).add(ShareCode(
>       code: 'DEV-${DateTime.now().millisecondsSinceEpoch % 10000}-AB',
>       packageId: package.id,
>       giftContents: package.giftContents,
>       status: ShareCodeStatus.pending,
>       createdAt: DateTime.now(),
>     )));
>   }
>   if (context.mounted) {
>     await PurchaseSuccessSheet.show(context, result.shareCode!);
>   }
> }
> ```

3. Remover o método `_deliverItemsLocally` completo.

4. Remover imports que `_deliverItemsLocally` usava se não forem mais usados em outro lugar:
```bash
grep -n "inventoryProvider\|inventory_notifier\|shareCodesProvider\|share_codes" lib/presentation/screens/shop_screen.dart
```

- [ ] **Step 7.2: Verificar análise estática de `shop_screen.dart`**

```bash
flutter analyze lib/presentation/screens/shop_screen.dart
```

Esperado: `No issues found!`

- [ ] **Step 7.3: Criar `docs/IAP.md`**

```markdown
# Configuração de Produtos IAP — Olha o Bichim!

Este documento descreve todos os produtos in-app purchase (consumíveis) do app,
e como configurá-los nas lojas antes do lançamento.

---

## Produtos registrados

| ID do produto | Tipo       | Preço (BRL) | Conteúdo                    |
|---------------|------------|-------------|-----------------------------|
| `p1`          | Consumível | R$ 3,99     | 4× Bomba 3                  |
| `p2`          | Consumível | R$ 1,99     | 4× Desfazer 3               |
| `p3`          | Consumível | R$ 2,49     | 6 Vidas                     |
| `p4`          | Consumível | R$ 4,99     | 10 Vidas                    |
| `p5`          | Consumível | R$ 4,99     | Combo Mata Atlântica        |
| `p6`          | Consumível | R$ 9,99     | Combo Floresta Amazônica    |
| `u_bomb3`     | Consumível | R$ 1,99     | 1× Bomba 3                  |
| `u_undo3`     | Consumível | R$ 0,99     | 1× Desfazer 3               |
| `u_bomb2`     | Consumível | R$ 1,19     | 1× Bomba 2                  |
| `u_undo1`     | Consumível | R$ 0,49     | 1× Desfazer 1               |

Todos são **consumíveis** (o jogador pode comprar múltiplas vezes).

---

## Google Play Console

1. Acesse **Monetização → Produtos in-app → Produtos gerenciados**
2. Para cada ID da tabela acima:
   - Clique em **Criar produto**
   - Tipo: **Consumível**
   - ID do produto: exatamente como na tabela (ex: `u_bomb3`)
   - Nome e descrição em PT-BR conforme a tabela
   - Preço: conforme tabela (ajustar para a faixa de preço mais próxima disponível no BR)
   - Status: **Ativo**
3. Salvar
4. Publicar (produtos só ficam disponíveis após o app estar publicado, ao menos em **Teste Interno**)

---

## App Store Connect

1. Acesse o app → **Recursos → Compras no app**
2. Para cada ID:
   - Clique em **+** → **Consumível**
   - ID de referência: exatamente como na tabela
   - Nome de referência: nome interno (ex: "Bomba 3 unitária")
   - Localização: adicionar PT-BR com nome e descrição
   - Preço: escolher faixa equivalente
   - Screenshot de revisão: captura do item sendo usado no jogo
3. Salvar e submeter junto com a próxima versão do app

---

## Testando em sandbox

### Android (flavor `dev`)

O flavor `dev` usa `IAPServiceImpl` (sandbox do Google Play).

1. Configure uma conta de teste licenciada no Play Console:
   - **Configuração → Licenciamento e testes in-app → Testadores de licença**
   - Adicionar o e-mail da conta Google de teste
2. No dispositivo de teste, faça login com a conta de testes
3. Execute o app com `flutter run --dart-define=FLAVOR=dev`
4. Compras no sandbox não cobram valor real

### iOS (flavor `dev`)

1. Crie um **Sandbox Tester** em App Store Connect → **Usuários e acesso → Sandbox**
2. No dispositivo de teste, saia do iCloud e entre com o Sandbox Tester
3. Execute com `flutter run --dart-define=FLAVOR=dev`

---

## Flavor `tst` (testes automatizados)

O flavor `tst` usa `FakeIAPService` — sempre retorna sucesso instantaneamente, sem
chamadas de rede. Ideal para testes unitários e de widget.

Para testar com IAP real em `tst` (smoke test):
```bash
flutter run --dart-define=FLAVOR=tst --dart-define=USE_REAL_IAP=true
```

---

## Código-fonte relacionado

| Arquivo | Responsabilidade |
|---------|-----------------|
| `lib/domain/shop/iap_service.dart` | Interface `IAPService` + `FakeIAPService` + provider |
| `lib/data/repositories/iap_service_impl.dart` | Implementação real com `in_app_purchase` |
| `lib/data/repositories/iap_startup_service.dart` | Inicialização da store na startup do app |
| `lib/data/shop_data.dart` | Definições de todos os produtos (`kShopPackages`, `kShopUnitPackages`) |
| `lib/core/utils/iap_delivery.dart` | Helper para entrega local de itens pós-compra |
```

- [ ] **Step 7.4: Commit**

```bash
git add lib/presentation/screens/shop_screen.dart docs/IAP.md
git commit -m "refactor(shop): use deliverIAPItems helper in ShopScreen + add docs/IAP.md"
```

---

## Task 8 — Verificação final

- [ ] **Step 8.1: Rodar suite completa**

```bash
flutter test --no-pub 2>&1 | tail -5
```

Esperado: todos os testes novos passam; os 7 falhos pré-existentes (invite_service URL + IAP/shop) são aceitáveis.

- [ ] **Step 8.2: Análise estática completa**

```bash
flutter analyze lib/ 2>&1 | tail -5
```

Esperado: `No issues found!`

- [ ] **Step 8.3: Build de verificação**

```bash
flutter build apk --debug --flavor tst --dart-define=FLAVOR=tst 2>&1 | tail -5
```

Esperado: `✓ Built build/app/outputs/flutter-apk/app-tst-debug.apk`

- [ ] **Step 8.4: Atualizar CHANGELOG.md**

Adicionar/atualizar seção `[Unreleased]`:

```markdown
## [Unreleased]

### Added

- **Haptic feedback graduado**: vibração leve em level-up, média em 2048/4096/8192, forte em game over — respeitando toggle de Configurações
- **Produtos IAP unitários** (`u_bomb3`, `u_undo3`, `u_bomb2`, `u_undo1`): itens avulsos agora têm produtos reais registráveis nas lojas
- **`docs/IAP.md`**: guia completo de configuração dos produtos IAP no Google Play Console e App Store Connect
- **`deliverIAPItems` helper** (`lib/core/utils/iap_delivery.dart`): entrega local de itens IAP compartilhada entre ShopScreen, ShopUnitItemCard e ShopOverlay

### Changed

- **Bomba 3**: desabilitada quando há menos de 5 peças no tabuleiro; exibe aviso "São necessárias pelo menos 5 peças..." igual ao Desfazer 3
- **Loja principal — itens avulsos**: compra agora usa fluxo IAP real (IAPConfirmationSheet + iapService) em vez de adicionar direto ao inventário
- **Shop overlay**: compra de combos e itens avulsos agora usa fluxo IAP real
- **Recompensa dobrada**: botão de dismiss muda de "Não, obrigado" para "Ok" após assistir o anúncio
- **`FakeIAPService`**: restrito ao flavor `tst` (antes era padrão em `dev` também)

### Fixed

- `ShopScreen._deliverItemsLocally` substituído pelo helper compartilhado `deliverIAPItems` (DRY)
```

- [ ] **Step 8.5: Atualizar AGENTS.md**

Na tabela de roadmap, adicionar linha para Fase 4.5 e atualizar "Fase atual":

```markdown
| 4.5 ✅    | Polimento: Bomba 3 ≥5 peças, IAP real em dev/overlay/loja, haptic graduado, texto "Ok" pós-ad |
```

Atualizar linha "Fase atual":
```
Fase atual: **Fase 4.5 completa — próximo: Fase 5 (Arte adicional e polimento visual)**
```

- [ ] **Step 8.6: Commit final**

```bash
git add CHANGELOG.md AGENTS.md
git commit -m "docs: changelog + roadmap for Fase 4.5 polish"
```
