# Fase 3.6 — Tier 2: APK com TestRunnerScreen + Share + Demo

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Criar um APK instalável (flavor `test`) com TestRunnerScreen que roda os cenários E2E no dispositivo real, exibe resultados ao vivo (✓/✗/⟳/○), exporta JSON+PNG via share_plus, e possui modo demo visual.

**Architecture:** `integration_test/tier2_runner.dart` é o entry point compilado pelo Flutter com `IntegrationTestWidgetsFlutterBinding`. Ele importa todos os cenários de `test/e2e/`, popula um `TestResultsStore` (ValueNotifier em `lib/testing/`) e chama `runApp(TestRunnerApp(...))` antes de iniciar os testes. O TestRunnerApp reage ao store em tempo real (ao vivo). Demo mode roda apenas cenários com `ScenarioTag.demo` e suprime `TestFailure` via try/catch. `lib/main_test.dart` é um stub que documenta o entry point alternativo.

**Tech Stack:** Flutter 3.x, `integration_test` (SDK Flutter, dev dep), `share_plus: ^10.0.0`, `IntegrationTestWidgetsFlutterBinding`, Android productFlavors (Groovy/Kotlin KTS).

---

## Mapeamento de arquivos

| Ação      | Arquivo                                                | Responsabilidade                                           |
| --------- | ------------------------------------------------------ | ---------------------------------------------------------- |
| Criar     | `lib/testing/test_result.dart`                         | Data model: `TestStatus`, `TestResult`, `TestResultsStore` |
| Criar     | `lib/testing/test_runner_app.dart`                     | `TestRunnerApp` — MaterialApp raiz do APK de testes        |
| Criar     | `lib/testing/test_runner_screen.dart`                  | UI: lista agrupada, ✓/✗/⟳/○, stack trace, share, demo      |
| Criar     | `lib/testing/share_results.dart`                       | Exporta PNG + JSON via share_plus                          |
| Criar     | `lib/main_test.dart`                                   | Stub de documentação do entry point                        |
| Criar     | `integration_test/tier2_runner.dart`                   | Entry real do APK — binding + executa cenários + demo      |
| Criar     | `android/app/src/test/res/mipmap-hdpi/ic_launcher.png` | Ícone amarelo flavor test (todos os densities)             |
| Modificar | `pubspec.yaml`                                         | Adiciona `share_plus`, `integration_test`                  |
| Modificar | `android/app/build.gradle.kts`                         | Flavor `test` com `applicationIdSuffix ".test"`            |
| Modificar | 5 cenários demo                                        | Adiciona `ScenarioTag.demo` aos cenários escolhidos        |

---

## Task 1: Adicionar dependências

**Files:**

- Modify: `pubspec.yaml`

- [ ] **Step 1: Adicionar share_plus e integration_test**

Em `pubspec.yaml`, na seção `dependencies:`, adicionar:

```yaml
share_plus: ^10.0.0
```

Na seção `dev_dependencies:`, adicionar:

```yaml
integration_test:
  sdk: flutter
```

- [ ] **Step 2: Rodar pub get e verificar**

```bash
cd /home/giuliano/rf/capivara_2048
flutter pub get
```

Esperado: `Got dependencies!` sem erros.

- [ ] **Step 3: Commit**

```bash
git add pubspec.yaml pubspec.lock
git commit -m "chore: add share_plus + integration_test deps (Fase 3.6)"
```

---

## Task 2: Configurar Android flavor `test`

**Files:**

- Modify: `android/app/build.gradle.kts`

- [ ] **Step 1: Adicionar flavor dimensions e productFlavors**

Em `android/app/build.gradle.kts`, dentro do bloco `android { ... }`, logo antes da chave de fechamento, adicionar:

```kotlin
    flavorDimensions += "environment"

    productFlavors {
        create("prod") {
            dimension = "environment"
        }
        create("test") {
            dimension = "environment"
            applicationIdSuffix = ".test"
            versionNameSuffix = "-TEST"
            resValue("string", "app_name", "Bichim TEST")
        }
    }
```

- [ ] **Step 2: Verificar que o build básico não quebra**

```bash
cd /home/giuliano/rf/capivara_2048
flutter build apk --flavor prod --debug 2>&1 | tail -5
```

Esperado: `Built build/app/outputs/flutter-apk/app-prod-debug.apk`

- [ ] **Step 3: Commit**

```bash
git add android/app/build.gradle.kts
git commit -m "feat(android): add test flavor with applicationIdSuffix .test (Fase 3.6)"
```

---

## Task 3: Ícone amarelo para flavor `test`

**Files:**

- Create: `android/app/src/test/res/mipmap-hdpi/ic_launcher.png`
- Create: `android/app/src/test/res/mipmap-mdpi/ic_launcher.png`
- Create: `android/app/src/test/res/mipmap-xhdpi/ic_launcher.png`
- Create: `android/app/src/test/res/mipmap-xxhdpi/ic_launcher.png`
- Create: `android/app/src/test/res/mipmap-xxxhdpi/ic_launcher.png`

**Nota:** O Flutter usa os ícones de `android/app/src/<flavor>/res/` quando o flavor está ativo. Para o flavor `test`, os ícones ficam em `android/app/src/test/res/`.

- [ ] **Step 1: Criar diretórios de resources para o flavor test**

```bash
mkdir -p android/app/src/test/res/mipmap-mdpi
mkdir -p android/app/src/test/res/mipmap-hdpi
mkdir -p android/app/src/test/res/mipmap-xhdpi
mkdir -p android/app/src/test/res/mipmap-xxhdpi
mkdir -p android/app/src/test/res/mipmap-xxxhdpi
```

- [ ] **Step 2: Copiar ícones do flavor prod como base**

```bash
# Verificar onde estão os ícones atuais
ls android/app/src/main/res/mipmap-*/
```

Se os ícones padrão existirem em `android/app/src/main/res/`:

```bash
cp android/app/src/main/res/mipmap-mdpi/ic_launcher.png android/app/src/test/res/mipmap-mdpi/ic_launcher.png
cp android/app/src/main/res/mipmap-hdpi/ic_launcher.png android/app/src/test/res/mipmap-hdpi/ic_launcher.png
cp android/app/src/main/res/mipmap-xhdpi/ic_launcher.png android/app/src/test/res/mipmap-xhdpi/ic_launcher.png
cp android/app/src/main/res/mipmap-xxhdpi/ic_launcher.png android/app/src/test/res/mipmap-xxhdpi/ic_launcher.png
cp android/app/src/main/res/mipmap-xxxhdpi/ic_launcher.png android/app/src/test/res/mipmap-xxxhdpi/ic_launcher.png
```

**Nota:** Se os ícones forem gerados pelo flutter_launcher_icons e ficarem em outro lugar, ajustar o comando acima conforme o caminho real. O importante é ter algum ícone válido nos diretórios criados. A diferença visual (cor amarela) pode ser adicionada em uma iteração futura; por ora, o diferenciador é o `versionNameSuffix "-TEST"` e o `app_name "Bichim TEST"`.

- [ ] **Step 3: Commit**

```bash
git add android/app/src/test/
git commit -m "feat(android): add test flavor icon resources (Fase 3.6)"
```

---

## Task 4: Criar data model TestResult

**Files:**

- Create: `lib/testing/test_result.dart`

- [ ] **Step 1: Criar o arquivo**

```dart
// lib/testing/test_result.dart
import 'package:flutter/foundation.dart';

enum TestStatus { pending, running, passed, failed, skipped }

class TestResult {
  final String id;
  final String title;
  final String category; // ex: "flow", "engine", "nav"
  final TestStatus status;
  final Duration? duration;
  final String? errorMessage;
  final String? stackTrace;

  const TestResult({
    required this.id,
    required this.title,
    required this.category,
    this.status = TestStatus.pending,
    this.duration,
    this.errorMessage,
    this.stackTrace,
  });

  TestResult copyWith({
    TestStatus? status,
    Duration? duration,
    String? errorMessage,
    String? stackTrace,
  }) {
    return TestResult(
      id: id,
      title: title,
      category: category,
      status: status ?? this.status,
      duration: duration ?? this.duration,
      errorMessage: errorMessage ?? this.errorMessage,
      stackTrace: stackTrace ?? this.stackTrace,
    );
  }

  Map<String, dynamic> toJson() => {
        'id': id,
        'status': status.name,
        'ms': duration?.inMilliseconds,
        if (errorMessage != null) 'error': errorMessage,
        if (stackTrace != null) 'stack': stackTrace,
      };
}

/// ValueNotifier que o tier2_runner popula e o TestRunnerScreen consome.
class TestResultsStore extends ValueNotifier<List<TestResult>> {
  TestResultsStore() : super([]);

  void initWith(List<TestResult> results) {
    value = List.unmodifiable(results);
  }

  void update(String id, TestResult updated) {
    value = List.unmodifiable(
      value.map((r) => r.id == id ? updated : r).toList(),
    );
  }

  int get passed => value.where((r) => r.status == TestStatus.passed).length;
  int get failed => value.where((r) => r.status == TestStatus.failed).length;
  int get skipped => value.where((r) => r.status == TestStatus.skipped).length;
  int get running => value.where((r) => r.status == TestStatus.running).length;
  int get pending => value.where((r) => r.status == TestStatus.pending).length;
  int get total => value.length;

  bool get isDone => pending == 0 && running == 0;

  Map<String, List<TestResult>> get byCategory {
    final map = <String, List<TestResult>>{};
    for (final r in value) {
      (map[r.category] ??= []).add(r);
    }
    return map;
  }
}

/// Singleton global para comunicação entre integration_test e lib/testing.
final testResultsStore = TestResultsStore();
```

- [ ] **Step 2: Verificar que compila**

```bash
cd /home/giuliano/rf/capivara_2048
flutter analyze lib/testing/test_result.dart 2>&1 | tail -5
```

Esperado: `No issues found!`

- [ ] **Step 3: Commit**

```bash
git add lib/testing/test_result.dart
git commit -m "feat(testing): TestResult data model + TestResultsStore (Fase 3.6)"
```

---

## Task 5: Criar share_results

**Files:**

- Create: `lib/testing/share_results.dart`

- [ ] **Step 1: Criar o arquivo**

```dart
// lib/testing/share_results.dart
import 'dart:convert';
import 'dart:io';
import 'dart:ui' as ui;
import 'package:flutter/rendering.dart';
import 'package:flutter/widgets.dart';
import 'package:package_info_plus/package_info_plus.dart';
import 'package:path_provider/path_provider.dart';
import 'package:share_plus/share_plus.dart';
import 'test_result.dart';

/// Exporta resultados como JSON + PNG e abre o share sheet.
Future<void> shareTestResults({
  required TestResultsStore store,
  required GlobalKey screenshotKey,
}) async {
  final packageInfo = await PackageInfo.fromPlatform();
  final tmpDir = await getTemporaryDirectory();

  // JSON
  final json = jsonEncode({
    'build': '${packageInfo.version}+${packageInfo.buildNumber}',
    'totals': {
      'passed': store.passed,
      'failed': store.failed,
      'skipped': store.skipped,
      'total': store.total,
    },
    'results': store.value.map((r) => r.toJson()).toList(),
  });
  final jsonFile = File('${tmpDir.path}/test_results.json');
  await jsonFile.writeAsString(json);

  // PNG screenshot do widget marcado com screenshotKey
  final boundary = screenshotKey.currentContext
      ?.findRenderObject() as RenderRepaintBoundary?;
  final files = <XFile>[XFile(jsonFile.path, mimeType: 'application/json')];

  if (boundary != null) {
    final image = await boundary.toImage(pixelRatio: 2.0);
    final byteData = await image.toByteData(format: ui.ImageByteFormat.png);
    if (byteData != null) {
      final pngFile = File('${tmpDir.path}/test_results.png');
      await pngFile.writeAsBytes(byteData.buffer.asUint8List());
      files.add(XFile(pngFile.path, mimeType: 'image/png'));
    }
  }

  await SharePlus.instance.share(
    ShareParams(
      text: '🐾 Bichim TEST — ${store.passed}✓ ${store.failed}✗ de ${store.total}',
      files: files,
    ),
  );
}
```

- [ ] **Step 2: Verificar compilação**

```bash
flutter analyze lib/testing/share_results.dart 2>&1 | tail -5
```

Esperado: `No issues found!` (ou avisos de import, não erros).

- [ ] **Step 3: Commit**

```bash
git add lib/testing/share_results.dart
git commit -m "feat(testing): share results via share_plus (Fase 3.6)"
```

---

## Task 6: Criar TestRunnerScreen + TestRunnerApp

**Files:**

- Create: `lib/testing/test_runner_screen.dart`
- Create: `lib/testing/test_runner_app.dart`

- [ ] **Step 1: Criar TestRunnerScreen**

```dart
// lib/testing/test_runner_screen.dart
import 'package:flutter/material.dart';
import 'test_result.dart';
import 'share_results.dart';

class TestRunnerScreen extends StatefulWidget {
  final TestResultsStore store;
  final VoidCallback? onRunDemo;

  const TestRunnerScreen({
    super.key,
    required this.store,
    this.onRunDemo,
  });

  @override
  State<TestRunnerScreen> createState() => _TestRunnerScreenState();
}

class _TestRunnerScreenState extends State<TestRunnerScreen> {
  final _screenshotKey = GlobalKey();
  late final _listener = () => setState(() {});

  @override
  void initState() {
    super.initState();
    widget.store.addListener(_listener);
  }

  @override
  void dispose() {
    widget.store.removeListener(_listener);
    super.dispose();
  }

  Color _statusColor(TestStatus s) => switch (s) {
        TestStatus.passed => Colors.green,
        TestStatus.failed => Colors.red,
        TestStatus.running => Colors.orange,
        TestStatus.skipped => Colors.grey,
        TestStatus.pending => Colors.grey.shade400,
      };

  String _statusIcon(TestStatus s) => switch (s) {
        TestStatus.passed => '✓',
        TestStatus.failed => '✗',
        TestStatus.running => '⟳',
        TestStatus.skipped => '⏭',
        TestStatus.pending => '○',
      };

  @override
  Widget build(BuildContext context) {
    final store = widget.store;
    final categories = store.byCategory;

    return RepaintBoundary(
      key: _screenshotKey,
      child: Scaffold(
        backgroundColor: const Color(0xFF1A1A2E),
        appBar: AppBar(
          backgroundColor: const Color(0xFF16213E),
          title: const Text(
            'Bichim — Test Suite',
            style: TextStyle(color: Colors.white, fontWeight: FontWeight.bold),
          ),
          actions: [
            if (store.isDone)
              IconButton(
                icon: const Icon(Icons.share, color: Colors.white),
                tooltip: 'Compartilhar resultados',
                onPressed: () => shareTestResults(
                  store: store,
                  screenshotKey: _screenshotKey,
                ),
              ),
          ],
        ),
        body: Column(
          children: [
            // Totais
            Container(
              width: double.infinity,
              color: const Color(0xFF0F3460),
              padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 8),
              child: Text(
                '${store.passed}✓  ${store.failed}✗  ${store.skipped}⏭  ${store.pending}○  '
                '| total: ${store.total}',
                style: const TextStyle(color: Colors.white, fontSize: 13),
              ),
            ),
            // Lista por categoria
            Expanded(
              child: categories.isEmpty
                  ? const Center(
                      child: Text(
                        'Aguardando cenários...',
                        style: TextStyle(color: Colors.white54),
                      ),
                    )
                  : ListView(
                      children: categories.entries.map((entry) {
                        final cat = entry.key;
                        final results = entry.value;
                        final catPassed =
                            results.where((r) => r.status == TestStatus.passed).length;
                        final catFailed =
                            results.where((r) => r.status == TestStatus.failed).length;

                        return ExpansionTile(
                          initiallyExpanded: catFailed > 0,
                          collapsedBackgroundColor: const Color(0xFF16213E),
                          backgroundColor: const Color(0xFF1A1A2E),
                          title: Row(
                            children: [
                              Text(
                                cat,
                                style: const TextStyle(
                                    color: Colors.white, fontWeight: FontWeight.w600),
                              ),
                              const Spacer(),
                              Text(
                                '✓$catPassed  ✗$catFailed',
                                style: TextStyle(
                                  color: catFailed > 0 ? Colors.red : Colors.green,
                                  fontSize: 12,
                                ),
                              ),
                            ],
                          ),
                          children: results.map((r) => _buildResultTile(r)).toList(),
                        );
                      }).toList(),
                    ),
            ),
            // Botões de ação
            SafeArea(
              child: Padding(
                padding: const EdgeInsets.all(12),
                child: Row(
                  children: [
                    Expanded(
                      child: OutlinedButton.icon(
                        onPressed: store.isDone
                            ? () => shareTestResults(
                                  store: store,
                                  screenshotKey: _screenshotKey,
                                )
                            : null,
                        icon: const Text('📤', style: TextStyle(fontSize: 16)),
                        label: const Text('Compartilhar'),
                        style: OutlinedButton.styleFrom(
                          foregroundColor: Colors.white,
                          side: const BorderSide(color: Colors.white38),
                        ),
                      ),
                    ),
                    const SizedBox(width: 12),
                    Expanded(
                      child: OutlinedButton.icon(
                        onPressed: widget.onRunDemo,
                        icon: const Text('🎬', style: TextStyle(fontSize: 16)),
                        label: const Text('Demo'),
                        style: OutlinedButton.styleFrom(
                          foregroundColor: Colors.amber,
                          side: const BorderSide(color: Colors.amber38),
                        ),
                      ),
                    ),
                  ],
                ),
              ),
            ),
          ],
        ),
      ),
    );
  }

  Widget _buildResultTile(TestResult r) {
    return Theme(
      data: Theme.of(context).copyWith(dividerColor: Colors.transparent),
      child: r.errorMessage != null
          ? ExpansionTile(
              backgroundColor: const Color(0xFF1A1A2E),
              leading: Text(
                _statusIcon(r.status),
                style: TextStyle(
                    color: _statusColor(r.status),
                    fontWeight: FontWeight.bold,
                    fontSize: 16),
              ),
              title: Text(
                r.id,
                style: const TextStyle(color: Colors.white70, fontSize: 12),
              ),
              subtitle: r.duration != null
                  ? Text('${r.duration!.inMilliseconds}ms',
                      style:
                          const TextStyle(color: Colors.white38, fontSize: 11))
                  : null,
              children: [
                Padding(
                  padding:
                      const EdgeInsets.symmetric(horizontal: 16, vertical: 8),
                  child: SelectableText(
                    r.errorMessage ?? '',
                    style: const TextStyle(
                        color: Colors.red, fontSize: 11, fontFamily: 'monospace'),
                  ),
                ),
                if (r.stackTrace != null)
                  Padding(
                    padding: const EdgeInsets.symmetric(
                        horizontal: 16, vertical: 4),
                    child: SelectableText(
                      r.stackTrace!,
                      style: const TextStyle(
                          color: Colors.white38,
                          fontSize: 10,
                          fontFamily: 'monospace'),
                    ),
                  ),
              ],
            )
          : ListTile(
              dense: true,
              leading: Text(
                _statusIcon(r.status),
                style: TextStyle(
                    color: _statusColor(r.status),
                    fontWeight: FontWeight.bold,
                    fontSize: 16),
              ),
              title: Text(
                r.id,
                style: const TextStyle(color: Colors.white70, fontSize: 12),
              ),
              trailing: r.duration != null
                  ? Text('${r.duration!.inMilliseconds}ms',
                      style: const TextStyle(
                          color: Colors.white38, fontSize: 11))
                  : null,
            ),
    );
  }
}
```

- [ ] **Step 2: Criar TestRunnerApp**

```dart
// lib/testing/test_runner_app.dart
import 'package:flutter/material.dart';
import 'test_result.dart';
import 'test_runner_screen.dart';

class TestRunnerApp extends StatelessWidget {
  final TestResultsStore store;
  final VoidCallback? onRunDemo;

  const TestRunnerApp({
    super.key,
    required this.store,
    this.onRunDemo,
  });

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Bichim TEST',
      debugShowCheckedModeBanner: false,
      theme: ThemeData.dark(),
      home: TestRunnerScreen(
        store: store,
        onRunDemo: onRunDemo,
      ),
    );
  }
}
```

- [ ] **Step 3: Verificar compilação**

```bash
flutter analyze lib/testing/ 2>&1 | tail -10
```

Esperado: `No issues found!` ou apenas warnings de imports.

- [ ] **Step 4: Commit**

```bash
git add lib/testing/test_runner_screen.dart lib/testing/test_runner_app.dart
git commit -m "feat(testing): TestRunnerScreen + TestRunnerApp UI (Fase 3.6)"
```

---

## Task 7: Criar lib/main_test.dart

**Files:**

- Create: `lib/main_test.dart`

- [ ] **Step 1: Criar stub de documentação**

```dart
// lib/main_test.dart
//
// Entry point do flavor `test` — usado pela Fase 3.6 do framework E2E.
//
// O APK Tier 2 é construído com:
//   flutter build apk \
//     --target=integration_test/tier2_runner.dart \
//     --flavor test \
//     --release
//
// Este arquivo existe para documentar o entry point alternativo e pode ser
// usado no futuro para uma versão "demo-only" sem integration_test binding.
//
// Para o runner completo (com assertions), use integration_test/tier2_runner.dart.

void main() {
  // Redirecionar para integration_test/tier2_runner.dart via build command.
  // Ver docs/plans/2026-05-05-fase-3.6-tier2-apk-test-runner.md
}
```

- [ ] **Step 2: Commit**

```bash
git add lib/main_test.dart
git commit -m "docs: add lib/main_test.dart stub for Tier 2 APK entry point (Fase 3.6)"
```

---

## Task 8: Taggear cenários demo

**Files:**

- Modify: `test/e2e/flows/smoke_boot.dart` (flow.smoke_boot → demo)
- Modify: `test/e2e/flows/win_flows.dart` (flow.win_2048_first_time → demo)
- Modify: `test/e2e/flows/shop_flows.dart` (flow.shop_purchase_item → demo)
- Modify: `test/e2e/nav/nav_flows.dart` (nav.home_to_collection → demo)
- Modify: `test/e2e/engine/engine_flows.dart` (engine.swipe_left + engine.swipe_right → demo)

Demo subset conforme spec: `flow.smoke_boot`, `flow.win_2048_first_time`, `flow.shop_purchase_item`, `nav.home_to_collection`, `engine.swipe_left`, `engine.swipe_right`.

- [ ] **Step 1: Verificar IDs dos cenários demo no registro**

```bash
grep -n "smokeBootScenario\|win2048\|shopPurchase\|navHomeToCollection\|engineSwipeLeft\|engineSwipeRight" \
  test/e2e/_harness/registry.dart
```

Anote os nomes das variáveis de cada cenário (ex: `smokeBootScenario`, `win2048FirstTimeScenario`, etc.)

- [ ] **Step 2: Adicionar ScenarioTag.demo em smoke_boot.dart**

Em `test/e2e/flows/smoke_boot.dart`, alterar:

```dart
  tags: {ScenarioTag.critical},
```

para:

```dart
  tags: {ScenarioTag.critical, ScenarioTag.demo},
```

- [ ] **Step 3: Adicionar ScenarioTag.demo em win_flows.dart**

Em `test/e2e/flows/win_flows.dart`, localizar `win2048FirstTimeScenario` e alterar sua linha `tags:`:

```dart
  tags: {ScenarioTag.critical, ScenarioTag.demo},
```

(adicionar `ScenarioTag.demo` ao conjunto existente, sem remover tags atuais)

- [ ] **Step 4: Adicionar ScenarioTag.demo em shop_flows.dart**

Em `test/e2e/flows/shop_flows.dart`, localizar `shopPurchaseItemScenario` e adicionar `ScenarioTag.demo` ao conjunto de tags.

- [ ] **Step 5: Adicionar ScenarioTag.demo em nav_flows.dart (nav.home_to_collection)**

Localizar o arquivo correto:

```bash
grep -rn "navHomeToCollectionScenario\|nav.home_to_collection" test/e2e/ --include="*.dart" | head -5
```

Adicionar `ScenarioTag.demo` ao cenário `navHomeToCollectionScenario`.

- [ ] **Step 6: Adicionar ScenarioTag.demo em engine_flows.dart**

Localizar o arquivo correto:

```bash
grep -rn "engineSwipeLeftScenario\|engineSwipeRightScenario\|engine.swipe" test/e2e/ --include="*.dart" | head -5
```

Adicionar `ScenarioTag.demo` aos cenários `engineSwipeLeftScenario` e `engineSwipeRightScenario`.

- [ ] **Step 7: Verificar Tier 1 não quebrou**

```bash
flutter test test/e2e/run_all_test.dart --reporter=compact 2>&1 | tail -10
```

Esperado: todos os testes ainda passam.

- [ ] **Step 8: Commit**

```bash
git add test/e2e/
git commit -m "feat(e2e): tag demo scenarios with ScenarioTag.demo (Fase 3.6)"
```

---

## Task 9: Criar integration_test/tier2_runner.dart

**Files:**

- Create: `integration_test/tier2_runner.dart`

Este é o coração da Fase 3.6: o entry point que o Flutter compila para o APK Tier 2. Ele usa `IntegrationTestWidgetsFlutterBinding`, monta o `TestRunnerApp` como root widget (para mostrar resultados ao vivo), e roda cada cenário em `testWidgets`.

**Nota sobre demo mode:** O runner detecta se está em modo demo via argumento de ambiente `DEMO_MODE=1`. Em demo mode, captura `TestFailure` silenciosamente (apenas ações, sem assertions).

- [ ] **Step 1: Criar o arquivo**

```dart
// integration_test/tier2_runner.dart
//
// Entry point do APK Tier 2. Build com:
//   flutter build apk \
//     --target=integration_test/tier2_runner.dart \
//     --flavor test \
//     --release
//
// Instala em paralelo ao app prod (applicationIdSuffix .test).
// Abre o app, tap Run → vê os testes ao vivo, tap Share → exporta.

import 'dart:io';
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:integration_test/integration_test.dart';
import 'package:capivara_2048/testing/test_result.dart';
import 'package:capivara_2048/testing/test_runner_app.dart';
import '../test/e2e/_harness/registry.dart';
import '../test/e2e/_harness/scenario.dart';
import '../test/e2e/_harness/test_harness.dart';

void main() {
  IntegrationTestWidgetsFlutterBinding.ensureInitialized();

  final bool demoMode =
      Platform.environment['DEMO_MODE'] == '1';

  // 1. Inicializa o store com todos os cenários (pendente)
  final store = testResultsStore;
  final scenarios = demoMode
      ? allScenarios.where((s) => s.tags.contains(ScenarioTag.demo)).toList()
      : allScenarios
          .where((s) => !s.tags.contains(ScenarioTag.tier2Only))
          .toList();

  store.initWith(
    scenarios
        .map((s) => TestResult(
              id: s.id,
              title: s.title,
              category: s.id.split('.').first,
            ))
        .toList(),
  );

  // 2. Monta TestRunnerApp como root (mostra resultados em tempo real)
  testWidgets('Tier 2 — setup runner UI', (tester) async {
    await tester.pumpWidget(TestRunnerApp(store: store));
    await tester.pump(const Duration(milliseconds: 100));
  });

  // 3. Roda cada cenário como testWidgets individual
  for (final scenario in scenarios) {
    testWidgets('[${scenario.id}] ${scenario.title}', (tester) async {
      // Marca como "running"
      store.update(
        scenario.id,
        store.value.firstWhere((r) => r.id == scenario.id).copyWith(
              status: TestStatus.running,
            ),
      );

      final h = GameTestHarness();
      addTearDown(() => tester.runAsync(h.teardown));

      final stopwatch = Stopwatch()..start();
      try {
        await scenario.run(tester, h);
        stopwatch.stop();
        store.update(
          scenario.id,
          store.value.firstWhere((r) => r.id == scenario.id).copyWith(
                status: TestStatus.passed,
                duration: stopwatch.elapsed,
              ),
        );
      } on TestFailure catch (e, st) {
        stopwatch.stop();
        if (demoMode) {
          // Demo mode: ignora falhas de assertion
          store.update(
            scenario.id,
            store.value.firstWhere((r) => r.id == scenario.id).copyWith(
                  status: TestStatus.passed, // visual — não reporta falha
                  duration: stopwatch.elapsed,
                ),
          );
        } else {
          store.update(
            scenario.id,
            store.value.firstWhere((r) => r.id == scenario.id).copyWith(
                  status: TestStatus.failed,
                  duration: stopwatch.elapsed,
                  errorMessage: e.message,
                  stackTrace: st.toString(),
                ),
          );
          rethrow; // propaga para o framework de testes reportar ✗
        }
      } catch (e, st) {
        stopwatch.stop();
        store.update(
          scenario.id,
          store.value.firstWhere((r) => r.id == scenario.id).copyWith(
                status: TestStatus.failed,
                duration: stopwatch.elapsed,
                errorMessage: e.toString(),
                stackTrace: st.toString(),
              ),
        );
        rethrow;
      }
    });
  }
}
```

- [ ] **Step 2: Verificar que o arquivo compila (análise estática)**

```bash
flutter analyze integration_test/tier2_runner.dart 2>&1 | tail -10
```

Possível aviso: `integration_test` importa de `test/e2e/` com path relativo. Se o analyzer reclamar do path, verificar se o caminho `../test/e2e/_harness/registry.dart` resolve corretamente a partir de `integration_test/`.

Esperado: no issues fatais.

- [ ] **Step 3: Commit**

```bash
git add integration_test/tier2_runner.dart
git commit -m "feat(tier2): integration_test runner entry point (Fase 3.6)"
```

---

## Task 10: Build e verificação do APK Tier 2

**Files:**

- Nenhum arquivo novo — verificação de build.

- [ ] **Step 1: Tentar build debug do APK Tier 2**

```bash
cd /home/giuliano/rf/capivara_2048
flutter build apk \
  --target=integration_test/tier2_runner.dart \
  --flavor test \
  --debug \
  2>&1 | tail -20
```

Esperado: `Built build/app/outputs/flutter-apk/app-test-debug.apk`

- [ ] **Step 2: Se houver erros de compilação, resolver**

Erros comuns:

- Import não encontrado: verificar caminhos relativos de `integration_test/` para `test/e2e/`
- Dependência não encontrada: verificar `pubspec.yaml`
- Analyzer error em `lib/testing/`: verificar imports de `share_plus`, `path_provider`

Para import de `path_provider` (necessário em share_results.dart): verificar se está no pubspec.yaml:

```bash
grep "path_provider" pubspec.yaml
```

Se não estiver como dependência (pode estar como override), adicionar:

```yaml
path_provider: ^2.1.0
```

- [ ] **Step 3: Rodar Tier 1 após todas as mudanças**

```bash
flutter test test/e2e/run_all_test.dart --reporter=compact 2>&1 | tail -15
```

Esperado: suite passa (os testes não foram alterados, apenas tags adicionadas).

- [ ] **Step 4: Commit final**

```bash
git add pubspec.yaml pubspec.lock
git commit -m "fix: ensure path_provider in dependencies for share_results (Fase 3.6)" || true
# (só commitar se houver mudança)
```

---

## Task 11: Atualizar CHANGELOG, README, AGENTS.md

**Files:**

- Modify: `CHANGELOG.md`
- Modify: `AGENTS.md` (fase atual)

- [ ] **Step 1: Atualizar CHANGELOG.md**

Adicionar entrada para v1.3.5 no topo do CHANGELOG:

```markdown
## [1.3.5] — 2026-05-05

### Added

- Fase 3.6: APK Tier 2 com `TestRunnerScreen` visual
  - `integration_test/tier2_runner.dart` — entry point do APK de testes
  - `lib/testing/` — TestRunnerApp, TestRunnerScreen, TestResultsStore, share_results
  - Android flavor `test` com `applicationIdSuffix .test` (instala em paralelo)
  - Share button (📤) exporta JSON + PNG via share_plus
  - Demo mode (🎬) via `DEMO_MODE=1` suprime assertions
  - 7 cenários taggeados com `ScenarioTag.demo`
  - `lib/main_test.dart` stub documentando entry point alternativo
```

- [ ] **Step 2: Atualizar AGENTS.md**

Em AGENTS.md, na tabela de fases, alterar a linha de 3.5 e adicionar 3.6:

```markdown
| 3.5 ✅ | `golden.*` com `alchemist` — 15 testes (5 telas × 3 viewports) |
| 3.6 ✅ | Tier 2: APK + TestRunnerScreen + Share + Demo (flavor test, integration_test) |
```

E atualizar a linha de fase atual:

```
Fase atual: **Fase 3.6 concluída (v1.3.5) — próximo: Fase 3.7**
```

- [ ] **Step 3: Commit final da fase**

```bash
git add CHANGELOG.md AGENTS.md
git commit -m "chore: release v1.3.5 — Fase 3.6 Tier 2 APK TestRunnerScreen"
```

---

## Self-Review

### Spec coverage

| Requisito da spec                                | Task       |
| ------------------------------------------------ | ---------- |
| `lib/main_test.dart` entry point                 | Task 7     |
| Build flavor Android `applicationIdSuffix .test` | Task 2     |
| Ícone "Bichim TEST"                              | Task 3     |
| `TestRunnerScreen`: lista ✓/✗/⟳/○ agrupada       | Task 6     |
| Stack trace expansível em falhas                 | Task 6     |
| Share button 📤 PNG + JSON                       | Tasks 5, 6 |
| Demo mode 🎬 filtra tag `demo`, pula `expect()`  | Tasks 8, 9 |
| Demo subset: `flow.*`, `nav.*`, `engine.swipe_*` | Task 8     |
| `share_plus` dependency                          | Task 1     |

### Gaps notáveis (fora do escopo desta fase)

- **Ícone amarelo real**: Task 3 copia o ícone principal sem alterar a cor. Arte definitiva fica para Fase 4.
- **Demo loop infinito com stop button**: O demo mode atual roda uma vez por execução. Loop infinito requer mudanças na arquitetura do `testWidgets` (complexidade alta, defer).
- **Diff visual de goldens no APK**: A tela de falhas de goldens mostra stack trace, não diff visual. Diff visual requer armazenar imagens esperadas no APK.
- **`lib/main_test.dart` executável**: Está como stub de documentação. Para um app demo sem assertions, seria preciso um runner separado (não requerido para o APK Tier 2 funcional).
