# Fase 2.13 — Redesign da Home + Reorganização de Assets

**Versão alvo:** v1.2.0  
**Data:** 2026-05-04  
**Status:** Pronto para implementação

---

## Objetivo

Implementar o novo visual da `HomeScreen` com botões ilustrados PNG, remover o `LivesIndicator` da Home, e consolidar todos os assets visuais sob `assets/images/`, eliminando a pasta `assets/icons/`.

**Referência visual obrigatória:** `menu.jpeg` na raiz do projeto.

---

## Sub-entrega A — Reorganização de assets

### Mudanças no filesystem

```
MOVER (físico):
  assets/icons/inventory/bomb_2.png  →  assets/images/inventory/bomb_2.png
  assets/icons/inventory/bomb_3.png  →  assets/images/inventory/bomb_3.png
  assets/icons/inventory/undo_1.png  →  assets/images/inventory/undo_1.png
  assets/icons/inventory/undo_3.png  →  assets/images/inventory/undo_3.png

DELETAR após mover:
  assets/icons/inventory/  (pasta vazia)
  assets/icons/            (pasta vazia)

REGISTRAR (já existem no disco):
  assets/images/home/Colecao.png
  assets/images/home/ComoJogar.png
  assets/images/home/Configuracao.png
  assets/images/home/IconeLoja.png
  assets/images/home/Ranking.png
  assets/images/home/Recompensas.png
```

> Os SVGs em `assets/icons/inventory/` não são referenciados em código Dart — não precisam ser movidos, mas podem ser deletados junto com a pasta.

### Mudanças no `pubspec.yaml`

```yaml
# Remover:
  - assets/icons/inventory/

# Adicionar:
  - assets/images/inventory/
  - assets/images/home/
```

### Busca e substituição no código

- **Buscar:** `assets/icons/inventory/`
- **Substituir por:** `assets/images/inventory/`
- **27 ocorrências em 7 arquivos:**

| Arquivo | Ocorrências |
|---|---|
| `lib/presentation/widgets/inventory_bar.dart` | 8 |
| `lib/presentation/screens/game/game_over_item_overlay.dart` | 4 |
| `lib/presentation/widgets/game_over_no_items_overlay.dart` | 4 |
| `lib/presentation/widgets/daily_reward_overlay.dart` | 2 |
| `lib/presentation/widgets/daily_reward_day_tile.dart` | 2 |
| `lib/presentation/widgets/shop_unit_item_card.dart` | 4 |
| `lib/app.dart` | 4 |

### Atualizar `precacheImage` em `lib/app.dart`

O `precacheImage` está em `app.dart` (`didChangeDependencies`), **não** na `SplashScreen`. São 4 calls explícitos para inventory. Após a mudança:

1. Corrigir os 4 paths de inventory para `assets/images/inventory/`
2. Adicionar 6 novos calls para os PNGs de home:

```dart
// inventory (paths atualizados)
precacheImage(const AssetImage('assets/images/inventory/bomb_2.png'), context);
precacheImage(const AssetImage('assets/images/inventory/bomb_3.png'), context);
precacheImage(const AssetImage('assets/images/inventory/undo_1.png'), context);
precacheImage(const AssetImage('assets/images/inventory/undo_3.png'), context);

// home buttons (novos)
precacheImage(const AssetImage('assets/images/home/Colecao.png'), context);
precacheImage(const AssetImage('assets/images/home/ComoJogar.png'), context);
precacheImage(const AssetImage('assets/images/home/Configuracao.png'), context);
precacheImage(const AssetImage('assets/images/home/IconeLoja.png'), context);
precacheImage(const AssetImage('assets/images/home/Ranking.png'), context);
precacheImage(const AssetImage('assets/images/home/Recompensas.png'), context);
```

### Critérios de aceite — Sub-entrega A

1. `flutter build apk --debug` sem erro de asset
2. `grep -r "assets/icons" lib/` → zero resultados
3. `assets/icons/` não existe no filesystem
4. Os 4 PNGs físicos existem em `assets/images/inventory/`

---

## Sub-entrega B — Redesign da `HomeScreen`

### Arquivo a modificar

`lib/presentation/screens/home_screen.dart`

### Decisões fechadas

| Ponto | Decisão |
|---|---|
| `LivesIndicator` | **Removido** da Home. Permanece apenas na `GameScreen`. |
| `GameTitleImage` | **Sem mudança** — mantém alternância `title_orange.png` / `title_brown.png` por sessão (lógica do `initState` existente). |
| "Continuar Jogo" | **Provider reativo existente** — `ref.watch(gameProvider)` + `_hasSavedGame()` já funcionam. Zero mudança de lógica. |
| `ComoJogar.png` | **Mantém o modal** `_HowToPlaySheet` atual — sem criar `TutorialScreen` na Fase 2.13. |
| Contorno branco nos botões | **Renderizado em Flutter** via `ColorFiltered + Transform.scale` (sem editar assets). |

### Layout — estrutura do `build`

```
Scaffold (backgroundColor: transparent)
└── Stack (fullscreen)
    ├── GameBackground()                         // fundo.png, BoxFit.cover
    └── SafeArea
        └── Stack
            ├── Positioned(top, left)   → _HomeButton(Colecao.png    → CollectionScreen)
            ├── Positioned(top, right)  → _HomeButton(Configuracao.png → SettingsScreen)
            ├── Center
            │   └── Column(mainAxisSize: min)
            │       ├── GameTitleImage(asset: _titleAsset)   // fade+scale animate
            │       ├── SizedBox(height: titleActionGap)
            │       ├── [se hasCurrentGame] _ActionButton('Continuar Jogo')
            │       ├── SizedBox(height: 12)
            │       └── _ActionButton('Novo jogo')
            ├── Positioned(bottom=rowTop, left)  → _HomeButtonWithBadge(Recompensas.png → DailyRewardsScreen)
            ├── Positioned(bottom=rowTop, right) → _HomeButton(Ranking.png     → RankingScreen)
            ├── Positioned(bottom=rowBase, left) → _HomeButton(IconeLoja.png   → ShopScreen)
            └── Positioned(bottom=rowBase, right)→ _HomeButton(ComoJogar.png  → modal HowToPlay)
```

### `HomeConstants` — `lib/core/constants/home_constants.dart`

Breakpoint em 700dp de altura (separa telas compactas das normais).

```dart
class HomeConstants {
  HomeConstants._();

  static double buttonSize(double screenH)     => screenH < 700 ?  90.0 : 110.0;
  static double edgePad(double screenH)        => screenH < 700 ?   6.0 :   8.0;
  static double rowTopBottom(double screenH)   => screenH < 700 ?  96.0 : 120.0;
  static double rowBaseBottom(double screenH)  => screenH < 700 ?   4.0 :   8.0;
  static double titleActionGap(double screenH) => screenH < 700 ?  20.0 :  32.0;

  static const double actionButtonWidth  = 260.0;
  static const double actionButtonHeight =  52.0;
}
```

Uso: `final h = MediaQuery.of(context).size.height;`

### Widgets novos / modificados

#### `_HomeButton`

```
GestureDetector (onTap: scale 1→0.92→1, 100ms + haptic light)
└── Stack (clipBehavior: Clip.none)
    ├── Transform.scale(scale: 1.06)
    │   └── ColorFiltered(mode: white/srcIn)
    │       └── Image.asset(path, width: buttonSize, fit: BoxFit.contain)
    └── Image.asset(path, width: buttonSize, fit: BoxFit.contain)
```

Sem texto abaixo — label já está impresso no PNG.

#### `_HomeButtonWithBadge`

```
Stack (clipBehavior: Clip.none)
├── _HomeButton(...)
└── [if showBadge] Positioned(top: -4, right: -4)
    └── Container(16dp, círculo vermelho #EF5350, texto '!')
```

#### `_ActionButton`

```
GestureDetector (onTap: scale 1→0.95→1, 100ms)
└── Container(
      width: 260, height: 52, borderRadius: 30,
      color: Colors.white.withOpacity(0.80),
      shadow: BoxShadow(black26, blur:8, offset:(0,3))
    )
    └── Text(label, Fredoka Bold 20, #3E2723)
```

### Remover da `HomeScreen`

- Widget `LivesIndicator` e seu import
- Classe `_HomeCard`
- Classe `_PlayButton`
- Import de `lives_notifier.dart` (se não usado em outro lugar)
- Grid `GridView.count` com os 6 cards antigos

---

## Casos de teste obrigatórios

### Sub-entrega A

```dart
// Verificações de estrutura (podem ser testes de integração ou CI script)
test('pubspec registra assets/images/inventory/ e assets/images/home/')
test('pubspec NÃO registra assets/icons/')
test('zero refs a assets/icons no código Dart')
test('4 PNGs existem em assets/images/inventory/')
test('pasta assets/icons/ não existe')
```

### Sub-entrega B — `home_screen_test.dart`

```dart
testWidgets('HomeScreen não contém LivesIndicator')
testWidgets('6 Image widgets com paths assets/images/home/ presentes')
testWidgets('GameTitleImage presente')
testWidgets('"Novo jogo" sempre visível')
testWidgets('"Continuar Jogo" visível apenas quando score > 0 && !isGameOver')
testWidgets('"Continuar Jogo" ausente quando gameState é novo')
testWidgets('badge visível quando dailyRewardAvailable == true')
testWidgets('badge ausente quando dailyRewardAvailable == false')
testWidgets('tap Coleção → CollectionScreen')
testWidgets('tap Configurações → SettingsScreen')
testWidgets('tap Recompensas → DailyRewardsScreen')
testWidgets('tap Ranking → RankingScreen')
testWidgets('tap Loja → ShopScreen')
testWidgets('tap ComoJogar → BottomSheet presente na widget tree')
```

---

## Plano de validação visual

| Passo | 360×640 | 390×844 |
|---|---|---|
| Zero RenderFlex overflow no console | ✓ | ✓ |
| 4 botões inferiores visíveis sem scroll | ✓ | ✓ |
| Título não sobrepõe botões de ação | ✓ | ✓ |
| "Continuar Jogo"+"Novo jogo" sem corte | ✓ | ✓ |
| Badge de Recompensas não clipado pelo SafeArea | ✓ | ✓ |
| Contorno branco visível nos 6 botões | ✓ | ✓ |

---

## Arquivos afetados — resumo completo

### Sub-entrega A
- `pubspec.yaml`
- `lib/app.dart`
- `lib/presentation/widgets/inventory_bar.dart`
- `lib/presentation/screens/game/game_over_item_overlay.dart`
- `lib/presentation/widgets/game_over_no_items_overlay.dart`
- `lib/presentation/widgets/daily_reward_overlay.dart`
- `lib/presentation/widgets/daily_reward_day_tile.dart`
- `lib/presentation/widgets/shop_unit_item_card.dart`
- *(filesystem)* mover 4 PNGs, deletar `assets/icons/`

### Sub-entrega B
- `lib/presentation/screens/home_screen.dart` *(rewrite)*
- `lib/core/constants/home_constants.dart` *(novo)*
- `test/presentation/screens/home_screen_test.dart` *(novo)*
