# Fase 2.3.10 — Reorganização do cabeçalho + Anfitrião 2×2

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reorganizar o cabeçalho da `GameScreen` em 3 zonas distintas e expandir o anfitrião de 1×1 para 2×2 tiles.

**Architecture:** Extrair `GameHeader` como `ConsumerWidget` separado. Um `Padding(horizontal: 12dp)` pai compartilhado alinha header e board no mesmo eixo. `HostBanner` ocupa cols 1-2 (esquerda), `StatusPanel` + `PauseButtonTile` empilhados ocupam cols 3-4 (direita). Entrega A cria a estrutura; Entrega B preenche com conteúdo 2×2.

**Tech Stack:** Flutter 3.x, Dart, Riverpod, flutter_test, google_fonts

---

## File Map

| Arquivo | Ação | Responsabilidade |
|---------|------|-----------------|
| `lib/presentation/widgets/game_header.dart` | **CRIAR** | Cabeçalho completo: LivesIndicator + Row(HostBanner, Column(StatusPanel, PauseBtn)) |
| `lib/presentation/screens/game/game_screen.dart` | **MODIFICAR** | Remove padding duplo; adiciona Padding(12) pai; usa GameHeader() |
| `lib/presentation/widgets/host_banner.dart` | **MODIFICAR** | Slot 2×2, nome 16sp, placeholder com silhueta Capivara |
| `lib/presentation/widgets/host_artwork.dart` | **MODIFICAR** | BoxFit.cover (era contain) |
| `lib/presentation/widgets/status_panel.dart` | **MODIFICAR** | Fontes 18/24/13sp, CrossAxisAlignment.center |
| `lib/presentation/screens/debug/animals_gallery_screen.dart` | **MODIFICAR** | Adiciona coluna host 2×2 na galeria |
| `test/presentation/game_header_test.dart` | **CRIAR** | Testes do novo GameHeader |
| `test/presentation/host_banner_test.dart` | **MODIFICAR** | Atualizar para slot 2×2, placeholder novo |
| `test/presentation/game_screen_layout_test.dart` | **MODIFICAR** | Atualizar para nova estrutura |
| `test/presentation/animals_gallery_screen_test.dart` | **MANTER** | Já passa; não muda estrutura de nomes |

---

## ENTREGA A — Estrutura do cabeçalho

---

### Task 1: Criar `game_header.dart` com estrutura básica

**Files:**
- Create: `lib/presentation/widgets/game_header.dart`
- Create: `test/presentation/game_header_test.dart`

- [ ] **Step 1: Escrever o teste que verifica os 4 widgets presentes na árvore**

```dart
// test/presentation/game_header_test.dart
import 'package:capivara_2048/presentation/widgets/game_header.dart';
import 'package:capivara_2048/presentation/widgets/host_banner.dart';
import 'package:capivara_2048/presentation/widgets/lives_indicator.dart';
import 'package:capivara_2048/presentation/widgets/pause_button_tile.dart';
import 'package:capivara_2048/presentation/widgets/status_panel.dart';
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';

Widget _wrap(Widget child) => ProviderScope(
      child: MaterialApp(home: Scaffold(body: child)),
    );

void main() {
  group('GameHeader', () {
    testWidgets('renderiza LivesIndicator, HostBanner, StatusPanel e PauseButtonTile', (tester) async {
      await tester.pumpWidget(_wrap(GameHeader(onPauseTap: () {})));
      await tester.pump();
      expect(find.byType(LivesIndicator), findsOneWidget);
      expect(find.byType(HostBanner), findsOneWidget);
      expect(find.byType(StatusPanel), findsOneWidget);
      expect(find.byType(PauseButtonTile), findsOneWidget);
    });
  });
}
```

- [ ] **Step 2: Rodar o teste e confirmar que falha**

```bash
cd /home/giuliano/rf/capivara_2048
flutter test test/presentation/game_header_test.dart -v
```

Esperado: FAIL — `GameHeader` não existe ainda.

- [ ] **Step 3: Criar `game_header.dart` com implementação mínima**

```dart
// lib/presentation/widgets/game_header.dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import '../../core/constants/game_constants.dart';
import '../controllers/game_notifier.dart';
import 'host_banner.dart';
import 'lives_indicator.dart';
import 'pause_button_tile.dart';
import 'status_panel.dart';

class GameHeader extends ConsumerWidget {
  final VoidCallback onPauseTap;

  const GameHeader({super.key, required this.onPauseTap});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    const tileSize = GameConstants.tileSize;
    const slotWidth = GameConstants.twoCellWidth;

    return Column(
      mainAxisSize: MainAxisSize.min,
      crossAxisAlignment: CrossAxisAlignment.stretch,
      children: [
        const LivesIndicator(),
        const SizedBox(height: 12),
        Row(
          crossAxisAlignment: CrossAxisAlignment.center,
          children: [
            const Expanded(
              flex: 2,
              child: HostBanner(),
            ),
            Expanded(
              flex: 2,
              child: Column(
                mainAxisSize: MainAxisSize.min,
                children: [
                  const StatusPanel(),
                  const SizedBox(height: 6),
                  SizedBox(
                    width: slotWidth,
                    height: tileSize,
                    child: Align(
                      alignment: Alignment.centerLeft,
                      child: PauseButtonTile(
                        tileSize: tileSize,
                        onTap: onPauseTap,
                      ),
                    ),
                  ),
                ],
              ),
            ),
          ],
        ),
        const SizedBox(height: 6),
      ],
    );
  }
}
```

- [ ] **Step 4: Rodar o teste e confirmar que passa**

```bash
flutter test test/presentation/game_header_test.dart -v
```

Esperado: PASS.

- [ ] **Step 5: Commit**

```bash
git add lib/presentation/widgets/game_header.dart test/presentation/game_header_test.dart
git commit -m "feat: GameHeader widget — estrutura cabeçalho com LivesIndicator, HostBanner, StatusPanel, PauseButtonTile"
```

---

### Task 2: Testar posicionamento relativo (HostBanner esquerda, StatusPanel+Pause direita)

**Files:**
- Modify: `test/presentation/game_header_test.dart`

- [ ] **Step 1: Adicionar testes de posicionamento ao arquivo existente**

Adicionar dentro do `group('GameHeader', ...)` em `test/presentation/game_header_test.dart`:

```dart
    testWidgets('HostBanner está à esquerda do StatusPanel', (tester) async {
      await tester.pumpWidget(_wrap(GameHeader(onPauseTap: () {})));
      await tester.pump();
      final hostPos = tester.getTopLeft(find.byType(HostBanner));
      final statusPos = tester.getTopLeft(find.byType(StatusPanel));
      expect(hostPos.dx, lessThan(statusPos.dx));
    });

    testWidgets('tap no PauseButtonTile dispara onPauseTap', (tester) async {
      bool tapped = false;
      await tester.pumpWidget(_wrap(GameHeader(onPauseTap: () => tapped = true)));
      await tester.pump();
      await tester.tap(find.byType(PauseButtonTile));
      await tester.pumpAndSettle();
      expect(tapped, isTrue);
    });

    testWidgets('sem overflow em 360dp de largura', (tester) async {
      tester.view.physicalSize = const Size(360, 800);
      tester.view.devicePixelRatio = 1.0;
      addTearDown(tester.view.resetPhysicalSize);
      await tester.pumpWidget(_wrap(GameHeader(onPauseTap: () {})));
      await tester.pump();
      expect(tester.takeException(), isNull);
    });
```

- [ ] **Step 2: Rodar os testes e confirmar que passam**

```bash
flutter test test/presentation/game_header_test.dart -v
```

Esperado: todos PASS.

- [ ] **Step 3: Commit**

```bash
git add test/presentation/game_header_test.dart
git commit -m "test: GameHeader — posicionamento, tap callback e overflow 360dp"
```

---

### Task 3: Integrar `GameHeader` na `GameScreen`

**Files:**
- Modify: `lib/presentation/screens/game/game_screen.dart`
- Modify: `test/presentation/game_screen_layout_test.dart`

- [ ] **Step 1: Atualizar o teste de layout da GameScreen**

Substituir o conteúdo de `test/presentation/game_screen_layout_test.dart`:

```dart
import 'package:capivara_2048/presentation/widgets/game_header.dart';
import 'package:capivara_2048/presentation/widgets/pause_button_tile.dart';
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';

Widget _wrap(Widget child) => ProviderScope(
      child: MaterialApp(home: Scaffold(body: child)),
    );

void main() {
  group('GameScreen layout', () {
    testWidgets('PauseButtonTile tem tileSize de 72dp', (tester) async {
      await tester.pumpWidget(
        MaterialApp(
          home: Scaffold(
            body: PauseButtonTile(tileSize: 72, onTap: () {}),
          ),
        ),
      );
      final size = tester.getSize(find.byType(PauseButtonTile));
      expect(size.width, 72.0);
      expect(size.height, 72.0);
    });

    testWidgets('GameHeader está presente na GameScreen', (tester) async {
      await tester.pumpWidget(_wrap(const _FakeGameScreen()));
      await tester.pump();
      expect(find.byType(GameHeader), findsOneWidget);
    });
  });
}

// Fake mínimo para não depender de providers complexos no teste estrutural
class _FakeGameScreen extends StatelessWidget {
  const _FakeGameScreen();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Padding(
        padding: const EdgeInsets.symmetric(horizontal: 12),
        child: Column(
          children: [
            GameHeader(onPauseTap: () {}),
            const Expanded(child: Placeholder()),
          ],
        ),
      ),
    );
  }
}
```

- [ ] **Step 2: Rodar o teste (espera FAIL porque GameHeader não está na GameScreen real ainda)**

```bash
flutter test test/presentation/game_screen_layout_test.dart -v
```

O teste `GameHeader está presente na GameScreen` passa porque usa `_FakeGameScreen`. Os demais devem passar também.

- [ ] **Step 3: Refatorar `game_screen.dart` para usar `GameHeader`**

Substituir o conteúdo de `lib/presentation/screens/game/game_screen.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import '../../../core/constants/game_constants.dart';
import '../../../data/animals_data.dart';
import '../../../data/models/game_state.dart';
import '../../../domain/game_engine/direction.dart';
import '../../../domain/lives/lives_notifier.dart';
import '../../controllers/game_notifier.dart';
import '../../widgets/board_widget.dart';
import '../../widgets/bomb_selection_overlay.dart';
import '../../widgets/game_background.dart';
import '../../widgets/game_header.dart';
import '../../widgets/game_over_modal.dart';
import '../../widgets/inventory_bar.dart';
import '../../widgets/pause_overlay.dart';

class GameScreen extends ConsumerWidget {
  const GameScreen({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final state = ref.watch(gameProvider);
    final isGameOver = state.isGameOver;
    final hasWon = state.hasWon;
    final notifier = ref.read(gameProvider.notifier);
    final hostAnimal =
        state.maxLevel > 0 ? animalForLevel(state.maxLevel) : null;

    ref.listen<GameState>(gameProvider, (prev, next) {
      if (prev != null && !prev.isGameOver && next.isGameOver && !next.hasWon) {
        ref.read(livesProvider.notifier).consume();
      }
    });

    return Scaffold(
      body: GameBackground(
        animal: hostAnimal,
        child: SafeArea(
          child: Stack(
            children: [
              Padding(
                padding: const EdgeInsets.symmetric(horizontal: 12),
                child: Column(
                  children: [
                    GameHeader(
                      onPauseTap: state.isPaused
                          ? notifier.resume
                          : notifier.pause,
                    ),
                    GestureDetector(
                      behavior: HitTestBehavior.opaque,
                      onPanEnd: (details) {
                        if (state.isPaused ||
                            isGameOver ||
                            hasWon ||
                            state.bombMode != null) return;
                        final v = details.velocity.pixelsPerSecond;
                        const threshold = 100.0;
                        if (v.dx.abs() > v.dy.abs()) {
                          if (v.dx > threshold) {
                            notifier.onSwipe(Direction.right);
                          } else if (v.dx < -threshold) {
                            notifier.onSwipe(Direction.left);
                          }
                        } else {
                          if (v.dy > threshold) {
                            notifier.onSwipe(Direction.down);
                          } else if (v.dy < -threshold) {
                            notifier.onSwipe(Direction.up);
                          }
                        }
                      },
                      child: const RepaintBoundary(child: BoardWidget()),
                    ),
                    const Spacer(),
                    const InventoryBar(),
                    const SizedBox(height: 8),
                  ],
                ),
              ),
              if (state.isPaused) const Positioned.fill(child: PauseOverlay()),
              if (state.bombMode != null)
                const Positioned.fill(child: BombSelectionOverlay()),
              if (isGameOver)
                const Positioned.fill(
                    child: GameOverModal(message: 'Game Over!')),
              if (hasWon && !isGameOver)
                const Positioned.fill(
                    child: GameOverModal(message: 'Capivara Lendária! 🎉')),
            ],
          ),
        ),
      ),
    );
  }
}
```

- [ ] **Step 4: Rodar todos os testes**

```bash
flutter test -v
```

Esperado: todos passam (ou só snapshots quebrados — aceitável neste passo).

- [ ] **Step 5: Commit**

```bash
git add lib/presentation/screens/game/game_screen.dart test/presentation/game_screen_layout_test.dart
git commit -m "refactor: GameScreen usa GameHeader, Padding(12) pai compartilhado com board"
```

---

## ENTREGA B — Anfitrião 2×2 + StatusPanel escalado

---

### Task 4: Atualizar `HostArtwork` para `BoxFit.cover`

**Files:**
- Modify: `lib/presentation/widgets/host_artwork.dart`

- [ ] **Step 1: Não há teste específico para BoxFit — verificar que o widget existente compila e renderiza**

```bash
flutter test test/presentation/host_banner_test.dart -v
```

Anotar resultado atual (linha de base).

- [ ] **Step 2: Alterar `BoxFit.contain` para `BoxFit.cover` em `host_artwork.dart`**

```dart
// lib/presentation/widgets/host_artwork.dart
import 'package:flutter/material.dart';
import '../../core/constants/game_constants.dart';
import '../../data/models/animal.dart';

class HostArtwork extends StatelessWidget {
  final Animal animal;
  final double size;

  const HostArtwork({
    super.key,
    required this.animal,
    this.size = GameConstants.tileSize,
  });

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      width: size,
      height: size,
      child: Image.asset(
        animal.hostPngPath,
        fit: BoxFit.cover,
        errorBuilder: (_, __, ___) => const SizedBox.shrink(),
      ),
    );
  }
}
```

- [ ] **Step 3: Rodar testes**

```bash
flutter test -v
```

Esperado: todos passam (mudança não quebra contratos existentes).

- [ ] **Step 4: Commit**

```bash
git add lib/presentation/widgets/host_artwork.dart
git commit -m "feat: HostArtwork usa BoxFit.cover — PNGs são quadrados, sem risco de corte"
```

---

### Task 5: Atualizar `HostBanner` para slot 2×2 com placeholder novo

**Files:**
- Modify: `lib/presentation/widgets/host_banner.dart`
- Modify: `test/presentation/host_banner_test.dart`

- [ ] **Step 1: Escrever os novos testes antes de alterar o widget**

Substituir `test/presentation/host_banner_test.dart`:

```dart
import 'package:capivara_2048/core/constants/game_constants.dart';
import 'package:capivara_2048/presentation/widgets/host_banner.dart';
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';

Widget _wrap(Widget child) => ProviderScope(
      child: MaterialApp(home: Scaffold(body: child)),
    );

void main() {
  group('HostBanner', () {
    testWidgets('renderiza sem erro', (tester) async {
      await tester.pumpWidget(_wrap(const HostBanner()));
      await tester.pump();
      expect(find.byType(HostBanner), findsOneWidget);
    });

    testWidgets('largura igual a twoCellWidth (152dp)', (tester) async {
      await tester.pumpWidget(_wrap(const HostBanner()));
      await tester.pump();
      final size = tester.getSize(find.byType(HostBanner));
      expect(size.width, GameConstants.twoCellWidth);
    });

    testWidgets('placeholder mostra texto Comece! quando maxLevel == 0', (tester) async {
      await tester.pumpWidget(_wrap(const HostBanner()));
      await tester.pump();
      expect(find.text('Comece!'), findsOneWidget);
    });

    testWidgets('nome Mico-leão-dourado não causa overflow em 152dp', (tester) async {
      // Renderiza com largura restrita a 152dp
      await tester.pumpWidget(
        _wrap(
          SizedBox(
            width: GameConstants.twoCellWidth,
            child: const HostBanner(),
          ),
        ),
      );
      await tester.pump();
      expect(tester.takeException(), isNull);
    });
  });
}
```

- [ ] **Step 2: Rodar os testes e confirmar falha no teste de largura 152dp**

```bash
flutter test test/presentation/host_banner_test.dart -v
```

Esperado: teste de `largura igual a twoCellWidth` falha (ainda retorna 72dp).

- [ ] **Step 3: Atualizar `host_banner.dart` para slot 2×2**

```dart
// lib/presentation/widgets/host_banner.dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:google_fonts/google_fonts.dart';
import '../../core/constants/game_constants.dart';
import '../../core/theme/text_styles.dart';
import '../../data/animals_data.dart';
import '../controllers/game_notifier.dart';
import 'host_artwork.dart';

class HostBanner extends ConsumerWidget {
  const HostBanner({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final maxLevel = ref.watch(gameProvider.select((s) => s.maxLevel));
    const slotWidth = GameConstants.twoCellWidth;

    return SizedBox(
      width: slotWidth,
      child: AnimatedSwitcher(
        duration: const Duration(milliseconds: 400),
        transitionBuilder: (child, anim) =>
            FadeTransition(opacity: anim, child: child),
        child: maxLevel == 0
            ? _Placeholder(key: const ValueKey('ph'), slotWidth: slotWidth)
            : _AnimalHost(
                key: ValueKey(maxLevel),
                level: maxLevel,
                slotWidth: slotWidth,
              ),
      ),
    );
  }
}

class _Placeholder extends StatelessWidget {
  final double slotWidth;
  const _Placeholder({super.key, required this.slotWidth});

  @override
  Widget build(BuildContext context) {
    final slotHeight = GameConstants.twoCellWidth; // slot quadrado 2×2
    return Semantics(
      label: 'Anfitrião: nenhum. Faça seu primeiro merge!',
      child: SizedBox(
        width: slotWidth,
        height: slotHeight + 28, // +28 reserva para o nome
        child: Stack(
          alignment: Alignment.center,
          children: [
            Opacity(
              opacity: 0.15,
              child: Image.asset(
                'assets/images/animals/host/Capivara.png',
                width: slotWidth,
                height: slotHeight,
                fit: BoxFit.cover,
                errorBuilder: (_, __, ___) => const SizedBox.shrink(),
              ),
            ),
            Text(
              'Comece!',
              style: outlinedWhiteTextStyle(
                GoogleFonts.nunito(fontSize: 13, fontWeight: FontWeight.w600),
              ),
              textAlign: TextAlign.center,
            ),
          ],
        ),
      ),
    );
  }
}

class _AnimalHost extends StatelessWidget {
  final int level;
  final double slotWidth;
  const _AnimalHost({super.key, required this.level, required this.slotWidth});

  @override
  Widget build(BuildContext context) {
    final animal = animalForLevel(level);
    return Semantics(
      label: 'Anfitrião: ${animal.name}',
      child: Column(
        mainAxisSize: MainAxisSize.min,
        children: [
          Text(
            animal.name,
            style: outlinedWhiteTextStyle(
              GoogleFonts.fredoka(fontSize: 16, fontWeight: FontWeight.w600),
            ),
            textAlign: TextAlign.center,
            maxLines: 2,
            overflow: TextOverflow.ellipsis,
          ),
          const SizedBox(height: 4),
          HostArtwork(animal: animal, size: slotWidth),
        ],
      ),
    );
  }
}
```

- [ ] **Step 4: Rodar testes do HostBanner**

```bash
flutter test test/presentation/host_banner_test.dart -v
```

Esperado: todos PASS.

- [ ] **Step 5: Rodar suite completa**

```bash
flutter test -v
```

Esperado: todos passam.

- [ ] **Step 6: Commit**

```bash
git add lib/presentation/widgets/host_banner.dart test/presentation/host_banner_test.dart
git commit -m "feat: HostBanner 2×2 — slotWidth 152dp, placeholder silhueta Capivara, nome 16sp"
```

---

### Task 6: Escalar fontes do `StatusPanel`

**Files:**
- Modify: `lib/presentation/widgets/status_panel.dart`

- [ ] **Step 1: Não há testes de fonte no arquivo atual — criar um antes de mudar**

Criar `test/presentation/status_panel_test.dart`:

```dart
import 'package:capivara_2048/presentation/widgets/status_panel.dart';
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';

Widget _wrap(Widget child) => ProviderScope(
      child: MaterialApp(home: Scaffold(body: child)),
    );

void main() {
  group('StatusPanel', () {
    testWidgets('renderiza sem PauseButtonTile na subárvore', (tester) async {
      await tester.pumpWidget(_wrap(const StatusPanel()));
      await tester.pump();
      // Verifica que não há botão de pause no StatusPanel
      expect(find.byKey(const Key('pause_button')), findsNothing);
    });

    testWidgets('renderiza sem erro', (tester) async {
      await tester.pumpWidget(_wrap(const StatusPanel()));
      await tester.pump();
      expect(find.byType(StatusPanel), findsOneWidget);
    });
  });
}
```

- [ ] **Step 2: Rodar o novo teste**

```bash
flutter test test/presentation/status_panel_test.dart -v
```

Esperado: PASS.

- [ ] **Step 3: Atualizar fontes e alinhamento em `status_panel.dart`**

```dart
// lib/presentation/widgets/status_panel.dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:google_fonts/google_fonts.dart';
import '../../core/theme/text_styles.dart';
import '../controllers/game_notifier.dart';

class StatusPanel extends ConsumerWidget {
  const StatusPanel({super.key});

  String _formatTime(int elapsedMs) {
    if (elapsedMs == 0) return '--:--:--';
    final total = elapsedMs ~/ 1000;
    final hh = (total ~/ 3600).toString().padLeft(2, '0');
    final mm = ((total % 3600) ~/ 60).toString().padLeft(2, '0');
    final ss = (total % 60).toString().padLeft(2, '0');
    return '$hh:$mm:$ss';
  }

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final score = ref.watch(gameProvider.select((s) => s.score));
    final highScore = ref.watch(gameProvider.select((s) => s.highScore));
    final elapsedMs = ref.watch(gameProvider.select((s) => s.elapsedMs));

    return Column(
      crossAxisAlignment: CrossAxisAlignment.center,
      mainAxisSize: MainAxisSize.min,
      children: [
        Text(
          _formatTime(elapsedMs),
          style: outlinedWhiteTextStyle(
            GoogleFonts.fredoka(fontSize: 18, fontWeight: FontWeight.bold),
          ),
        ),
        const SizedBox(height: 2),
        Text(
          '$score',
          style: outlinedWhiteTextStyle(
            GoogleFonts.fredoka(fontSize: 24, fontWeight: FontWeight.bold),
          ),
        ),
        Text(
          'Recorde: $highScore',
          style: outlinedWhiteTextStyle(
            GoogleFonts.nunito(fontSize: 13),
          ),
        ),
      ],
    );
  }
}
```

- [ ] **Step 4: Rodar testes**

```bash
flutter test -v
```

Esperado: todos passam.

- [ ] **Step 5: Commit**

```bash
git add lib/presentation/widgets/status_panel.dart test/presentation/status_panel_test.dart
git commit -m "feat: StatusPanel — fontes 18/24/13sp, CrossAxisAlignment.center"
```

---

### Task 7: Atualizar galeria de debug para mostrar host 2×2

**Files:**
- Modify: `lib/presentation/screens/debug/animals_gallery_screen.dart`
- Modify: `test/presentation/animals_gallery_screen_test.dart`

- [ ] **Step 1: Adicionar teste para o novo modo 2×2 na galeria**

Adicionar ao final do `main()` em `test/presentation/animals_gallery_screen_test.dart`:

```dart
  testWidgets('AnimalsGalleryScreen mostra coluna Host 2×2', (tester) async {
    await tester.pumpWidget(
      const MaterialApp(home: AnimalsGalleryScreen()),
    );
    await tester.pump();
    expect(find.text('Host 2×2'), findsAtLeastNWidgets(1));
  });
```

- [ ] **Step 2: Rodar o teste e confirmar que falha**

```bash
flutter test test/presentation/animals_gallery_screen_test.dart -v
```

Esperado: FAIL — `Host 2×2` não existe ainda.

- [ ] **Step 3: Atualizar `animals_gallery_screen.dart` para incluir coluna host 2×2**

Substituir a classe `_AnimalRow`:

```dart
class _AnimalRow extends StatelessWidget {
  final Animal animal;
  const _AnimalRow({required this.animal});

  @override
  Widget build(BuildContext context) {
    const slot2x = GameConstants.twoCellWidth;
    return Column(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        Text(
          'Nível ${animal.level} — ${animal.name} — ${animal.value}',
          style: const TextStyle(fontWeight: FontWeight.bold, fontSize: 14),
        ),
        const SizedBox(height: 8),
        Row(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            Column(
              children: [
                const Text('Tile', style: TextStyle(fontSize: 10)),
                const SizedBox(height: 4),
                _TilePreview(animal: animal),
              ],
            ),
            const SizedBox(width: 16),
            Column(
              children: [
                const Text('Host 1×1', style: TextStyle(fontSize: 10)),
                const SizedBox(height: 4),
                HostArtwork(animal: animal),
              ],
            ),
            const SizedBox(width: 16),
            Column(
              children: [
                const Text('Host 2×2', style: TextStyle(fontSize: 10)),
                const SizedBox(height: 4),
                HostArtwork(animal: animal, size: slot2x),
              ],
            ),
          ],
        ),
        const SizedBox(height: 8),
        Row(
          children: [
            _ColorChip(label: 'Border', color: animal.borderColor),
            const SizedBox(width: 8),
            _ColorChip(label: 'Bg', color: animal.backgroundBaseColor),
          ],
        ),
      ],
    );
  }
}
```

Adicionar import no topo do arquivo (se não existir):
```dart
import '../../../core/constants/game_constants.dart';
```

- [ ] **Step 4: Rodar testes da galeria**

```bash
flutter test test/presentation/animals_gallery_screen_test.dart -v
```

Esperado: todos PASS.

- [ ] **Step 5: Rodar suite completa**

```bash
flutter test -v
```

Esperado: todos passam.

- [ ] **Step 6: Commit**

```bash
git add lib/presentation/screens/debug/animals_gallery_screen.dart test/presentation/animals_gallery_screen_test.dart
git commit -m "feat: galeria debug — coluna Host 2×2 para validação visual dos 11 animais"
```

---

### Task 8: Validação visual e limpeza final

**Files:**
- No new files

- [ ] **Step 1: Rodar suite completa de testes**

```bash
flutter test -v 2>&1 | tail -20
```

Esperado: todos os testes passam. Se houver snapshot tests quebrados pelo redesign, regenerar:

```bash
flutter test --update-goldens
```

- [ ] **Step 2: Abrir a galeria de debug no app e percorrer os 11 animais**

```bash
flutter run -d <device>
```

Navegar até `/debug/animals_gallery`. Verificar coluna `Host 2×2` para cada animal:
- Nenhum animal com barras brancas (cover garante isso para PNGs quadrados)
- "Mico-leão-dourado" cabe no nome sem truncar visualmente de forma estranha
- Placeholder "Comece!" aparece na posição correta na `GameScreen` antes do primeiro merge

- [ ] **Step 3: Commit final de versão**

```bash
git add -p  # revisar mudanças residuais
git commit -m "chore: bump version to 0.7.0+11 (Fase 2.3.10)"
```

(Atualizar `pubspec.yaml` com `version: 0.7.0+11` antes do commit.)

---

## Checklist de critérios de aceite

- [ ] Anfitrião à esquerda (cols 1-2), StatusPanel + Pause à direita (cols 3-4)
- [ ] Pause não toca StatusPanel acima (≥6dp), não toca board abaixo (≥6dp)
- [ ] Placeholder: silhueta Capivara visível (opacity 0.15) + "Comece!" legível
- [ ] Nome do anfitrião em 16sp Fredoka SemiBold
- [ ] StatusPanel: cronômetro 18sp, pontuação 24sp, recorde 13sp
- [ ] PauseButtonTile: 72×72 alinhado à esquerda no slot 2×1
- [ ] Header e board compartilham padding horizontal 12dp
- [ ] Galeria debug mostra colunas Tile, Host 1×1 e Host 2×2
- [ ] Todos os 143+ testes passam
