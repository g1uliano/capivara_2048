# Fase 2.3.11 — Tanajura inicial + Fundo unificado Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Exibir Tanajura como anfitrião desde o boot (em vez do placeholder "Comece!") e aplicar o mesmo `fundo.png` na `HomeScreen`, unificando o background com a `GameScreen`.

**Architecture:** Dois grupos independentes de mudanças. Grupo 1 (Tasks 1–3): `maxLevel` passa a iniciar em 1 no engine e no notifier; `_Placeholder` é removido do `HostBanner`; guard em `GameScreen` é eliminado. Grupo 2 (Tasks 4–5): `GameBackground` perde o parâmetro `animal` morto; `HomeScreen` passa a usar `GameBackground`. Task 6 é cosmética (nota na galeria de debug).

**Tech Stack:** Flutter 3.x / Dart, flutter_test, Riverpod ProviderContainer nos testes

---

## File Map

| Arquivo | Operação |
|---|---|
| `lib/domain/game_engine/game_engine.dart` | Modificar: `maxLevel: 1` em `newGame()` |
| `lib/presentation/controllers/game_notifier.dart` | Modificar: `maxLevel: 1` em `restart()` |
| `lib/presentation/widgets/host_banner.dart` | Modificar: remover `_Placeholder`, remover branch `maxLevel == 0` |
| `lib/presentation/screens/game/game_screen.dart` | Modificar: remover guard `maxLevel > 0`, remover arg `animal` no `GameBackground` |
| `lib/presentation/widgets/game_background.dart` | Modificar: remover parâmetro `animal` |
| `lib/presentation/screens/home_screen.dart` | Modificar: usar `GameBackground` em vez de `backgroundColor: AppColors.primary` |
| `lib/presentation/screens/debug/animals_gallery_screen.dart` | Modificar: adicionar nota textual sobre Tanajura inicial |
| `test/domain/game_engine_test.dart` | Modificar: corrigir `maxLevel starts at 0` → `starts at 1` |
| `test/presentation/game_notifier_test.dart` | Modificar: adicionar asserção `maxLevel == 1` pós-restart |
| `test/presentation/host_banner_test.dart` | Modificar: substituir teste do placeholder por teste Tanajura inicial |
| `test/presentation/game_background_test.dart` | Modificar: remover arg `animal: null` dos call sites |

---

## Task 1: `maxLevel` inicia em 1 — engine + notifier

**Files:**
- Modify: `lib/domain/game_engine/game_engine.dart` (método `newGame`, linha ~27)
- Modify: `lib/presentation/controllers/game_notifier.dart` (método `restart`, linha ~93)
- Test: `test/domain/game_engine_test.dart`
- Test: `test/presentation/game_notifier_test.dart`

- [ ] **Step 1: Corrigir o teste de engine**

Em `test/domain/game_engine_test.dart`, localizar o grupo `'maxLevel tracking'` e substituir o teste existente:

```dart
// ANTES:
test('maxLevel starts at 0 on newGame', () {
  final state = engine.newGame();
  expect(state.maxLevel, 0);
});

// DEPOIS:
test('maxLevel starts at 1 on newGame', () {
  final state = engine.newGame();
  expect(state.maxLevel, 1);
});
```

- [ ] **Step 2: Rodar o teste para confirmar que falha**

```bash
cd /home/giuliano/rf/capivara_2048
flutter test test/domain/game_engine_test.dart --name "maxLevel starts at 1" 2>&1 | tail -20
```

Esperado: `FAIL` — `Expected: <1>  Actual: <0>`

- [ ] **Step 3: Implementar `maxLevel: 1` em `newGame()`**

Em `lib/domain/game_engine/game_engine.dart`, linha ~27, dentro de `newGame()`, adicionar `maxLevel: 1` ao construtor de `GameState`:

```dart
var state = GameState(
  board: board,
  score: 0,
  highScore: 0,
  isGameOver: false,
  hasWon: false,
  maxLevel: 1,
);
```

- [ ] **Step 4: Rodar o teste para confirmar que passa**

```bash
flutter test test/domain/game_engine_test.dart --name "maxLevel starts at 1" 2>&1 | tail -10
```

Esperado: `PASS`

- [ ] **Step 5: Adicionar teste de restart no notifier**

Em `test/presentation/game_notifier_test.dart`, dentro do grupo `'GameNotifier.restart'` (bloco do `test('restart resets elapsedMs and isPaused', ...)`), adicionar um novo test logo abaixo:

```dart
test('restart resets maxLevel to 1', () {
  final container = ProviderContainer();
  addTearDown(container.dispose);
  // Simular maxLevel alto (após alguns moves hipotéticos)
  // restart() deve sempre voltar para 1
  container.read(gameProvider.notifier).restart();
  final state = container.read(gameProvider);
  expect(state.maxLevel, 1);
});
```

- [ ] **Step 6: Rodar para confirmar que falha**

```bash
flutter test test/presentation/game_notifier_test.dart --name "restart resets maxLevel to 1" 2>&1 | tail -10
```

Esperado: `FAIL` — `Expected: <1>  Actual: <0>`

- [ ] **Step 7: Corrigir `maxLevel: 0` → `maxLevel: 1` em `restart()`**

Em `lib/presentation/controllers/game_notifier.dart`, método `restart()`:

```dart
void restart() {
  _stopTimer();
  _timerStarted = false;
  final fresh = _engine.newGame();
  state = fresh.copyWith(
    elapsedMs: 0,
    isPaused: false,
    maxLevel: 1,
  );
}
```

- [ ] **Step 8: Rodar todos os testes do notifier**

```bash
flutter test test/presentation/game_notifier_test.dart 2>&1 | tail -15
```

Esperado: todos passam.

- [ ] **Step 9: Rodar todos os testes**

```bash
flutter test 2>&1 | tail -20
```

Esperado: todos passam (exceto possivelmente `host_banner_test` e `game_background_test` que serão corrigidos nas tasks seguintes — anotar quais falharam).

- [ ] **Step 10: Commit**

```bash
git add lib/domain/game_engine/game_engine.dart \
        lib/presentation/controllers/game_notifier.dart \
        test/domain/game_engine_test.dart \
        test/presentation/game_notifier_test.dart
git commit -m "feat: maxLevel inicia em 1 — Tanajura como anfitrião desde o boot"
```

---

## Task 2: Remover `_Placeholder` do `HostBanner`

**Files:**
- Modify: `lib/presentation/widgets/host_banner.dart`
- Modify: `lib/presentation/screens/game/game_screen.dart`
- Test: `test/presentation/host_banner_test.dart`

- [ ] **Step 1: Corrigir o teste do placeholder**

Em `test/presentation/host_banner_test.dart`, substituir:

```dart
// ANTES:
testWidgets('placeholder mostra texto Comece! quando maxLevel == 0', (tester) async {
  await tester.pumpWidget(_wrap(const HostBanner()));
  await tester.pump();
  expect(find.text('Comece!'), findsOneWidget);
});

// DEPOIS:
testWidgets('exibe Tanajura no estado inicial (maxLevel == 1)', (tester) async {
  await tester.pumpWidget(_wrap(const HostBanner()));
  await tester.pump();
  expect(find.text('Tanajura'), findsOneWidget);
  expect(find.text('Comece!'), findsNothing);
});
```

- [ ] **Step 2: Rodar para confirmar que falha**

```bash
flutter test test/presentation/host_banner_test.dart --name "exibe Tanajura" 2>&1 | tail -15
```

Esperado: `FAIL` — `Comece!` encontrado, `Tanajura` não encontrado.

- [ ] **Step 3: Reescrever `HostBanner` — remover `_Placeholder`, remover branch `maxLevel == 0`**

Substituir o conteúdo completo de `lib/presentation/widgets/host_banner.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:google_fonts/google_fonts.dart';
import '../../core/constants/game_constants.dart';
import '../../core/theme/text_styles.dart';
import '../../data/animals_data.dart';
import '../controllers/game_notifier.dart';
import 'host_artwork.dart';

class HostBanner extends ConsumerWidget {
  const HostBanner({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final maxLevel = ref.watch(gameProvider.select((s) => s.maxLevel));
    const slotWidth = GameConstants.twoCellWidth;

    return SizedBox(
      width: slotWidth,
      child: AnimatedSwitcher(
        duration: const Duration(milliseconds: 400),
        transitionBuilder: (child, anim) =>
            FadeTransition(opacity: anim, child: child),
        child: _AnimalHost(
          key: ValueKey(maxLevel),
          level: maxLevel,
          slotWidth: slotWidth,
        ),
      ),
    );
  }
}

class _AnimalHost extends StatelessWidget {
  final int level;
  final double slotWidth;
  const _AnimalHost({super.key, required this.level, required this.slotWidth});

  @override
  Widget build(BuildContext context) {
    final animal = animalForLevel(level);
    return Semantics(
      label: 'Anfitrião: ${animal.name}',
      child: Column(
        mainAxisSize: MainAxisSize.min,
        children: [
          Text(
            animal.name,
            style: outlinedWhiteTextStyle(
              GoogleFonts.fredoka(fontSize: 16, fontWeight: FontWeight.w600),
            ),
            textAlign: TextAlign.center,
            maxLines: 2,
            overflow: TextOverflow.ellipsis,
          ),
          const SizedBox(height: 4),
          HostArtwork(animal: animal, size: slotWidth),
        ],
      ),
    );
  }
}
```

- [ ] **Step 4: Remover guard `maxLevel > 0` em `GameScreen`**

Em `lib/presentation/screens/game/game_screen.dart`, substituir:

```dart
// ANTES:
final hostAnimal =
    state.maxLevel > 0 ? animalForLevel(state.maxLevel) : null;
```

por (remover a variável `hostAnimal` inteiramente — ela não é mais necessária pois `GameBackground` não usa `animal`). Isso será consolidado na Task 4 quando removermos o parâmetro `animal` de `GameBackground`. Por ora, apenas remover o `? null` tornando o código direto:

```dart
// Nenhuma mudança necessária neste step — o guard em game_screen.dart
// será removido em Task 4 junto com a remoção do parâmetro animal.
// Pular este step; verificar apenas que host_banner_test passa.
```

*(Nota: a Task 4 cuida do `game_screen.dart` de forma coordenada com `GameBackground`.)*

- [ ] **Step 5: Rodar testes do host_banner**

```bash
flutter test test/presentation/host_banner_test.dart 2>&1 | tail -15
```

Esperado: todos passam.

- [ ] **Step 6: Rodar todos os testes**

```bash
flutter test 2>&1 | tail -20
```

Esperado: todos passam (ou falham apenas nos `game_background_test` / `game_screen` que ainda têm `animal: null` — anotar).

- [ ] **Step 7: Commit**

```bash
git add lib/presentation/widgets/host_banner.dart \
        test/presentation/host_banner_test.dart
git commit -m "feat: HostBanner — remove _Placeholder, Tanajura sempre visível (Fase 2.3.11)"
```

---

## Task 3: Nota na galeria de debug

**Files:**
- Modify: `lib/presentation/screens/debug/animals_gallery_screen.dart`

- [ ] **Step 1: Adicionar nota no topo da lista**

Em `lib/presentation/screens/debug/animals_gallery_screen.dart`, dentro de `AnimalsGalleryScreen.build()`, adicionar um item estático no topo do `ListView`. Substituir o `ListView.separated` por uma versão com header:

```dart
@override
Widget build(BuildContext context) {
  return Scaffold(
    appBar: AppBar(title: const Text('Galeria de Animais')),
    body: ListView.separated(
      padding: const EdgeInsets.all(16),
      itemCount: animals.length + 1,
      separatorBuilder: (_, __) => const Divider(height: 32),
      itemBuilder: (context, index) {
        if (index == 0) {
          return const Text(
            'Nível 1 (Tanajura) é o anfitrião inicial desde a Fase 2.3.11 (maxLevel = 1).',
            style: TextStyle(fontSize: 12, color: Colors.black54),
          );
        }
        final animal = animals[index - 1];
        return _AnimalRow(animal: animal);
      },
    ),
  );
}
```

- [ ] **Step 2: Rodar testes da galeria**

```bash
flutter test test/presentation/animals_gallery_screen_test.dart 2>&1 | tail -15
```

Esperado: todos passam.

- [ ] **Step 3: Commit**

```bash
git add lib/presentation/screens/debug/animals_gallery_screen.dart
git commit -m "docs: galeria debug — nota Tanajura como anfitrião inicial (Fase 2.3.11)"
```

---

## Task 4: Remover parâmetro `animal` de `GameBackground`

**Files:**
- Modify: `lib/presentation/widgets/game_background.dart`
- Modify: `lib/presentation/screens/game/game_screen.dart`
- Test: `test/presentation/game_background_test.dart`

- [ ] **Step 1: Corrigir os testes de `GameBackground`**

Em `test/presentation/game_background_test.dart`, remover `animal: null,` dos três call sites:

```dart
// ANTES (3 ocorrências):
GameBackground(
  animal: null,
  child: SizedBox(width: 100, height: 100),
)

// DEPOIS:
GameBackground(
  child: SizedBox(width: 100, height: 100),
)
```

Fazer isso nas três ocorrências (linhas ~11, ~28, ~44).

- [ ] **Step 2: Rodar para confirmar que falha**

```bash
flutter test test/presentation/game_background_test.dart 2>&1 | tail -15
```

Esperado: erro de compilação — `animal` ainda é parâmetro obrigatório.

- [ ] **Step 3: Reescrever `GameBackground` sem o parâmetro `animal`**

Substituir o conteúdo completo de `lib/presentation/widgets/game_background.dart`:

```dart
import 'package:flutter/material.dart';
import '../../core/constants/app_colors.dart';

class GameBackground extends StatelessWidget {
  final Widget child;

  const GameBackground({super.key, required this.child});

  @override
  Widget build(BuildContext context) {
    return RepaintBoundary(
      child: DecoratedBox(
        decoration: const BoxDecoration(
          color: AppColors.gameBackground,
          image: DecorationImage(
            image: AssetImage('assets/images/fundo.png'),
            fit: BoxFit.cover,
          ),
        ),
        child: child,
      ),
    );
  }
}
```

- [ ] **Step 4: Corrigir `game_screen.dart` — remover `hostAnimal` e atualizar call site**

Em `lib/presentation/screens/game/game_screen.dart`, remover a linha com `hostAnimal` e simplificar o `GameBackground`:

```dart
// ANTES:
final hostAnimal =
    state.maxLevel > 0 ? animalForLevel(state.maxLevel) : null;
// ...
body: GameBackground(
  animal: hostAnimal,
  child: SafeArea(...)
)

// DEPOIS (remover as duas linhas de hostAnimal; simplificar GameBackground):
body: GameBackground(
  child: SafeArea(...)
)
```

Se o import de `animals_data.dart` só era usado para `animalForLevel` (chamado via `hostAnimal`), remover também o import. Verificar se há outros usos na mesma tela antes de remover.

- [ ] **Step 5: Rodar testes do `game_background` e `game_screen`**

```bash
flutter test test/presentation/game_background_test.dart test/presentation/game_screen_layout_test.dart 2>&1 | tail -20
```

Esperado: todos passam.

- [ ] **Step 6: Rodar todos os testes**

```bash
flutter test 2>&1 | tail -20
```

Esperado: todos passam.

- [ ] **Step 7: Commit**

```bash
git add lib/presentation/widgets/game_background.dart \
        lib/presentation/screens/game/game_screen.dart \
        test/presentation/game_background_test.dart
git commit -m "refactor: GameBackground — remove parâmetro animal (dead code)"
```

---

## Task 5: Aplicar `GameBackground` na `HomeScreen`

**Files:**
- Modify: `lib/presentation/screens/home_screen.dart`

- [ ] **Step 1: Adicionar import e envolver o corpo com `GameBackground`**

Em `lib/presentation/screens/home_screen.dart`:

1. Adicionar import no topo (após os imports existentes):
```dart
import '../widgets/game_background.dart';
```

2. Substituir o `Scaffold` raiz para usar `backgroundColor: Colors.transparent` e envolver o `body` com `GameBackground`:

```dart
// ANTES:
return Scaffold(
  backgroundColor: AppColors.primary,
  body: SafeArea(
    child: Padding(
      padding: const EdgeInsets.symmetric(horizontal: 32, vertical: 16),
      child: Column(
        children: [
          // ... conteúdo
        ],
      ),
    ),
  ),
);

// DEPOIS:
return GameBackground(
  child: Scaffold(
    backgroundColor: Colors.transparent,
    body: SafeArea(
      child: Padding(
        padding: const EdgeInsets.symmetric(horizontal: 32, vertical: 16),
        child: Column(
          children: [
            // ... conteúdo (inalterado)
          ],
        ),
      ),
    ),
  ),
);
```

Verificar se `AppColors.primary` pode ser removido dos imports se não for mais usado na tela (outros botões podem ainda usar `AppColors.primary` — conferir antes de remover).

- [ ] **Step 2: Rodar testes da HomeScreen**

```bash
flutter test test/presentation/ 2>&1 | tail -20
```

Esperado: todos passam. Se houver testes de snapshot (golden) da HomeScreen falhando devido à mudança de background, regenerar:

```bash
flutter test test/presentation/ --update-goldens 2>&1 | tail -20
```

- [ ] **Step 3: Rodar todos os testes**

```bash
flutter test 2>&1 | tail -20
```

Esperado: todos passam.

- [ ] **Step 4: Commit**

```bash
git add lib/presentation/screens/home_screen.dart
git commit -m "feat: HomeScreen usa GameBackground — fundo unificado com GameScreen (Fase 2.3.11)"
```

---

## Task 6: Verificação final e release

**Files:** nenhum arquivo novo

- [ ] **Step 1: Rodar suite completa**

```bash
flutter test 2>&1 | tail -30
```

Esperado: todos os testes passam. Anotar o número total de testes.

- [ ] **Step 2: Build smoke test (opcional mas recomendado)**

```bash
flutter analyze 2>&1 | tail -20
```

Esperado: zero erros, zero warnings relevantes.

- [ ] **Step 3: Atualizar CLAUDE.md — fase atual**

Em `CLAUDE.md`, linha com `Fase atual:`, atualizar:

```
Fase atual: **Fase 2.3.11 concluída (v0.8.0) — próximo: Fase 2.4 (áudio)**.
```

- [ ] **Step 4: Commit final e tag**

```bash
git add CLAUDE.md
git commit -m "chore: CLAUDE.md — Fase 2.3.11 concluída (v0.8.0)"
```

---

## Resumo de testes a ajustar

| Teste | Ação |
|---|---|
| `game_engine_test.dart` — `maxLevel starts at 0` | Task 1: corrigir para `starts at 1` |
| `game_notifier_test.dart` | Task 1: adicionar `maxLevel == 1` pós-restart |
| `host_banner_test.dart` — `placeholder mostra Comece!` | Task 2: substituir por teste Tanajura inicial |
| `game_background_test.dart` — `animal: null` (3x) | Task 4: remover argumento |
| Goldens HomeScreen (se existirem) | Task 5: `--update-goldens` |
