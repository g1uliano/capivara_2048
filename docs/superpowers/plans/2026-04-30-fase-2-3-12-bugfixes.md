# Fase 2.3.12 — Bugfixes de Layout, Regen e Ícones do Inventário — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Corrigir 4 bugs identificados após 2.3.11 — centralização do `LivesIndicator`, alinhamento do `HostBanner`, timer de regen de vidas e PNGs finais do inventário.

**Architecture:** Quatro tasks independentes em ordem crescente de complexidade: layout puro (A, B) → domínio com timer e lifecycle (C) → apresentação de assets (D). Tasks A e B tocam os mesmos arquivos de widget e devem ser executadas em sequência; C e D são independentes entre si.

**Tech Stack:** Flutter 3.x / Dart, Riverpod (StateNotifier), `dart:async` (Timer), `AppLifecycleListener` (Flutter), `fake_async` (testes de timer).

---

## File Map

| Arquivo | Task | Ação |
|---------|------|------|
| `lib/presentation/widgets/game_header.dart` | A, B | Modify |
| `lib/presentation/screens/home_screen.dart` | A | Modify |
| `lib/domain/lives/lives_notifier.dart` | C | Modify |
| `lib/presentation/widgets/inventory_bar.dart` | D | Modify |
| `lib/presentation/widgets/confirm_use_dialog.dart` | D | Modify |
| `lib/app.dart` | D | Modify |
| `test/presentation/game_header_test.dart` | A, B | Modify |
| `test/presentation/home_screen_test.dart` (se existir) | A | Modify |
| `test/domain/lives_notifier_test.dart` | C | Modify (add group) |
| `test/presentation/inventory_bar_test.dart` | D | Modify |
| `test/presentation/confirm_use_dialog_test.dart` | D | Modify |

---

## Task 1: Item A — Centralizar LivesIndicator

**Files:**
- Modify: `lib/presentation/widgets/game_header.dart`
- Modify: `lib/presentation/screens/home_screen.dart`
- Modify: `test/presentation/game_header_test.dart`

- [ ] **Step 1: Adicionar teste que verifica centralização em GameHeader**

Abrir `test/presentation/game_header_test.dart`. Adicionar dentro de `group('GameHeader', ...)`, antes do teste de overflow:

```dart
testWidgets('LivesIndicator está horizontalmente centralizado', (tester) async {
  await tester.pumpWidget(_wrap(GameHeader(onPauseTap: () {})));
  await tester.pump();
  final headerBox = tester.getRect(find.byType(GameHeader));
  final indicatorBox = tester.getRect(find.byType(LivesIndicator));
  final headerCenter = headerBox.left + headerBox.width / 2;
  final indicatorCenter = indicatorBox.left + indicatorBox.width / 2;
  expect(indicatorCenter, closeTo(headerCenter, 2.0));
});
```

- [ ] **Step 2: Rodar o teste para confirmar que falha**

```bash
flutter test test/presentation/game_header_test.dart --name "está horizontalmente centralizado" -v
```

Esperado: FAIL (indicatorCenter != headerCenter).

- [ ] **Step 3: Aplicar fix em game_header.dart**

Abrir `lib/presentation/widgets/game_header.dart`. Trocar:

```dart
const LivesIndicator(),
```

por:

```dart
const Center(child: LivesIndicator()),
```

(Linha ~20, primeiro filho do `Column`.)

- [ ] **Step 4: Rodar testes de GameHeader**

```bash
flutter test test/presentation/game_header_test.dart -v
```

Esperado: todos PASS.

- [ ] **Step 5: Adicionar teste que verifica centralização em HomeScreen**

Abrir `test/presentation/` — verificar se `home_screen_test.dart` existe. Se existir, adicionar teste nele. Se não existir, criar o arquivo:

```dart
import 'dart:io';
import 'package:capivara_2048/data/models/lives_state_adapter.dart';
import 'package:capivara_2048/presentation/screens/home_screen.dart';
import 'package:capivara_2048/presentation/widgets/lives_indicator.dart';
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:hive/hive.dart';
import 'package:shared_preferences/shared_preferences.dart';

Widget _wrap(Widget child) => ProviderScope(
      child: MaterialApp(home: child),
    );

void main() {
  late Directory tempDir;

  setUpAll(() async {
    tempDir = await Directory.systemTemp.createTemp('hive_home_screen_test');
    Hive.init(tempDir.path);
    if (!Hive.isAdapterRegistered(1)) {
      Hive.registerAdapter(LivesStateAdapter());
    }
  });

  tearDownAll(() async {
    await Hive.close();
    await tempDir.delete(recursive: true);
  });

  setUp(() {
    SharedPreferences.setMockInitialValues({});
  });

  group('HomeScreen', () {
    testWidgets('LivesIndicator está horizontalmente centralizado', (tester) async {
      await tester.pumpWidget(_wrap(const HomeScreen()));
      await tester.pump();
      final screenBox = tester.getRect(find.byType(HomeScreen));
      final indicatorBox = tester.getRect(find.byType(LivesIndicator));
      final screenCenter = screenBox.left + screenBox.width / 2;
      final indicatorCenter = indicatorBox.left + indicatorBox.width / 2;
      expect(indicatorCenter, closeTo(screenCenter, 2.0));
    });
  });
}
```

- [ ] **Step 6: Rodar o teste de HomeScreen para confirmar que falha**

```bash
flutter test test/presentation/home_screen_test.dart -v
```

Esperado: FAIL.

- [ ] **Step 7: Aplicar fix em home_screen.dart**

Abrir `lib/presentation/screens/home_screen.dart`. Trocar linhas 29–32:

```dart
const Align(
  alignment: Alignment.centerRight,
  child: LivesIndicator(),
),
```

por:

```dart
const Center(child: LivesIndicator()),
```

- [ ] **Step 8: Rodar todos os testes afetados**

```bash
flutter test test/presentation/game_header_test.dart test/presentation/home_screen_test.dart -v
```

Esperado: todos PASS.

- [ ] **Step 9: Commit**

```bash
git add lib/presentation/widgets/game_header.dart \
        lib/presentation/screens/home_screen.dart \
        test/presentation/game_header_test.dart \
        test/presentation/home_screen_test.dart
git commit -m "fix: centralizar LivesIndicator em GameHeader e HomeScreen (Fase 2.3.12-A)"
```

---

## Task 2: Item B — Colar HostBanner à coluna 1

**Files:**
- Modify: `lib/presentation/widgets/game_header.dart`
- Modify: `test/presentation/game_header_test.dart`

- [ ] **Step 1: Adicionar teste que verifica flush-left do HostBanner**

Adicionar em `test/presentation/game_header_test.dart`, dentro de `group('GameHeader', ...)`:

```dart
testWidgets('HostBanner está no lado esquerdo sem espaço à esquerda', (tester) async {
  tester.view.physicalSize = const Size(400, 800);
  tester.view.devicePixelRatio = 1.0;
  addTearDown(tester.view.resetPhysicalSize);
  await tester.pumpWidget(_wrap(GameHeader(onPauseTap: () {})));
  await tester.pump();
  final headerBox = tester.getRect(find.byType(GameHeader));
  final hostBox = tester.getRect(find.byType(HostBanner));
  // HostBanner deve começar na borda esquerda do GameHeader (tolerância 2px)
  expect(hostBox.left, closeTo(headerBox.left, 2.0));
});

testWidgets('PauseButtonTile está à direita do HostBanner', (tester) async {
  await tester.pumpWidget(_wrap(GameHeader(onPauseTap: () {})));
  await tester.pump();
  final hostBox = tester.getRect(find.byType(HostBanner));
  final pauseBox = tester.getRect(find.byType(PauseButtonTile));
  expect(pauseBox.left, greaterThan(hostBox.right));
});
```

- [ ] **Step 2: Rodar os testes novos para confirmar que falham**

```bash
flutter test test/presentation/game_header_test.dart --name "está no lado esquerdo" -v
flutter test test/presentation/game_header_test.dart --name "está à direita do HostBanner" -v
```

Esperado: FAIL (HostBanner não está flush-left com Expanded atual).

- [ ] **Step 3: Aplicar fix em game_header.dart**

Abrir `lib/presentation/widgets/game_header.dart`. Substituir o `Row` inteiro (linhas ~23–44):

```dart
Row(
  crossAxisAlignment: CrossAxisAlignment.center,
  children: [
    const Expanded(
      flex: 2,
      child: HostBanner(),
    ),
    Expanded(
      flex: 2,
      child: Column(
        mainAxisSize: MainAxisSize.min,
        crossAxisAlignment: CrossAxisAlignment.end,
        children: [
          const StatusPanel(),
          const SizedBox(height: 6),
          SizedBox(
            width: slotWidth,
            height: tileSize,
            child: Align(
              alignment: Alignment.centerRight,
              child: PauseButtonTile(
                tileSize: tileSize,
                onTap: onPauseTap,
              ),
            ),
          ),
        ],
      ),
    ),
  ],
),
```

por:

```dart
Row(
  crossAxisAlignment: CrossAxisAlignment.center,
  children: [
    const HostBanner(),
    const Spacer(),
    Column(
      mainAxisSize: MainAxisSize.min,
      crossAxisAlignment: CrossAxisAlignment.end,
      children: [
        const StatusPanel(),
        const SizedBox(height: 6),
        PauseButtonTile(
          tileSize: tileSize,
          onTap: onPauseTap,
        ),
      ],
    ),
  ],
),
```

Nota: `SizedBox` em volta do `PauseButtonTile` removido — o tamanho agora vem do próprio widget. Se `PauseButtonTile` precisar de tamanho fixo, verificar sua implementação e adicionar `SizedBox(width: slotWidth, height: tileSize, child: PauseButtonTile(...))` se necessário.

- [ ] **Step 4: Rodar todos os testes de GameHeader**

```bash
flutter test test/presentation/game_header_test.dart -v
```

Esperado: todos PASS, incluindo o teste de overflow em 360dp.

- [ ] **Step 5: Verificar overflow em tela estreita**

```bash
flutter test test/presentation/game_header_test.dart --name "sem overflow" -v
```

Esperado: PASS.

- [ ] **Step 6: Commit**

```bash
git add lib/presentation/widgets/game_header.dart \
        test/presentation/game_header_test.dart
git commit -m "fix: HostBanner flush-left com Spacer, remove Expanded duplo (Fase 2.3.12-B)"
```

---

## Task 3: Item C — Timer de Regeneração de Vidas

**Files:**
- Modify: `lib/domain/lives/lives_notifier.dart`
- Modify: `test/domain/lives_notifier_test.dart`

- [ ] **Step 1: Adicionar dependência fake_async**

Verificar se `fake_async` já está em `pubspec.yaml` (dev_dependencies):

```bash
grep fake_async pubspec.yaml
```

Se não estiver:

```bash
flutter pub add --dev fake_async
```

- [ ] **Step 2: Escrever testes de regen com timer (grupo novo)**

Adicionar no final de `test/domain/lives_notifier_test.dart`, antes do `}` final do `main()`:

```dart
group('regen timer (LivesNotifier._onTick)', () {
  // Testa o comportamento puro de _onTick via calcRegen diretamente,
  // pois o timer real requer integração. Os testes abaixo validam a
  // lógica que _onTick executa.

  test('calcRegen após 30s não ganha vida (janela não fechou)', () {
    final last = DateTime.now().subtract(const Duration(seconds: 30));
    final s = _state(lives: 3, lastRegenAt: last);
    final result = LivesNotifier.calcRegen(state: s, now: DateTime.now());
    // 30 segundos < 30 minutos
    expect(result.lives, 3);
  });

  test('calcRegen após 30min ganha 1 vida', () {
    final last = DateTime.now().subtract(const Duration(minutes: 30));
    final s = _state(lives: 3, lastRegenAt: last);
    final result = LivesNotifier.calcRegen(state: s, now: DateTime.now());
    expect(result.lives, 4);
  });

  test('calcRegen com lives == regenCap não altera estado', () {
    final last = DateTime.now().subtract(const Duration(minutes: 60));
    final s = _state(lives: 5, regenCap: 5, lastRegenAt: last);
    final result = LivesNotifier.calcRegen(state: s, now: DateTime.now());
    expect(result.lives, 5);
    // Deve retornar a mesma instância (sem emit)
    expect(identical(result, s), isTrue);
  });

  test('resumeRegen offline 6h: lives == 0 → lives == 5', () {
    final last = DateTime.now().subtract(const Duration(hours: 6));
    final s = _state(lives: 0, regenCap: 5, lastRegenAt: last);
    final result = LivesNotifier.calcRegen(state: s, now: DateTime.now());
    expect(result.lives, 5);
  });

  test('resumeRegen offline 6h com lives == 2: clamp a 5', () {
    final last = DateTime.now().subtract(const Duration(hours: 6));
    final s = _state(lives: 2, regenCap: 5, lastRegenAt: last);
    final result = LivesNotifier.calcRegen(state: s, now: DateTime.now());
    expect(result.lives, 5);
  });

  test('calcRegen com remaining negativo (lastRegenAt no futuro): sem mudança', () {
    // Edge case: relógio do sistema foi ajustado para trás
    final last = DateTime.now().add(const Duration(minutes: 5));
    final s = _state(lives: 3, lastRegenAt: last);
    final result = LivesNotifier.calcRegen(state: s, now: DateTime.now());
    expect(result.lives, 3);
  });
});
```

- [ ] **Step 3: Rodar os novos testes para confirmar que passam (são testes de lógica pura)**

```bash
flutter test test/domain/lives_notifier_test.dart --name "regen timer" -v
```

Esperado: todos PASS (calcRegen já é correto — os testes validam comportamento existente).

- [ ] **Step 4: Implementar _regenTimer e _lifecycleListener em lives_notifier.dart**

Abrir `lib/domain/lives/lives_notifier.dart`. Fazer as seguintes mudanças:

**1. Adicionar import no topo:**

```dart
import 'package:flutter/widgets.dart';
```

(Adicionar após `import 'dart:async';`)

**2. Adicionar campos na classe, após `final _ready = Completer<void>();`:**

```dart
Timer? _regenTimer;
AppLifecycleListener? _lifecycleListener;
```

**3. Substituir o final de `_init()` (após `_ready.complete();` no branch não-migration):**

Encontrar o bloco:
```dart
var loaded = await _repo.load();
loaded = calcRegen(state: loaded, now: DateTime.now());
state = loaded;
await _repo.save(state);
_ready.complete();
```

Substituir por:
```dart
var loaded = await _repo.load();
loaded = calcRegen(state: loaded, now: DateTime.now());
state = loaded;
await _repo.save(state);
_ready.complete();
_startRegenTimer();
_lifecycleListener = AppLifecycleListener(
  onPause: _pauseRegen,
  onResume: _resumeRegen,
);
```

**4. Adicionar os 4 métodos auxiliares antes de `consume()`:**

```dart
void _startRegenTimer() {
  _regenTimer = Timer.periodic(const Duration(seconds: 30), (_) => _onRegenTick());
}

void _onRegenTick() {
  final before = state.lives;
  final updated = calcRegen(state: state, now: DateTime.now());
  if (updated.lives != before) {
    state = updated;
    _repo.save(state);
  }
}

void _pauseRegen() {
  _regenTimer?.cancel();
  _regenTimer = null;
}

void _resumeRegen() {
  final updated = calcRegen(state: state, now: DateTime.now());
  if (updated.lives != state.lives) {
    state = updated;
    _repo.save(state);
  }
  _startRegenTimer();
}
```

**5. Adicionar override de `dispose()` após `rewardFromAd()`:**

```dart
@override
void dispose() {
  _regenTimer?.cancel();
  _lifecycleListener?.dispose();
  super.dispose();
}
```

- [ ] **Step 5: Verificar que todos os testes existentes continuam passando**

```bash
flutter test test/domain/lives_notifier_test.dart -v
```

Esperado: todos PASS (incluindo os grupos anteriores de `applyRegen`, `consume`, etc.).

- [ ] **Step 6: Rodar suite completa de testes do projeto**

```bash
flutter test --reporter=compact
```

Esperado: 0 failures.

- [ ] **Step 7: Commit**

```bash
git add lib/domain/lives/lives_notifier.dart \
        test/domain/lives_notifier_test.dart
git commit -m "feat: Timer.periodic regen + AppLifecycleListener em LivesNotifier (Fase 2.3.12-C)"
```

---

## Task 4: Item D — PNGs do Inventário

**Files:**
- Modify: `lib/presentation/widgets/inventory_bar.dart`
- Modify: `lib/presentation/widgets/confirm_use_dialog.dart`
- Modify: `lib/app.dart`
- Modify: `test/presentation/inventory_bar_test.dart`
- Modify: `test/presentation/confirm_use_dialog_test.dart`

### Subtask D1 — inventory_bar.dart

- [ ] **Step 1: Escrever teste que verifica que pngPath é passado**

Abrir `test/presentation/inventory_bar_test.dart`. Adicionar após os testes existentes:

```dart
testWidgets('Bomba 2 button has pngPath set', (tester) async {
  await tester.pumpWidget(buildSubject());
  await tester.pump();
  final buttons = tester
      .widgetList<InventoryItemButton>(find.byType(InventoryItemButton))
      .toList();
  // bomb_2 é o primeiro botão
  expect(buttons[0].pngPath, 'assets/icons/inventory/bomb_2.png');
  expect(buttons[1].pngPath, 'assets/icons/inventory/bomb_3.png');
  expect(buttons[2].pngPath, 'assets/icons/inventory/undo_1.png');
  expect(buttons[3].pngPath, 'assets/icons/inventory/undo_3.png');
});
```

- [ ] **Step 2: Rodar o teste para confirmar que falha**

```bash
flutter test test/presentation/inventory_bar_test.dart --name "pngPath" -v
```

Esperado: FAIL (pngPath é null atualmente).

- [ ] **Step 3: Aplicar fix em inventory_bar.dart**

Abrir `lib/presentation/widgets/inventory_bar.dart`. Trocar os 4 `InventoryItemButton(...)`:

```dart
InventoryItemButton(
  label: 'Bomba 2',
  icon: Icons.bolt,
  pngPath: 'assets/icons/inventory/bomb_2.png',
  count: inventory.bomb2,
  onPressed: inventory.bomb2 > 0 ? useBomb2 : null,
),
InventoryItemButton(
  label: 'Bomba 3',
  icon: Icons.auto_fix_high,
  pngPath: 'assets/icons/inventory/bomb_3.png',
  count: inventory.bomb3,
  onPressed: inventory.bomb3 > 0 ? useBomb3 : null,
),
InventoryItemButton(
  label: 'Desfazer 1',
  icon: Icons.undo,
  pngPath: 'assets/icons/inventory/undo_1.png',
  count: inventory.undo1,
  onPressed: inventory.undo1 > 0 ? useUndo1 : null,
),
InventoryItemButton(
  label: 'Desfazer 3',
  icon: Icons.fast_rewind,
  pngPath: 'assets/icons/inventory/undo_3.png',
  count: inventory.undo3,
  onPressed: inventory.undo3 > 0 ? useUndo3 : null,
),
```

- [ ] **Step 4: Rodar testes de inventory_bar**

```bash
flutter test test/presentation/inventory_bar_test.dart -v
```

Esperado: todos PASS.

### Subtask D2 — confirm_use_dialog.dart

- [ ] **Step 5: Escrever teste que verifica ícone no dialog**

Abrir `test/presentation/confirm_use_dialog_test.dart`. Adicionar grupo:

```dart
group('ConfirmUseDialog com pngPath', () {
  testWidgets('exibe Image.asset no título quando pngPath fornecido', (tester) async {
    await tester.pumpWidget(
      MaterialApp(
        home: Builder(
          builder: (ctx) => ElevatedButton(
            onPressed: () => showConfirmUseDialog(
              context: ctx,
              itemName: 'Bomba 2',
              description: 'Remove tiles.',
              pngPath: 'assets/icons/inventory/bomb_2.png',
            ),
            child: const Text('Open'),
          ),
        ),
      ),
    );
    await tester.tap(find.text('Open'));
    await tester.pumpAndSettle();
    expect(find.byType(Image), findsOneWidget);
    expect(find.text('Usar Bomba 2?'), findsOneWidget);
  });

  testWidgets('sem Image quando pngPath é null', (tester) async {
    await tester.pumpWidget(
      MaterialApp(
        home: Builder(
          builder: (ctx) => ElevatedButton(
            onPressed: () => showConfirmUseDialog(
              context: ctx,
              itemName: 'Bomba 2',
              description: 'Remove tiles.',
            ),
            child: const Text('Open'),
          ),
        ),
      ),
    );
    await tester.tap(find.text('Open'));
    await tester.pumpAndSettle();
    expect(find.byType(Image), findsNothing);
    expect(find.text('Usar Bomba 2?'), findsOneWidget);
  });
});
```

- [ ] **Step 6: Rodar os testes para confirmar que falham**

```bash
flutter test test/presentation/confirm_use_dialog_test.dart --name "pngPath" -v
```

Esperado: FAIL (parâmetro `pngPath` não existe ainda).

- [ ] **Step 7: Aplicar fix em confirm_use_dialog.dart**

Substituir o arquivo completo por:

```dart
import 'package:flutter/material.dart';
import 'package:google_fonts/google_fonts.dart';

Future<bool> showConfirmUseDialog({
  required BuildContext context,
  required String itemName,
  required String description,
  String? pngPath,
}) async {
  final result = await showDialog<bool>(
    context: context,
    barrierDismissible: false,
    builder: (_) => _ConfirmUseDialog(
      itemName: itemName,
      description: description,
      pngPath: pngPath,
    ),
  );
  return result ?? false;
}

class _ConfirmUseDialog extends StatelessWidget {
  final String itemName;
  final String description;
  final String? pngPath;

  const _ConfirmUseDialog({
    required this.itemName,
    required this.description,
    this.pngPath,
  });

  @override
  Widget build(BuildContext context) {
    return AlertDialog(
      shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(20)),
      title: Row(
        children: [
          if (pngPath != null) ...[
            Image.asset(pngPath!, width: 40, height: 40),
            const SizedBox(width: 8),
          ],
          Flexible(
            child: Text(
              'Usar $itemName?',
              style: GoogleFonts.fredoka(fontWeight: FontWeight.bold, fontSize: 20),
            ),
          ),
        ],
      ),
      content: Text(
        description,
        style: GoogleFonts.nunito(fontSize: 15),
        textAlign: TextAlign.center,
      ),
      actionsAlignment: MainAxisAlignment.spaceEvenly,
      actions: [
        TextButton(
          onPressed: () => Navigator.of(context).pop(false),
          child: Text(
            'Cancelar',
            style: GoogleFonts.nunito(
              fontSize: 16,
              color: Colors.grey[700],
              fontWeight: FontWeight.w600,
            ),
          ),
        ),
        ElevatedButton(
          style: ElevatedButton.styleFrom(
            backgroundColor: const Color(0xFF4CAF50),
            foregroundColor: Colors.white,
            shape: RoundedRectangleBorder(
              borderRadius: BorderRadius.circular(12),
            ),
          ),
          onPressed: () => Navigator.of(context).pop(true),
          child: Text(
            'Confirmar',
            style: GoogleFonts.nunito(
              fontSize: 16,
              fontWeight: FontWeight.bold,
            ),
          ),
        ),
      ],
    );
  }
}
```

- [ ] **Step 8: Atualizar calls de showConfirmUseDialog em inventory_bar.dart para passar pngPath**

Abrir `lib/presentation/widgets/inventory_bar.dart`. Atualizar cada `showConfirmUseDialog(...)`:

```dart
// useBomb2:
final ok = await showConfirmUseDialog(
  context: context,
  itemName: 'Bomba 2',
  description: 'Remove os 2 tiles de menor valor do tabuleiro.',
  pngPath: 'assets/icons/inventory/bomb_2.png',
);

// useBomb3:
final ok = await showConfirmUseDialog(
  context: context,
  itemName: 'Bomba 3',
  description: 'Remove os 3 tiles de menor valor do tabuleiro.',
  pngPath: 'assets/icons/inventory/bomb_3.png',
);

// useUndo1:
final ok = await showConfirmUseDialog(
  context: context,
  itemName: 'Desfazer 1',
  description: 'Desfaz o último movimento.',
  pngPath: 'assets/icons/inventory/undo_1.png',
);

// useUndo3:
final ok = await showConfirmUseDialog(
  context: context,
  itemName: 'Desfazer 3',
  description: 'Desfaz os últimos 3 movimentos.',
  pngPath: 'assets/icons/inventory/undo_3.png',
);
```

- [ ] **Step 9: Rodar testes de confirm_use_dialog e inventory_bar**

```bash
flutter test test/presentation/confirm_use_dialog_test.dart test/presentation/inventory_bar_test.dart -v
```

Esperado: todos PASS.

### Subtask D3 — precacheImage em app.dart

- [ ] **Step 10: Adicionar precacheImage para os 4 PNGs em app.dart**

Abrir `lib/app.dart`. Após a linha `precacheImage(const AssetImage('assets/images/fundo.png'), context);` (linha ~41), adicionar:

```dart
precacheImage(const AssetImage('assets/icons/inventory/bomb_2.png'), context);
precacheImage(const AssetImage('assets/icons/inventory/bomb_3.png'), context);
precacheImage(const AssetImage('assets/icons/inventory/undo_1.png'), context);
precacheImage(const AssetImage('assets/icons/inventory/undo_3.png'), context);
```

- [ ] **Step 11: Rodar suite completa**

```bash
flutter test --reporter=compact
```

Esperado: 0 failures.

- [ ] **Step 12: Commit**

```bash
git add lib/presentation/widgets/inventory_bar.dart \
        lib/presentation/widgets/confirm_use_dialog.dart \
        lib/app.dart \
        test/presentation/inventory_bar_test.dart \
        test/presentation/confirm_use_dialog_test.dart
git commit -m "feat: PNGs finais do inventário em InventoryBar e ConfirmUseDialog (Fase 2.3.12-D)"
```

---

## Task 5: Validação Final e Versão

**Files:**
- Modify: `pubspec.yaml`
- Modify: `CHANGELOG.md`
- Modify: `CLAUDE.md`

- [ ] **Step 1: Rodar suite completa final**

```bash
flutter test --reporter=compact
```

Esperado: 0 failures.

- [ ] **Step 2: Bump de versão**

Abrir `pubspec.yaml`. Trocar `version: 0.8.0+17` por `version: 0.8.1+18`.

- [ ] **Step 3: Atualizar CHANGELOG.md**

Adicionar no topo (após `# Changelog`):

```markdown
## [0.8.1] — 2026-04-30

### Fixed
- `LivesIndicator` centralizado horizontalmente em `GameScreen` e `HomeScreen` (era esquerda/direita)
- `HostBanner` colado à borda esquerda do tabuleiro — gap eliminado com `Spacer()` (Fase 2.3.12-B)
- Timer de regen de vidas implementado em `LivesNotifier` — vidas agora incrementam durante sessão ativa (Fase 2.3.12-C)
- Recálculo offline de vidas ao retornar do background via `AppLifecycleListener` (Fase 2.3.12-C)

### Changed
- Ícones do inventário agora usam PNGs finais temáticos (Sucuri, Mico-leão, Capivara, Onça) (Fase 2.3.12-D)
- `ConfirmUseDialog` exibe ícone 40×40 do item no título (Fase 2.3.12-D)
```

- [ ] **Step 4: Atualizar CLAUDE.md**

Na tabela de fases, atualizar a linha de estado atual:

```markdown
Fase atual: **Fase 2.3.12 concluída (v0.8.1) — próximo: Fase 2.4 (áudio)**.
```

- [ ] **Step 5: Commit final**

```bash
git add pubspec.yaml CHANGELOG.md CLAUDE.md
git commit -m "chore: bump versão 0.8.0+17 → 0.8.1+18 (Fase 2.3.12)"
```
