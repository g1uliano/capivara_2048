# Fase 2.3.5 — Refinamento e Bugfixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Corrigir 5 bugs/UX issues identificados em uso real antes de avançar pra Fase 2.4.

**Architecture:** Execução em ordem A→E→B→D→C. Correção A (vidas) toca estado persistido — maior risco, vai primeiro. Correção E (mapa de cores) habilita B (flicker). D e C são cosméticos e independentes.

**Tech Stack:** Flutter 3.x, Dart, Riverpod (StateNotifier), Hive (persistência), flutter_test + golden files para snapshots visuais.

---

## File Map

| Arquivo | Ação | Correção |
|---|---|---|
| `lib/data/repositories/lives_repository.dart` | Modificar — adicionar `getMigrationFlag` / `setMigrationFlag` | A |
| `lib/domain/lives/lives_notifier.dart` | Modificar — migração v235 no `_init()` | A |
| `lib/presentation/screens/home_screen.dart` | Modificar — remover `consume()` de `_startNew` | A |
| `lib/presentation/screens/game/game_screen.dart` | Modificar — `ref.listen` para consumir vida no game over; LayoutBuilder para pause | A, C |
| `lib/data/models/animal.dart` | Modificar — adicionar campo `backgroundBaseColor` | E |
| `lib/data/animals_data.dart` | Modificar — 11 cores explícitas | E |
| `lib/presentation/widgets/game_background.dart` | Modificar — remover lerp, TweenAnimationBuilder | E, B |
| `lib/core/theme/text_styles.dart` | Criar — helper `outlinedWhiteTextStyle` | D |
| `lib/presentation/widgets/outlined_text.dart` | Criar — widget `OutlinedText` | D |
| `lib/presentation/widgets/status_panel.dart` | Modificar — aplicar `outlinedWhiteTextStyle` | D |
| `lib/presentation/widgets/host_banner.dart` | Modificar — aplicar `outlinedWhiteTextStyle` no nome do animal | D |
| `test/domain/lives_notifier_test.dart` | Modificar — 6 novos casos para Correção A | A |
| `test/presentation/game_background_test.dart` | Criar — snapshot de transição e cores | E, B |
| `test/presentation/outlined_text_test.dart` | Criar — snapshot OutlinedText | D |
| `test/presentation/game_screen_layout_test.dart` | Criar — posição do pause em múltiplas resoluções | C |

---

## Task 1: Migração de vidas — LivesRepository (Correção A)

**Files:**
- Modify: `lib/data/repositories/lives_repository.dart`
- Test: `test/domain/lives_notifier_test.dart`

- [ ] **Step 1: Escrever o teste de migração (vai falhar)**

Adicionar no final do arquivo `test/domain/lives_notifier_test.dart`:

```dart
group('migration v235', () {
  test('applyMigration sets lives to maxLives', () {
    final s = _state(lives: 2, maxLives: 5);
    final result = s.copyWith(lives: s.maxLives);
    expect(result.lives, 5);
  });
});
```

- [ ] **Step 2: Rodar o teste para confirmar que passa** (é uma teste de copyWith puro)

```bash
flutter test test/domain/lives_notifier_test.dart -v
```

Expected: PASS (não há lógica nova aqui — valida o comportamento existente do copyWith)

- [ ] **Step 3: Adicionar `getMigrationFlag` e `setMigrationFlag` em `LivesRepository`**

Em `lib/data/repositories/lives_repository.dart`, adicionar após o método `save`:

```dart
static const _prefsBox = 'prefs';

Future<bool> getMigrationFlag(String key) async {
  final box = await Hive.openBox<bool>(_prefsBox);
  return box.get(key, defaultValue: false)!;
}

Future<void> setMigrationFlag(String key) async {
  final box = await Hive.openBox<bool>(_prefsBox);
  await box.put(key, true);
}
```

- [ ] **Step 4: Rodar todos os testes**

```bash
flutter test
```

Expected: PASS (nenhuma lógica alterada ainda)

- [ ] **Step 5: Commit**

```bash
git add lib/data/repositories/lives_repository.dart test/domain/lives_notifier_test.dart
git commit -m "feat(lives): add migration flag helpers to LivesRepository"
```

---

## Task 2: Migração de vidas — LivesNotifier (Correção A)

**Files:**
- Modify: `lib/domain/lives/lives_notifier.dart`
- Test: `test/domain/lives_notifier_test.dart`

- [ ] **Step 1: Escrever testes para o novo comportamento de `consume` (vão falhar conceitualmente — são testes de intenção)**

Os testes existentes já cobrem `applyConsume`. Adicionar testes de comportamento no grupo `consume` em `test/domain/lives_notifier_test.dart`:

```dart
test('applyConsume from 1 → 0', () {
  final s = _state(lives: 1);
  expect(LivesNotifier.applyConsume(s).lives, 0);
});

test('applyConsume from 0 → stays 0 (idempotent)', () {
  final s = _state(lives: 0);
  expect(LivesNotifier.applyConsume(s).lives, 0);
});
```

- [ ] **Step 2: Rodar para confirmar que passam** (lógica já existe)

```bash
flutter test test/domain/lives_notifier_test.dart -v
```

Expected: PASS

- [ ] **Step 3: Aplicar migração no `_init()` de `LivesNotifier`**

Substituir o método `_init()` em `lib/domain/lives/lives_notifier.dart`:

```dart
Future<void> _init() async {
  var loaded = await _repo.load();
  loaded = calcRegen(state: loaded, now: DateTime.now());

  final hasReset = await _repo.getMigrationFlag('lives_reset_v235');
  if (!hasReset) {
    loaded = loaded.copyWith(lives: loaded.maxLives);
    await _repo.setMigrationFlag('lives_reset_v235');
  }

  state = loaded;
  await _repo.save(state);
  _ready.complete();
}
```

- [ ] **Step 4: Rodar todos os testes**

```bash
flutter test
```

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add lib/domain/lives/lives_notifier.dart test/domain/lives_notifier_test.dart
git commit -m "feat(lives): apply v235 migration — reset lives to max on first launch"
```

---

## Task 3: Mover `consume()` para o game over (Correção A)

**Files:**
- Modify: `lib/presentation/screens/home_screen.dart`
- Modify: `lib/presentation/screens/game/game_screen.dart`
- Test: `test/domain/lives_notifier_test.dart`

- [ ] **Step 1: Adicionar testes de comportamento (documenta a intenção)**

Adicionar grupo em `test/domain/lives_notifier_test.dart`:

```dart
group('consume timing contract', () {
  test('canPlay false when lives == 0', () {
    // LivesNotifier.canPlay usa state.lives > 0
    final s = _state(lives: 0);
    // canPlay é um getter no notifier, testamos via state diretamente
    expect(s.lives > 0, isFalse);
  });

  test('canPlay true when lives >= 1', () {
    final s = _state(lives: 1);
    expect(s.lives > 0, isTrue);
  });

  test('game over must trigger applyConsume (pure logic check)', () {
    // Valida que applyConsume reduz vida ao ser chamado no game over
    final before = _state(lives: 3);
    final after = LivesNotifier.applyConsume(before);
    expect(after.lives, 2);
  });
});
```

- [ ] **Step 2: Rodar para confirmar que passam**

```bash
flutter test test/domain/lives_notifier_test.dart -v
```

Expected: PASS

- [ ] **Step 3: Remover `consume()` de `HomeScreen._startNew()`**

Em `lib/presentation/screens/home_screen.dart`, no método `_startNew`, remover a linha:

```dart
ref.read(livesProvider.notifier).consume();  // REMOVER esta linha
```

O método fica assim:

```dart
void _startNew(BuildContext context, WidgetRef ref) {
  if (!ref.read(livesProvider.notifier).canPlay) {
    Navigator.push(
      context,
      MaterialPageRoute(builder: (_) => const NoLivesScreen(midGame: false)),
    );
    return;
  }
  ref.read(gameProvider.notifier).restart();
  Navigator.push(context, MaterialPageRoute(builder: (_) => const GameScreen()));
}
```

- [ ] **Step 4: Adicionar `ref.listen` no `GameScreen` para consumir vida no game over**

Em `lib/presentation/screens/game/game_screen.dart`, dentro do método `build`, ANTES do `return Scaffold(...)`, adicionar:

```dart
ref.listen<GameState>(gameProvider, (prev, next) {
  if (prev != null && !prev.isGameOver && next.isGameOver) {
    ref.read(livesProvider.notifier).consume();
  }
});
```

Também verificar que o import de `livesProvider` existe. Se não existir, adicionar:
```dart
import '../../controllers/lives_provider.dart'; // ajustar path se necessário
```

Procurar onde `livesProvider` é importado em `home_screen.dart` e usar o mesmo import em `game_screen.dart`.

- [ ] **Step 5: Rodar todos os testes**

```bash
flutter test
```

Expected: PASS

- [ ] **Step 6: Smoke test manual**

```bash
flutter run
```

Verificar:
1. Iniciar novo jogo → vidas inalteradas na tela do jogo
2. Sair pro menu (back) → vidas inalteradas no HomeScreen
3. Preencher o tabuleiro até game over → vidas decrement em 1

- [ ] **Step 7: Commit**

```bash
git add lib/presentation/screens/home_screen.dart lib/presentation/screens/game/game_screen.dart
git commit -m "fix(lives): consume life only on game over, not on new game start"
```

---

## Task 4: Campo `backgroundBaseColor` no modelo `Animal` (Correção E)

**Files:**
- Modify: `lib/data/models/animal.dart`
- Modify: `lib/data/animals_data.dart`

- [ ] **Step 1: Adicionar campo `backgroundBaseColor` ao modelo `Animal`**

Em `lib/data/models/animal.dart`, adicionar o campo após `texturePattern`:

```dart
class Animal {
  final int level;
  final int value;
  final String name;
  final Color borderColor;
  final Color backgroundBaseColor;
  final String assetPath;
  final String? hostSvgPath;
  final double? hostAspectRatio;
  final String? backgroundTexturePath;
  final TexturePattern texturePattern;

  const Animal({
    required this.level,
    required this.value,
    required this.name,
    required this.borderColor,
    required this.backgroundBaseColor,
    required this.assetPath,
    required this.texturePattern,
    this.hostSvgPath,
    this.hostAspectRatio,
    this.backgroundTexturePath,
  });
}
```

- [ ] **Step 2: Tentar compilar para ver todos os erros de construtor (esperado — `animals_data.dart` não tem o campo)**

```bash
flutter analyze
```

Expected: erros em `animals_data.dart` (campo obrigatório faltando)

- [ ] **Step 3: Atualizar `animals_data.dart` com as 11 cores explícitas**

Substituir todo o conteúdo de `lib/data/animals_data.dart`:

```dart
import 'package:flutter/material.dart';
import 'models/animal.dart';

const List<Animal> animals = [
  Animal(
    level: 1, value: 2, name: 'Tanajura',
    borderColor: Color(0xFFC0392B),
    backgroundBaseColor: Color(0xFFF5C2BA),
    assetPath: 'assets/images/animals/tile/tanajura.png',
    texturePattern: TexturePattern.dots,
  ),
  Animal(
    level: 2, value: 4, name: 'Lobo-guará',
    borderColor: Color(0xFFE67E22),
    backgroundBaseColor: Color(0xFFFAD3B2),
    assetPath: 'assets/images/animals/tile/lobo_guara.png',
    texturePattern: TexturePattern.dots,
  ),
  Animal(
    level: 3, value: 8, name: 'Sapo-cururu',
    borderColor: Color(0xFF8D6E63),
    backgroundBaseColor: Color(0xFFD7C4BC),
    assetPath: 'assets/images/animals/tile/sapo_cururu.png',
    texturePattern: TexturePattern.diagonal,
  ),
  Animal(
    level: 4, value: 16, name: 'Tucano',
    borderColor: Color(0xFFFFB300),
    backgroundBaseColor: Color(0xFFFFE9A8),
    assetPath: 'assets/images/animals/tile/tucano.png',
    texturePattern: TexturePattern.diagonal,
  ),
  Animal(
    level: 5, value: 32, name: 'Arara-azul',
    borderColor: Color(0xFF1E88E5),
    backgroundBaseColor: Color(0xFFB5D7F4),
    assetPath: 'assets/images/animals/tile/arara_azul.png',
    texturePattern: TexturePattern.grid,
  ),
  Animal(
    level: 6, value: 64, name: 'Preguiça',
    borderColor: Color(0xFFBCAAA4),
    backgroundBaseColor: Color(0xFFE8E0DC),
    assetPath: 'assets/images/animals/tile/preguica.png',
    texturePattern: TexturePattern.grid,
  ),
  Animal(
    level: 7, value: 128, name: 'Mico-leão-dourado',
    borderColor: Color(0xFFFF8F00),
    backgroundBaseColor: Color(0xFFFFD7A1),
    assetPath: 'assets/images/animals/tile/mico_leao_dourado.png',
    texturePattern: TexturePattern.waves,
  ),
  Animal(
    level: 8, value: 256, name: 'Boto-cor-de-rosa',
    borderColor: Color(0xFFF48FB1),
    backgroundBaseColor: Color(0xFFFBD0DD),
    assetPath: 'assets/images/animals/tile/boto_cor_de_rosa.png',
    texturePattern: TexturePattern.waves,
  ),
  Animal(
    level: 9, value: 512, name: 'Onça-pintada',
    borderColor: Color(0xFFFBC02D),
    backgroundBaseColor: Color(0xFFFFEFB0),
    assetPath: 'assets/images/animals/tile/onca_pintada.png',
    texturePattern: TexturePattern.blobs,
  ),
  Animal(
    level: 10, value: 1024, name: 'Sucuri',
    borderColor: Color(0xFF2E7D32),
    backgroundBaseColor: Color(0xFFBFD9C0),
    assetPath: 'assets/images/animals/tile/sucuri.png',
    texturePattern: TexturePattern.scales,
  ),
  Animal(
    level: 11, value: 2048, name: 'Capivara Lendária',
    borderColor: Color(0xFFFFD54F),
    backgroundBaseColor: Color(0xFFFFEFB8),
    assetPath: 'assets/images/animals/tile/capivara_lendaria.png',
    texturePattern: TexturePattern.radial,
  ),
];

Animal animalForLevel(int level) =>
    animals.firstWhere((a) => a.level == level);
```

- [ ] **Step 4: Compilar e rodar todos os testes**

```bash
flutter analyze && flutter test
```

Expected: zero erros, todos os testes passam

- [ ] **Step 5: Commit**

```bash
git add lib/data/models/animal.dart lib/data/animals_data.dart
git commit -m "feat(animals): add backgroundBaseColor field — explicit per-animal bg colors"
```

---

## Task 5: Remover `Color.lerp` do `GameBackground` (Correção E)

**Files:**
- Modify: `lib/presentation/widgets/game_background.dart`
- Create: `test/presentation/game_background_test.dart`

- [ ] **Step 1: Criar teste que verifica a cor exata do Boto**

Criar `test/presentation/game_background_test.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:capivara_2048/data/animals_data.dart';
import 'package:capivara_2048/presentation/widgets/game_background.dart';

void main() {
  group('GameBackground colors', () {
    testWidgets('Boto background is pink, not beige', (tester) async {
      final boto = animalForLevel(8);
      // backgroundBaseColor deve ser rosa #FBD0DD
      expect(boto.backgroundBaseColor, const Color(0xFFFBD0DD));
    });

    testWidgets('Tucano background is yellow pastel', (tester) async {
      final tucano = animalForLevel(4);
      expect(tucano.backgroundBaseColor, const Color(0xFFFFE9A8));
    });

    testWidgets('Capivara background differs from Tucano and Onca', (tester) async {
      final capivara = animalForLevel(11);
      final tucano = animalForLevel(4);
      final onca = animalForLevel(9);
      expect(capivara.backgroundBaseColor, isNot(equals(tucano.backgroundBaseColor)));
      expect(capivara.backgroundBaseColor, isNot(equals(onca.backgroundBaseColor)));
    });
  });
}
```

- [ ] **Step 2: Rodar o teste para confirmar que passa** (dados já estão em `animals_data.dart`)

```bash
flutter test test/presentation/game_background_test.dart -v
```

Expected: PASS

- [ ] **Step 3: Remover `Color.lerp` de `_Background` em `game_background.dart`**

Em `lib/presentation/widgets/game_background.dart`, substituir o getter `_bgColor` e seu uso em `_Background.build`:

Remover:
```dart
Color get _bgColor =>
    Color.lerp(animal.borderColor, AppColors.mint, 0.65) ?? AppColors.mint;
```

E na `build` de `_Background`, substituir `ColoredBox(color: _bgColor)` por:
```dart
ColoredBox(color: animal.backgroundBaseColor),
```

O arquivo `_Background.build` completo fica:

```dart
@override
Widget build(BuildContext context) {
  return Stack(
    fit: StackFit.expand,
    children: [
      ColoredBox(color: animal.backgroundBaseColor),
      Opacity(
        opacity: 0.12,
        child: RepaintBoundary(
          child: animal.backgroundTexturePath != null
              ? Image.asset(
                  animal.backgroundTexturePath!,
                  repeat: ImageRepeat.repeat,
                  fit: BoxFit.none,
                  errorBuilder: (_, __, ___) => CustomPaint(
                    painter: TexturePainter(
                      pattern: animal.texturePattern,
                      color: animal.borderColor,
                    ),
                    size: Size.infinite,
                  ),
                )
              : CustomPaint(
                  painter: TexturePainter(
                    pattern: animal.texturePattern,
                    color: animal.borderColor,
                  ),
                  size: Size.infinite,
                ),
        ),
      ),
      child,
    ],
  );
}
```

- [ ] **Step 4: Rodar todos os testes**

```bash
flutter test
```

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add lib/presentation/widgets/game_background.dart test/presentation/game_background_test.dart
git commit -m "fix(background): use explicit backgroundBaseColor, remove Color.lerp derivation"
```

---

## Task 6: Transição de fundo sem flicker — TweenAnimationBuilder (Correção B)

**Files:**
- Modify: `lib/presentation/widgets/game_background.dart`

- [ ] **Step 1: Refatorar `GameBackground` para usar `TweenAnimationBuilder<Color?>`**

Substituir todo `lib/presentation/widgets/game_background.dart` (mantendo imports existentes):

```dart
import '../../data/models/animal.dart';
import 'texture_painter.dart';

class GameBackground extends StatefulWidget {
  final Animal? animal;
  final Widget child;

  const GameBackground({super.key, required this.animal, required this.child});

  @override
  State<GameBackground> createState() => _GameBackgroundState();
}

class _GameBackgroundState extends State<GameBackground> {
  Color _previousColor = AppColors.primary;

  @override
  Widget build(BuildContext context) {
    final targetColor = widget.animal?.backgroundBaseColor ?? AppColors.primary;

    return TweenAnimationBuilder<Color?>(
      tween: ColorTween(begin: _previousColor, end: targetColor),
      duration: const Duration(milliseconds: 700),
      curve: Curves.easeInOut,
      onEnd: () => _previousColor = targetColor,
      builder: (context, color, _) {
        return Stack(
          fit: StackFit.expand,
          children: [
            ColoredBox(color: color ?? AppColors.primary),
            if (widget.animal != null)
              AnimatedSwitcher(
                duration: const Duration(milliseconds: 400),
                child: _TextureLayer(
                  key: ValueKey(widget.animal!.level),
                  animal: widget.animal!,
                ),
              ),
            widget.child,
          ],
        );
      },
    );
  }
}

class _TextureLayer extends StatelessWidget {
  final Animal animal;
  const _TextureLayer({super.key, required this.animal});

  @override
  Widget build(BuildContext context) {
    return Opacity(
      opacity: 0.12,
      child: RepaintBoundary(
        child: animal.backgroundTexturePath != null
            ? Image.asset(
                animal.backgroundTexturePath!,
                repeat: ImageRepeat.repeat,
                fit: BoxFit.none,
                errorBuilder: (_, __, ___) => CustomPaint(
                  painter: TexturePainter(
                    pattern: animal.texturePattern,
                    color: animal.borderColor,
                  ),
                  size: Size.infinite,
                ),
              )
            : CustomPaint(
                painter: TexturePainter(
                  pattern: animal.texturePattern,
                  color: animal.borderColor,
                ),
                size: Size.infinite,
              ),
      ),
    );
  }
}
```

Nota: `_Background` e seu getter `_bgColor` são removidos. A textura agora é `_TextureLayer`.

- [ ] **Step 2: Rodar todos os testes**

```bash
flutter test
```

Expected: PASS

- [ ] **Step 3: Smoke test manual — verificar ausência de flicker**

```bash
flutter run
```

Jogar até trocar de anfitrião (ex: Tanajura → Lobo-guará). Verificar que a transição de cor é suave, sem piscar.

- [ ] **Step 4: Commit**

```bash
git add lib/presentation/widgets/game_background.dart
git commit -m "fix(background): smooth color transition via TweenAnimationBuilder, no flicker"
```

---

## Task 7: Helper `outlinedWhiteTextStyle` (Correção D)

**Files:**
- Create: `lib/core/theme/text_styles.dart`
- Create: `test/presentation/outlined_text_test.dart`

- [ ] **Step 1: Criar teste para o helper (vai falhar — arquivo não existe)**

Criar `test/presentation/outlined_text_test.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:capivara_2048/core/theme/text_styles.dart';

void main() {
  group('outlinedWhiteTextStyle', () {
    test('returns TextStyle with white color', () {
      const base = TextStyle(fontSize: 16);
      final result = outlinedWhiteTextStyle(base);
      expect(result.color, Colors.white);
    });

    test('applies 4 shadows for outline effect', () {
      const base = TextStyle(fontSize: 16);
      final result = outlinedWhiteTextStyle(base);
      expect(result.shadows, isNotNull);
      expect(result.shadows!.length, 4);
    });

    test('all shadows are black', () {
      const base = TextStyle(fontSize: 16);
      final result = outlinedWhiteTextStyle(base);
      for (final shadow in result.shadows!) {
        expect(shadow.color, Colors.black);
      }
    });
  });
}
```

- [ ] **Step 2: Rodar para confirmar que falha**

```bash
flutter test test/presentation/outlined_text_test.dart -v
```

Expected: FAIL — `outlinedWhiteTextStyle` not found

- [ ] **Step 3: Criar `lib/core/theme/text_styles.dart`**

```dart
import 'package:flutter/material.dart';

TextStyle outlinedWhiteTextStyle(TextStyle base) {
  const offset = 1.5;
  return base.copyWith(
    color: Colors.white,
    shadows: const [
      Shadow(color: Colors.black, offset: Offset(offset, offset)),
      Shadow(color: Colors.black, offset: Offset(-offset, offset)),
      Shadow(color: Colors.black, offset: Offset(offset, -offset)),
      Shadow(color: Colors.black, offset: Offset(-offset, -offset)),
    ],
  );
}
```

- [ ] **Step 4: Rodar o teste para confirmar que passa**

```bash
flutter test test/presentation/outlined_text_test.dart -v
```

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add lib/core/theme/text_styles.dart test/presentation/outlined_text_test.dart
git commit -m "feat(theme): add outlinedWhiteTextStyle helper with 4-shadow outline"
```

---

## Task 8: Widget `OutlinedText` (Correção D)

**Files:**
- Create: `lib/presentation/widgets/outlined_text.dart`
- Modify: `test/presentation/outlined_text_test.dart`

- [ ] **Step 1: Adicionar teste de widget para `OutlinedText` (vai falhar)**

Adicionar no grupo existente em `test/presentation/outlined_text_test.dart`:

```dart
import 'package:capivara_2048/presentation/widgets/outlined_text.dart';

// ... dentro de main():

group('OutlinedText widget', () {
  testWidgets('renders text content', (tester) async {
    await tester.pumpWidget(
      const MaterialApp(
        home: Scaffold(
          body: OutlinedText(
            text: 'Tucano',
            style: TextStyle(fontSize: 16),
          ),
        ),
      ),
    );
    expect(find.text('Tucano'), findsWidgets); // dois Text no Stack
  });
});
```

- [ ] **Step 2: Rodar para confirmar que falha**

```bash
flutter test test/presentation/outlined_text_test.dart -v
```

Expected: FAIL — `OutlinedText` not found

- [ ] **Step 3: Criar `lib/presentation/widgets/outlined_text.dart`**

```dart
import 'package:flutter/material.dart';

class OutlinedText extends StatelessWidget {
  final String text;
  final TextStyle? style;
  final TextAlign? textAlign;
  final int? maxLines;
  final TextOverflow? overflow;

  const OutlinedText({
    super.key,
    required this.text,
    this.style,
    this.textAlign,
    this.maxLines,
    this.overflow,
  });

  @override
  Widget build(BuildContext context) {
    const strokeWidth = 1.5;
    final baseStyle = style ?? const TextStyle();

    return Stack(
      children: [
        Text(
          text,
          style: baseStyle.copyWith(
            foreground: Paint()
              ..style = PaintingStyle.stroke
              ..strokeWidth = strokeWidth
              ..color = Colors.black,
          ),
          textAlign: textAlign,
          maxLines: maxLines,
          overflow: overflow,
        ),
        Text(
          text,
          style: baseStyle.copyWith(color: Colors.white),
          textAlign: textAlign,
          maxLines: maxLines,
          overflow: overflow,
        ),
      ],
    );
  }
}
```

- [ ] **Step 4: Rodar todos os testes**

```bash
flutter test
```

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add lib/presentation/widgets/outlined_text.dart test/presentation/outlined_text_test.dart
git commit -m "feat(widgets): add OutlinedText widget — white fill + black stroke"
```

---

## Task 9: Aplicar `outlinedWhiteTextStyle` em `StatusPanel` e `HostBanner` (Correção D)

**Files:**
- Modify: `lib/presentation/widgets/status_panel.dart`
- Modify: `lib/presentation/widgets/host_banner.dart`

- [ ] **Step 1: Atualizar `StatusPanel` para usar `outlinedWhiteTextStyle`**

Em `lib/presentation/widgets/status_panel.dart`, adicionar import:
```dart
import '../../core/theme/text_styles.dart';
```

Substituir os três `Text` do `build` para usar `outlinedWhiteTextStyle`:

```dart
Text(
  _formatTime(elapsedMs),
  style: outlinedWhiteTextStyle(
    GoogleFonts.fredoka(fontSize: 16, fontWeight: FontWeight.bold),
  ),
),
const SizedBox(height: 2),
Text(
  '$score',
  style: outlinedWhiteTextStyle(
    GoogleFonts.fredoka(fontSize: 20, fontWeight: FontWeight.bold),
  ),
),
Text(
  'Recorde: $highScore',
  style: outlinedWhiteTextStyle(
    GoogleFonts.nunito(fontSize: 11),
  ),
),
```

- [ ] **Step 2: Atualizar `HostBanner` — nome do animal em `_AnimalHost`**

Em `lib/presentation/widgets/host_banner.dart`, adicionar import:
```dart
import '../../core/theme/text_styles.dart';
```

Em `_AnimalHost.build`, substituir o `Text` do nome do animal:

```dart
Text(
  animal.name,
  style: outlinedWhiteTextStyle(
    GoogleFonts.nunito(fontSize: 13, fontWeight: FontWeight.w600),
  ),
  textAlign: TextAlign.center,
  overflow: TextOverflow.ellipsis,
),
```

- [ ] **Step 3: Rodar todos os testes**

```bash
flutter test
```

Expected: PASS

- [ ] **Step 4: Smoke test manual — legibilidade sobre fundo amarelo**

```bash
flutter run
```

Jogar até chegar no Tucano (nível 4, fundo amarelo). Verificar que cronômetro, pontuação, recorde e nome do animal têm contorno preto e são legíveis.

- [ ] **Step 5: Commit**

```bash
git add lib/presentation/widgets/status_panel.dart lib/presentation/widgets/host_banner.dart
git commit -m "fix(legibility): apply outlinedWhiteTextStyle to StatusPanel and HostBanner"
```

---

## Task 10: Botão de pause sem sobreposição via LayoutBuilder (Correção C)

**Files:**
- Modify: `lib/presentation/screens/game/game_screen.dart`
- Create: `test/presentation/game_screen_layout_test.dart`

- [ ] **Step 1: Criar testes de layout para o botão de pause**

Criar `test/presentation/game_screen_layout_test.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Pause button layout', () {
    testWidgets('pause button has minimum touch area 48x48', (tester) async {
      // Smoke: o botão de pause deve ter pelo menos 48x48 de área de toque
      // Testamos o tamanho do IconButton isolado
      await tester.pumpWidget(
        MaterialApp(
          home: Scaffold(
            body: IconButton(
              icon: const Icon(Icons.pause_rounded, color: Colors.white),
              iconSize: 32,
              onPressed: () {},
            ),
          ),
        ),
      );
      final button = tester.getSize(find.byType(IconButton));
      expect(button.width, greaterThanOrEqualTo(48));
      expect(button.height, greaterThanOrEqualTo(48));
    });
  });
}
```

- [ ] **Step 2: Rodar para confirmar que passa**

```bash
flutter test test/presentation/game_screen_layout_test.dart -v
```

Expected: PASS (IconButton padrão do Flutter já tem 48x48 mínimo)

- [ ] **Step 3: Converter `GameScreen` para `ConsumerStatefulWidget` e usar `GlobalKey` para posicionamento dinâmico**

Em `lib/presentation/screens/game/game_screen.dart`, converter a classe:

```dart
class GameScreen extends ConsumerStatefulWidget {
  const GameScreen({super.key});

  @override
  ConsumerState<GameScreen> createState() => _GameScreenState();
}

class _GameScreenState extends ConsumerState<GameScreen> {
  final GlobalKey _headerKey = GlobalKey();
  double _pauseTop = 80;

  @override
  void initState() {
    super.initState();
    WidgetsBinding.instance.addPostFrameCallback((_) => _updatePausePosition());
  }

  void _updatePausePosition() {
    final renderBox =
        _headerKey.currentContext?.findRenderObject() as RenderBox?;
    if (renderBox == null) return;
    final headerBottom = renderBox.localToGlobal(Offset.zero).dy +
        renderBox.size.height;
    if (mounted) {
      setState(() => _pauseTop = headerBottom + 12);
    }
  }

  @override
  Widget build(BuildContext context) {
    final state = ref.watch(gameProvider);
    final isGameOver = state.isGameOver;
    final hasWon = state.hasWon;
    final notifier = ref.read(gameProvider.notifier);
    final hostAnimal =
        state.maxLevel > 0 ? animalForLevel(state.maxLevel) : null;

    ref.listen<GameState>(gameProvider, (prev, next) {
      if (prev != null && !prev.isGameOver && next.isGameOver) {
        ref.read(livesProvider.notifier).consume();
      }
    });

    return Scaffold(
      body: GameBackground(
        animal: hostAnimal,
        child: SafeArea(
          child: Stack(
            children: [
              Column(
                children: [
                  Padding(
                    key: _headerKey,
                    padding: const EdgeInsets.symmetric(
                        horizontal: 16, vertical: 8),
                    child: Row(
                      crossAxisAlignment: CrossAxisAlignment.start,
                      children: [
                        const HostBanner(),
                        const Spacer(),
                        const StatusPanel(),
                      ],
                    ),
                  ),
                  const Spacer(),
                  GestureDetector(
                    behavior: HitTestBehavior.opaque,
                    onPanEnd: (details) {
                      if (state.isPaused || isGameOver) return;
                      final v = details.velocity.pixelsPerSecond;
                      const threshold = 100.0;
                      if (v.dx.abs() > v.dy.abs()) {
                        if (v.dx > threshold) {
                          notifier.onSwipe(Direction.right);
                        } else if (v.dx < -threshold) {
                          notifier.onSwipe(Direction.left);
                        }
                      } else {
                        if (v.dy > threshold) {
                          notifier.onSwipe(Direction.down);
                        } else if (v.dy < -threshold) {
                          notifier.onSwipe(Direction.up);
                        }
                      }
                    },
                    child: Stack(
                      children: [
                        const RepaintBoundary(child: BoardWidget()),
                        if (state.isPaused) const PauseOverlay(),
                      ],
                    ),
                  ),
                  const Spacer(),
                  const Padding(
                    padding:
                        EdgeInsets.symmetric(horizontal: 16, vertical: 8),
                    child: LivesIndicator(),
                  ),
                  if (isGameOver) _buildOverlay('Game Over!', notifier),
                  if (hasWon && !isGameOver)
                    _buildOverlay('Capivara Lendária! 🎉', notifier),
                ],
              ),
              if (!isGameOver && !hasWon)
                Positioned(
                  top: _pauseTop,
                  right: 8,
                  child: IconButton(
                    icon: Icon(
                      state.isPaused
                          ? Icons.play_arrow_rounded
                          : Icons.pause_rounded,
                      color: Colors.white,
                    ),
                    iconSize: 32,
                    onPressed:
                        state.isPaused ? notifier.resume : notifier.pause,
                  ),
                ),
            ],
          ),
        ),
      ),
    );
  }

  Widget _buildOverlay(String message, GameNotifier notifier) {
    return Padding(
      padding: const EdgeInsets.all(16),
      child: Column(
        children: [
          Text(message,
              style: const TextStyle(
                  fontSize: 28,
                  fontWeight: FontWeight.bold,
                  color: Colors.white)),
          const SizedBox(height: 8),
          ElevatedButton(
            onPressed: () => notifier.restart(),
            child: const Text('Jogar de novo'),
          ),
        ],
      ),
    );
  }
}
```

**Nota de integração:** Task 3 adicionou `ref.listen` no `GameScreen` (que ainda era `ConsumerWidget`). Task 10 converte `GameScreen` para `ConsumerStatefulWidget` e inclui o mesmo `ref.listen` no `build`. Ao aplicar Task 10, remover a adição de Task 3 — o código de Task 10 é a versão canônica e inclui tudo.

- [ ] **Step 4: Rodar todos os testes**

```bash
flutter test
```

Expected: PASS

- [ ] **Step 5: Smoke test manual — verificar em diferentes tamanhos de tela**

```bash
flutter run
```

Verificar:
1. No emulador padrão: pause não toca o `StatusPanel`
2. Cronômetro em formato HH:MM:SS (após 1h de jogo ou manipular o estado): pause ainda abaixo do panel

- [ ] **Step 6: Commit**

```bash
git add lib/presentation/screens/game/game_screen.dart test/presentation/game_screen_layout_test.dart
git commit -m "fix(layout): position pause button dynamically below StatusPanel via GlobalKey"
```

---

## Task 11: Atualizar docs e finalizar fase (Correção — todos)

**Files:**
- Modify: `CHANGELOG.md`
- Modify: `CAPIVARA_2048_DESIGN.md`

- [ ] **Step 1: Atualizar `CHANGELOG.md`**

Adicionar entrada no topo do arquivo (após o cabeçalho, antes da versão anterior):

```markdown
## [0.3.5] — 2026-04-26

### Fixed
- Vidas agora só são consumidas no game over (não ao iniciar nova partida)
- Transição de cor de fundo entre animais sem flicker (TweenAnimationBuilder)
- Botão de pause reposicionado dinamicamente abaixo do StatusPanel (GlobalKey)
- Texto branco com contorno preto em StatusPanel e HostBanner para legibilidade

### Changed
- Cores de fundo dos animais agora são explícitas por animal (backgroundBaseColor)
- Boto-cor-de-rosa exibe fundo rosa correto (#FBD0DD) em vez de bege derivado

### Migration
- Primeira abertura pós-update reseta vidas para 5 (goodwill adjustment)
```

- [ ] **Step 2: Atualizar `CAPIVARA_2048_DESIGN.md`**

Localizar a seção `### 🚧 Fase 2.3.5` e alterar `🚧` para `✅`. Atualizar o status no topo do arquivo:

Linha atual:
```
> **Status atual:** Fase 2.3 concluída ✅ ...
> **Próximo:** **Fase 2.3.5** ...
```

Substituir por:
```
> **Status atual:** Fase 2.3.5 concluída ✅ — 5 bugfixes: vidas no game over, cores explícitas por animal, transição sem flicker, OutlinedText, pause posicionado dinamicamente.
>
> **Próximo:** **Fase 2.4** — Inventário: Bomba + Desfazer.
```

- [ ] **Step 3: Rodar suite completa de testes**

```bash
flutter test
```

Expected: todos os testes passam, zero falhas

- [ ] **Step 4: Commit e tag**

```bash
git add CHANGELOG.md CAPIVARA_2048_DESIGN.md
git commit -m "chore: release v0.3.5 — Fase 2.3.5 bugs corrigidos, docs atualizados"
```

---

## Definição de "feito"

- [ ] `flutter test` — zero falhas
- [ ] Vidas não decrementam ao sair ou reiniciar partida (smoke manual)
- [ ] Boto exibe fundo rosa no emulador
- [ ] Texto branco legível sobre fundos amarelos/dourados
- [ ] Botão pause não toca StatusPanel em nenhuma resolução testada
- [ ] Transição de cor suave, sem piscar
- [ ] `CHANGELOG.md` com v0.3.5
- [ ] `CAPIVARA_2048_DESIGN.md` com Fase 2.3.5 marcada como ✅ e próximo apontando para Fase 2.4
