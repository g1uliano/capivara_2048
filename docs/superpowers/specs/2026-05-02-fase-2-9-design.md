# Spec — Fase 2.9: Splash Screen, Game Over redesenhado, Inventário maior e Orientação bloqueada

**Data:** 2026-05-02
**Versão alvo:** v0.9.5
**Status:** aprovado para implementação

---

## Visão geral

Quatro sub-entregas independentes de UI/UX. Sem impacto em backend, lógica de pontuação ou áudio.
Ordem de implementação: **D → C → A → B** (menor para maior risco).

---

## Sub-entrega D — Orientação portrait-only

### Objetivo
Bloquear rotação de tela em todos os dispositivos Android e iOS.

### Arquivos modificados
| Arquivo | Mudança |
|---------|---------|
| `lib/main.dart` | Adicionar `WidgetsFlutterBinding.ensureInitialized()` antes do `runApp` (se ausente); adicionar `await SystemChrome.setPreferredOrientations([DeviceOrientation.portraitUp, DeviceOrientation.portraitDown])` antes do `runApp` |
| `android/app/src/main/AndroidManifest.xml` | Adicionar `android:screenOrientation="portrait"` na `<activity>` principal |
| `ios/Runner/Info.plist` | `UISupportedInterfaceOrientations`: manter apenas `UIInterfaceOrientationPortrait` e `UIInterfaceOrientationPortraitUpsideDown`; `UISupportedInterfaceOrientations~ipad` não existe / não é adicionado (sem versão iPad) |

### Critérios de aceite
- Rotacionar dispositivo físico ou emulador em qualquer tela (Home, Jogo, Loja, Coleção, Configurações) — portrait mantido
- Nenhuma tela apresenta layout em landscape mesmo com rotação forçada

### Testes
- Verificação manual em dispositivo físico Android e iOS simulator
- Sem teste unitário útil para esta entrega

---

## Sub-entrega C — Inventário maior com espaçamento reduzido

### Objetivo
Ícones 56dp→72dp; espaçamento tabuleiro→`InventoryBar` 12dp→4dp. Sem overflow em 360×640dp.

### Arquivos modificados
| Arquivo | Mudança |
|---------|---------|
| `lib/core/constants/game_constants.dart` | Adicionar `static const double inventoryIconSize = 72.0` e `static const double boardToInventorySpacing = 4.0` |
| `lib/presentation/widgets/inventory_item_button.dart` | `SizedBox(width: 56, height: 56)` → `SizedBox(width: 72, height: 72)`; referenciar `GameConstants.inventoryIconSize` |
| `lib/presentation/screens/game/game_screen.dart` | Localizar `SizedBox` ou padding entre tabuleiro e `InventoryBar`; substituir valor hardcoded por `GameConstants.boardToInventorySpacing` (4dp) |

### Cálculo de overflow (360×640dp)
```
Status bar:        24dp
AppBar:            56dp
Header (scores):   60dp
Tabuleiro 4×4:    352dp  (4×72dp tiles + 5×8dp gaps + 2×12dp boardPadding)
Spacing:            4dp
InventoryBar:      ~80dp  (ícones 72dp + padding vertical 8dp)
─────────────────────────
Total:            576dp  (folga de ~40dp — sem overflow)
```

### Critérios de aceite
- 4 ícones de 72dp visíveis e tocáveis em 360×640dp
- 4 ícones de 72dp visíveis e tocáveis em 360×568dp (iPhone SE 1ª gen)
- Espaçamento de 4dp entre borda inferior do tabuleiro e topo da `InventoryBar` confirmado no widget tree
- Badge de contagem posicionado corretamente sobre ícone 72dp

### Testes
- Verificação visual em emulador 360×640dp e 360×568dp
- Inspecionar widget tree com Flutter DevTools para confirmar espaçamento

---

## Sub-entrega A — Splash Screen

### Objetivo
Splash nativa antes do Flutter (`flutter_native_splash`) + widget Flutter `SplashScreen` com `precacheImage` e inicialização em background. Duração mínima 1,5s.

### Asset
`assets/images/splash/splash_logo.png` (já existe — reutilizado sem alteração).

### Arquivos modificados/criados
| Arquivo | Mudança |
|---------|---------|
| `pubspec.yaml` | Adicionar `flutter_native_splash: ^2.x` em `dev_dependencies`; adicionar seção de configuração (ver abaixo) |
| `lib/presentation/screens/splash_screen.dart` | **Novo.** `SplashScreen` StatefulWidget (ver spec abaixo) |
| `lib/app.dart` | Trocar rota inicial de `HomeScreen` para `SplashScreen`; remover `precacheImage` existente (migrado para `SplashScreen`) |

### Configuração `flutter_native_splash` em `pubspec.yaml`
```yaml
flutter_native_splash:
  color: "#FFFFFF"          # fundo branco (ajustar para cor do tema se necessário)
  image: assets/images/splash/splash_logo.png
  android: true
  ios: true
  web: false
```
Rodar após adicionar: `dart run flutter_native_splash:create`

### Spec do widget `SplashScreen`
```
initState:
  - Inicia timer de 1500ms
  - Dispara _init() em paralelo

_init():
  - precacheImage de todos os assets de animais (tile + host)
  - precacheImage de fundo (assets/images/fundo.png)
  - Inicializa Hive se necessário
  - Marca _initDone = true
  - Chama _tryNavigate()

timer callback:
  - Marca _timerDone = true
  - Chama _tryNavigate()

_tryNavigate():
  - Se _initDone && _timerDone: Navigator.pushReplacement(HomeScreen)

Animação de entrada (flutter_animate):
  - Logo: fadeIn(600ms) + scale(begin: 0.95, end: 1.0, 600ms)

Animação de saída (antes do pushReplacement):
  - fadeOut(400ms)
```

### Critérios de aceite
- Splash nativa visível antes do Flutter estar pronto
- `SplashScreen` Flutter exibida por no mínimo 1,5s mesmo em dispositivo rápido
- Navegação automática para `HomeScreen` ao término da inicialização
- Nenhum `precacheImage` duplicado entre `SplashScreen` e `app.dart`

### Testes
- Verificação manual: fechar e reabrir o app — splash aparece, desaparece, `HomeScreen` carregada
- Verificar em dispositivo lento (emulador sem aceleração): splash não some antes de 1,5s

---

## Sub-entrega B — `GameOverItemScreen`

### Objetivo
Overlay dedicado ao fluxo de salvamento quando o tabuleiro trava com itens disponíveis. Vida consumida apenas após confirmação de desistência.

### Arquitetura
Overlay no `Stack` da `GameScreen` — consistente com `BombSelectionOverlay`, `PauseOverlay` e `GameOverModal` existentes.

### Fluxo completo
```
1. GameEngine detecta: sem movimentos válidos → isGameOver = true
2. GameNotifier._onGameOver():
   a. _getAvailableSavingItems(inv) → lista de ItemType com count > 0
   b. Se lista vazia → fluxo antigo (GameOverModal / consumir vida)
   c. Se lista não vazia → isAwaitingGameOverResolution = true
3. GameScreen observa isAwaitingGameOverResolution == true
   → renderiza Positioned.fill(GameOverItemOverlay) no topo do Stack
   → AbsorbPointer envolve conteúdo abaixo (tabuleiro, header, inventário)
4. GameOverItemOverlay:
   - Backdrop: Colors.black.withOpacity(0.75)
   - Card central: fundo sólido AppColors.cardBackground,
     BorderRadius.circular(20), padding 24dp, largura 85% da tela
   - Exibe item atual em destaque (ícone 120×120dp, nome, mensagem, contagem)
   - Botões: "Usar item" | "Ver próximo" (se múltiplos tipos) | "Desistir"
   - Troca de item: fade-out/in 200ms
5. "Usar item" com undo:
   → inventoryProvider.consume(type)
   → gameProvider.notifier.undo(n)
   → isAwaitingGameOverResolution = false
   → overlay some, jogo continua
6. "Usar item" com bomba:
   → isAwaitingGameOverResolution permanece true
   → gameProvider.notifier.enterBombMode(mode, type)
   → BombSelectionOverlay abre (comportamento existente)
   → Cancelar bomba: isAwaitingGameOverResolution ainda true
     → GameOverItemOverlay reaparece com mesmo item
   → Confirmar célula: bomba executada
     → isAwaitingGameOverResolution = false
     → overlay some, jogo continua
7. "Desistir":
   → AlertDialog: "Tem certeza? Você perderá uma vida."
   → Cancelar: permanece na GameOverItemOverlay
   → Confirmar: livesNotifier.consumeLife()
     → isAwaitingGameOverResolution = false
     → Navigator.pushReplacement(HomeScreen) ou NoLivesScreen
```

### Ordem de ciclagem de itens (prioridade)
1. `undo1`
2. `undo3`
3. `bomb2`
4. `bomb3`

Apenas tipos com `count > 0` entram na lista. "Ver próximo" cicla circularmente.

### Arquivos modificados/criados
| Arquivo | Mudança |
|---------|---------|
| `lib/presentation/controllers/game_notifier.dart` | Adicionar `bool isAwaitingGameOverResolution = false`; método `_getAvailableSavingItems(Inventory)`; setar flag em `_onGameOver()`; setar `false` ao resolver |
| `lib/presentation/screens/game/game_over_item_overlay.dart` | **Novo.** Widget `GameOverItemOverlay` (StatefulWidget — gerencia item atual em exibição) |
| `lib/presentation/screens/game/game_screen.dart` | Adicionar `Positioned.fill` condicional para `GameOverItemOverlay`; `AbsorbPointer` no conteúdo abaixo quando overlay ativo |
| `lib/presentation/widgets/inventory_bar.dart` | Desabilitar `onPressed` de todos os itens quando `isAwaitingGameOverResolution == true` |

### Visual do card
```
┌─────────────────────────────┐
│   [ícone PNG 120×120dp]     │
│   Nome do item (20sp SB)    │
│   Mensagem explicativa      │
│   (16sp, max 2 linhas)      │
│   Você tem: N               │
│                             │
│  [Usar item]  [Ver próximo] │
│       [Desistir]            │
└─────────────────────────────┘
```
"Ver próximo" só aparece quando há ≥2 tipos de itens disponíveis.

### Critérios de aceite
- Tabuleiro trava com undo1 disponível → overlay exibe undo1 em destaque
- "Usar item" (undo) → tabuleiro desbloqueado, item consumido, overlay some
- "Usar item" (bomba) → `BombSelectionOverlay` abre; cancelar → overlay reaparece; confirmar célula → jogo continua
- "Desistir" → `AlertDialog`; cancelar → permanece no overlay; confirmar → vida consumida, navega para `HomeScreen`/`NoLivesScreen`
- Tabuleiro trava sem itens → overlay não exibido
- `InventoryBar` taps desabilitados durante overlay ativo
- Toque no backdrop fora do card: sem ação (não fecha o overlay)

### Testes unitários obrigatórios
- `_getAvailableSavingItems`: retorna lista vazia quando inventário zerado
- `_getAvailableSavingItems`: retorna apenas tipos com count > 0, na ordem correta
- `isAwaitingGameOverResolution` setado para `true` quando `isGameOver` + itens disponíveis
- `isAwaitingGameOverResolution` setado para `false` após undo executado
- `isAwaitingGameOverResolution` setado para `false` após desistência confirmada

---

## Plano de validação manual

| Dispositivo | Resolução | D | C | A | B |
|------------|-----------|---|---|---|---|
| Emulador Android | 360×640dp | ✓ portrait | ✓ sem overflow | ✓ splash | ✓ game over flow |
| Emulador Android | 412×915dp | ✓ portrait | ✓ ícones | ✓ splash | ✓ game over flow |
| iPhone SE sim. | 360×568dp | ✓ portrait | ✓ sem overflow | ✓ splash | ✓ game over flow |
| iPhone 14 sim. | 390×844dp | ✓ portrait | ✓ ícones | ✓ splash | ✓ game over flow |
| iPad mini sim. | — | ✓ portrait | ✓ ícones | ✓ splash | ✓ game over flow |

---

## Prompt de brainstorm da Fase 3

> O prompt abaixo entra no fluxo do **superpowers/brainstorming**. O resultado esperado é uma **spec detalhada da Fase 3** (refinada via brainstorm), que depois alimenta o **superpowers/writing-plans** pra gerar o plano executável. Nada de código nesta etapa — apenas elicitação, refinamento de design e plano.

---

> Use a skill `superpowers/brainstorming` pra refinar o design da próxima fase do projeto **Olha o Bichim!** (Flutter, codename `capivara_2048`).
>
> **Contexto:** Fase 2.9 concluída (v0.9.5). Use `CAPIVARA_2048_DESIGN.md` como spec geral (especialmente §7 — Ranking, §8 — Monetização, §9 — Backend/Firebase, §15 Fase 3).
>
> **Fases concluídas:** 1 a 2.9 (v0.9.5). Áudio na Fase 5.
>
> **Tópico do brainstorm:** **Fase 3 — Backend, ranking e monetização**. Setup Firebase, autenticação, ranking global semanal, deep links de convite, Google Mobile Ads e `in_app_purchase`.
>
> **Sub-entregas:**
>
> **A — Firebase setup + autenticação:** `firebase_core`, `firebase_auth`, login Google/Apple/anônimo, `PlayerProfile` no Firestore.
>
> **B — Ranking global semanal:** tabela Firestore, score sincronizado, tela de ranking (`RankingScreen`), recompensas de ranking.
>
> **C — Convites com deep links:** sistema de códigos de compartilhamento, deep links, `InviteFriendsScreen` funcional.
>
> **D — Monetização:** Google Mobile Ads (banner/interstitial), `in_app_purchase` para os 6 pacotes da §7.1 substituindo a loja mock atual.
>
> **Pontos abertos pra explorar no brainstorm:**
>
> - Qual provider de backend? Firebase (já referenciado no design doc) vs. Supabase — confirmar escolha e justificativa
> - Autenticação: login anônimo suficiente para ranking ou Google/Apple obrigatório? Migração de conta anônima para social?
> - Ranking: score de quê exatamente — maior tile alcançado, pontuação máxima, ou combinação? Janela semanal: UTC ou timezone local?
> - Anti-cheat mínimo viável para scores (validação server-side, Cloud Functions ou regras Firestore?)
> - Sincronização de inventário/progresso entre dispositivos: escopo da Fase 3 ou Fase 6?
> - Ads: onde exibir (entre partidas, na tela de game over, na loja)? Frequência mínima para não degradar a experiência?
>
> **Output esperado do brainstorm:**
> Uma **spec detalhada da Fase 3** (`docs/superpowers/specs/YYYY-MM-DD-fase-3-design.md`) com:
> - Decisões tomadas em cada ponto aberto
> - Para cada sub-entrega: arquivos a criar/modificar, mudança exata, casos de teste obrigatórios, critérios de aceite
> - Plano de validação manual (dispositivos alvo: 360×640, 390×844, 412×915)
> - Ao final: **prompt de brainstorm da Fase 4** (Arte e polimento visual) seguindo este mesmo padrão de cascata
>
> **Não escreva código nesta etapa.** Foque em refinar o design, fazer perguntas críticas e produzir a spec.

---

## Nota para writing-plans

O `writing-plans` da Fase 2.9 deve, ao final do plano de implementação desta fase, incluir:
1. A **spec da Fase 3** produzida via brainstorm acima (adicionada a este arquivo ou em arquivo separado `docs/superpowers/specs/YYYY-MM-DD-fase-3-design.md`)
2. O **plano de implementação da Fase 3** como seção separada ou arquivo separado, pronto para execução na próxima sessão
