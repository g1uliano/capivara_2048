# Fase 2.8 — Loja Mock: Spec de Design

**Data:** 2026-05-02  
**Versão alvo:** v0.9.4  
**Fase anterior:** 2.7 ✅ (v0.9.3)  
**Próxima fase:** 3 (Backend)

---

## 1. Objetivo

Implementar a `ShopScreen` com os 6 pacotes da §7.1, cards com preços De/Por e badge de desconto, botão "Comprar" simulado que entrega itens localmente, e bottom sheet "Código para presentear" gerado após compra simulada. Sem integração real de pagamento (IAP real entra na Fase 3).

---

## 2. Decisões de Design

| Questão | Decisão | Justificativa |
|---------|---------|---------------|
| Modelo de conteúdo de pacote | `RewardBundle` com `lives + bomb2 + bomb3 + undo1 + undo3` (§13.7 exato) | Alinha com spec canônica; separação lives/inventory no `_onBuy` é trivial |
| Bombas nos combos | Combo Mata Atlântica → Bomba 2; Combo Floresta Amazônica → Bomba 3 | Progressão por tier: combo básico usa item mais fraco, combo premium usa item mais forte |
| Lógica de compra | Função local `_onBuy` no `ConsumerWidget`, sem `ShopNotifier` separado | Lógica mock é fina demais para justificar notifier próprio |
| Persistência de `ShareCode` | `ShareCodesRepository` dedicado (padrão do projeto) | Encapsula chave SharedPreferences; migração Fase 3 cirúrgica (trocar só o repositório) |
| Estrutura de arquivos | Flat — sem feature modules | Segue estrutura atual do projeto |
| Exibição do UUID | Truncado `xxxx-xxxx-xxxx` (primeiros 14 chars do UUID) | Legível sem poluir o UI |

---

## 3. Dados dos 6 Pacotes

| # | id | Nome | contents | giftContents | De (R$) | Por (R$) | % |
|---|----|------|----------|-------------|---------|---------|---|
| 1 | `p1` | 4× Bomba 3 | bomb3:4 | bomb3:2 | 7,99 | 3,99 | 50 |
| 2 | `p2` | 4× Desfazer 3 | undo3:4 | undo3:2 | 3,99 | 1,99 | 50 |
| 3 | `p3` | 6 Vidas | lives:6 | lives:3 | 9,99 | 2,49 | 75 |
| 4 | `p4` | 10 Vidas | lives:10 | lives:5 | 19,99 | 4,99 | 75 |
| 5 | `p5` | Combo Mata Atlântica | lives:6, bomb2:2, undo3:2 | lives:3, bomb2:1, undo3:1 | 10,99 | 4,99 | 50 |
| 6 | `p6` | Combo Floresta Amazônica | lives:10, bomb3:4, undo3:4 | lives:5, bomb3:2, undo3:2 | 31,99 | 9,99 | 50 |

`giftContents` = metade de `contents` (arredondado para baixo), conforme §7.2.

---

## 4. Sub-entregas

### A — Modelos (`lib/data/models/`)

**Arquivos novos:**
- `lib/data/models/shop_package.dart`
- `lib/data/models/share_code.dart`

**`shop_package.dart`:**
```dart
class RewardBundle {
  final int lives;
  final int bomb2;
  final int bomb3;
  final int undo1;
  final int undo3;
  const RewardBundle({required this.lives, required this.bomb2,
      required this.bomb3, required this.undo1, required this.undo3});
  static const empty = RewardBundle(lives:0, bomb2:0, bomb3:0, undo1:0, undo3:0);
}

class ShopPackage {
  final String id;
  final String name;
  final String description;
  final double originalPrice;
  final double currentPrice;
  final int discountPercent;
  final RewardBundle contents;
  final RewardBundle giftContents;
  const ShopPackage({...});
}
```

**`share_code.dart`:**
```dart
enum ShareCodeStatus { pending, redeemed, expired }

class ShareCode {
  final String code;           // UUID v4
  final String packageId;
  final RewardBundle giftContents;
  final ShareCodeStatus status;
  final DateTime createdAt;
  // redeemedBy / redeemedAt omitidos — adicionados na Fase 3

  Map<String, dynamic> toJson();
  factory ShareCode.fromJson(Map<String, dynamic> json);
}
```

**Critérios de aceite:**
- `RewardBundle.empty` é `const`
- `ShareCode.toJson` / `fromJson` são inversos (teste unitário)
- Todos os campos de `ShopPackage` são `final`

---

### B — `shop_data.dart` (`lib/data/shop_data.dart`)

**Arquivo novo.** Lista estática dos 6 `ShopPackage` + provider:

```dart
final shopPackagesProvider = Provider<List<ShopPackage>>((_) => kShopPackages);

const List<ShopPackage> kShopPackages = [ /* 6 pacotes conforme seção 3 */ ];
```

**Critérios de aceite:**
- `kShopPackages.length == 6`
- Preços e conteúdos batem com a tabela da seção 3
- Lista é `const`

---

### C — Persistência (`lib/data/repositories/` + `lib/domain/shop/`)

**Arquivos novos:**
- `lib/data/repositories/share_codes_repository.dart`
- `lib/domain/shop/share_codes_notifier.dart`

**`ShareCodesRepository`:**
```dart
class ShareCodesRepository {
  static const _key = 'generated_share_codes';
  Future<List<ShareCode>> load();   // decodifica JSON array
  Future<void> save(List<ShareCode> codes);  // codifica JSON array
}
```

**`ShareCodesNotifier`:**
```dart
class ShareCodesNotifier extends StateNotifier<List<ShareCode>> {
  ShareCodesNotifier(this._repo) : super([]);
  Future<void> load();
  Future<void> add(ShareCode code);  // append + save
}

final shareCodesRepositoryProvider = Provider((_) => ShareCodesRepository());
final shareCodesProvider = StateNotifierProvider<ShareCodesNotifier, List<ShareCode>>(
  (ref) => ShareCodesNotifier(ref.read(shareCodesRepositoryProvider)),
);
```

**Critérios de aceite:**
- `add()` persiste na lista e chama `save()`
- `load()` restaura lista corretamente após reinício simulado
- Chave SharedPreferences é `'generated_share_codes'`

---

### D — `ShopScreen` (`lib/presentation/screens/shop_screen.dart`)

**Arquivo existente — reescrever.**

**Layout:**
```
Scaffold (backgroundColor: transparent)
└── GameBackground()
    ├── AppBar("Loja", Fredoka 22, branco, fundo #3FA968)
    └── ListView.builder(padding: 16, itemCount: 6)
        └── _ShopPackageCard(package: pkg, onBuy: _onBuy)
            // margin bottom 12
```

**`_ShopPackageCard` (widget privado):**
```
Container(borderRadius:16, sombra inferior, fundo branco 0.92 opacity)
├── Row
│   ├── Text(name, Fredoka 18)
│   └── CircleAvatar(bg:#FF8C42, Text("50%", Fredoka Bold branco))
├── Text(description, Nunito 14, #9E9E9E)
├── Row
│   ├── Text("De R$ X,XX", Nunito 14, lineThrough, #9E9E9E)
│   └── Text("Por R$ X,XX", Fredoka 20, #3FA968)
└── ElevatedButton("Comprar", fullWidth, bg:#3FA968, onPressed: () => onBuy(package))
```

**`_onBuy` (método em `ShopScreen extends ConsumerWidget`):**
```
1. showDialog → AlertDialog
   título: "Confirmar compra"
   conteúdo: "Comprar [nome] por R$ X,XX?"
   ações: [TextButton("Cancelar"), ElevatedButton("Confirmar")]

2. Ao confirmar (await == true):
   a. if contents.lives > 0: ref.read(livesProvider.notifier).addPurchased(contents.lives)
   b. if contents.bomb2 > 0: ref.read(inventoryProvider.notifier).add(ItemType.bomb2, contents.bomb2)
   c. if contents.bomb3 > 0: ref.read(inventoryProvider.notifier).add(ItemType.bomb3, contents.bomb3)
   d. if contents.undo1 > 0: ref.read(inventoryProvider.notifier).add(ItemType.undo1, contents.undo1)
   e. if contents.undo3 > 0: ref.read(inventoryProvider.notifier).add(ItemType.undo3, contents.undo3)
   f. final code = ShareCode(
        code: const Uuid().v4(),
        packageId: package.id,
        giftContents: package.giftContents,
        status: ShareCodeStatus.pending,
        createdAt: DateTime.now(),
      )
   g. await ref.read(shareCodesProvider.notifier).add(code)
   h. if (mounted) showModalBottomSheet → _GiftCodeSheet(code: code)
```

**`_GiftCodeSheet` (widget privado):**
```
DraggableScrollableSheet(initialChildSize:0.5, min:0.4, max:0.8)
├── Text("Presente gerado!", Fredoka 24)
├── Text("Compartilhe este código com um amigo:", Nunito 14)
├── Container(destaque)
│   └── Text(code truncado "xxxx-xxxx-xxxx", Fredoka Bold 22)
├── IconButton(Icons.copy_outlined) → Clipboard.setData + SnackBar("Copiado!")
├── Text("Seu amigo recebe: [descrição de giftContents]", Nunito 14)
└── ElevatedButton("Fechar", onPressed: Navigator.pop)
```

Truncagem do UUID: `code.replaceAll('-','').substring(0,12)` formatado como `${s.substring(0,4)}-${s.substring(4,8)}-${s.substring(8,12)}` → `xxxx-xxxx-xxxx`.

**Critérios de aceite:**
- 6 cards visíveis em scroll em 360×640 sem overflow
- Badge 50% nos pacotes 1,2,5,6; badge 75% nos pacotes 3,4
- Preço "De" riscado, preço "Por" em verde
- Cancelar no dialog → inventário não muda
- Confirmar → itens entregues imediatamente
- `_GiftCodeSheet` abre com código truncado
- Botão copiar → clipboard + snackbar
- Sem IAP real

---

## 5. Providers — Resumo

| Provider | Tipo | Novo? | Arquivo |
|----------|------|-------|---------|
| `shopPackagesProvider` | `Provider<List<ShopPackage>>` | ✅ | `shop_data.dart` |
| `shareCodesRepositoryProvider` | `Provider<ShareCodesRepository>` | ✅ | `share_codes_notifier.dart` |
| `shareCodesProvider` | `StateNotifierProvider<ShareCodesNotifier, List<ShareCode>>` | ✅ | `share_codes_notifier.dart` |
| `inventoryProvider` | já existe | — | reutilizar |
| `livesProvider` | já existe | — | reutilizar via `addPurchased()` |

---

## 6. Testes Obrigatórios

### Widget tests (`test/presentation/screens/shop_screen_test.dart`)
```dart
testWidgets('6 cards presentes no widget tree')
testWidgets('cada card exibe nome, preço De riscado, preço Por, badge %')
testWidgets('tap Comprar → AlertDialog de confirmação aparece')
testWidgets('cancelar AlertDialog → inventoryProvider não muda')
testWidgets('confirmar compra p1 → bomb3 +4 no inventário')
testWidgets('confirmar compra p3 → livesProvider.addPurchased(6) chamado')
testWidgets('confirmar compra → _GiftCodeSheet aparece')
testWidgets('_GiftCodeSheet → código em formato xxxx-xxxx-xxxx')
testWidgets('botão Copiar → código no clipboard')
```

### Unit tests (`test/domain/shop/share_codes_notifier_test.dart`)
```dart
test('add() appenda código e persiste')
test('load() restaura lista do repositório')
test('ShareCode.toJson / fromJson são inversos')
```

---

## 7. Validação Manual

1. **360×640** — 6 cards scrollam sem overflow horizontal ou vertical
2. **414×896** — espaçamento adequado, sem cards cortados
3. Comprar cada um dos 6 pacotes — verificar inventário/vidas incrementados corretamente
4. Badge 50% em p1, p2, p5, p6; badge 75% em p3, p4
5. Copiar código → colar em outro app para confirmar clipboard
6. Fechar sheet → volta à lista sem erro de navegação
7. Reiniciar app → códigos gerados persistem (SharedPreferences)

---

## 8. Arquivos Modificados / Criados

| Ação | Arquivo |
|------|---------|
| Criar | `lib/data/models/shop_package.dart` |
| Criar | `lib/data/models/share_code.dart` |
| Criar | `lib/data/shop_data.dart` |
| Criar | `lib/data/repositories/share_codes_repository.dart` |
| Criar | `lib/domain/shop/share_codes_notifier.dart` |
| Reescrever | `lib/presentation/screens/shop_screen.dart` |
| Criar | `test/presentation/screens/shop_screen_test.dart` |
| Criar | `test/domain/shop/share_codes_notifier_test.dart` |
| Atualizar | `CHANGELOG.md` |
| Atualizar | `README.md` |
| Atualizar | `CLAUDE.md` |
| Atualizar | `CAPIVARA_2048_DESIGN.md` §15 |

---

## 9. Slot de Migração Fase 3

| Criado na Fase 2.8 | O que a Fase 3 faz |
|--------------------|-------------------|
| `_onBuy` mock (entrega local imediata) | Substituir por `in_app_purchase` real com receipt validation |
| `ShareCodesRepository` (SharedPreferences) | Trocar implementação por Firestore (`shareCodes/{code}`) |
| `ShareCode` sem `redeemedBy`/`redeemedAt` | Adicionar campos e lógica de resgate |
| `_GiftCodeSheet` com código local | Conectar ao backend para validação e expiração |

---

## 10. Prompt de Brainstorm — Fase 3 (Backend)

> Use a skill `superpowers/brainstorming` pra refinar o design da **Fase 3** do projeto **Olha o Bichim!** (Flutter, codename `capivara_2048`).
>
> **Contexto:** Fase 2.8 concluída (v0.9.4). Use `CAPIVARA_2048_DESIGN.md` como spec geral (especialmente §9, §10, §11, §14.2 e §15 — Fase 3).
>
> **Fases concluídas:** 1 a 2.8. Áudio na Fase 5.
>
> **Tópico do brainstorm:** **Fase 3 — Backend (Firebase)**. Autenticação anônima + upgrade para conta Google/Apple, Firestore para perfil do jogador e ranking semanal, migração de `ShareCode` de SharedPreferences para Firestore, deep links de convite (`app_links`), push notifications para recompensas de ranking e convites aceitos.
>
> **Sub-entregas principais:**
> - **A — Auth:** `firebase_auth` com login anônimo automático + upgrade opcional para Google/Apple Sign-In. `AuthNotifier` com estado `unauthenticated | anonymous | authenticated`.
> - **B — Firestore sync:** `PlayerProfile` sincronizado em `users/{userId}`. Merge local→remoto no primeiro login com conta.
> - **C — Ranking semanal:** coleção `rankings/{weekId}/entries/{userId}`, leitura em `RankingScreen` (top 10 + posição do jogador), escrita no fim de cada partida.
> - **D — ShareCode backend:** migrar `ShareCodesRepository` para Firestore (`shareCodes/{code}`), implementar resgate de código com validação e expiração.
> - **E — Convites:** deep link via `app_links`, registro de vínculo convidante→convidado, entrega de recompensa após 1ª partida do convidado.
> - **F — Push:** `firebase_messaging`, notificações para recompensa de ranking semanal e convite aceito.
>
> **Pontos abertos pra explorar no brainstorm:**
> - Estratégia de merge local→remoto no upgrade de conta anônima: qual dado tem precedência (local ou remoto)?
> - Ranking: leitura em tempo real (`snapshots()`) ou one-shot (`get()`) com pull-to-refresh?
> - Regras de segurança Firestore: quem pode escrever em `rankings`? Só Cloud Functions ou também o cliente?
> - `ShareCode` expiração: TTL fixo (ex: 30 dias) ou configurável por package?
> - Convite: como detectar que o convidado completou a 1ª partida sem Cloud Functions? (ou Cloud Functions são necessárias aqui?)
> - Versão mínima de Firebase compatível com Flutter 3.x sem conflitos com pacotes existentes.
>
> **Output esperado do brainstorm:**
> Uma **spec detalhada da Fase 3** (`docs/superpowers/specs/YYYY-MM-DD-fase-3-design.md`) com:
> - Decisões tomadas em cada ponto aberto
> - Para cada sub-entrega: arquivos a modificar, mudança exata, casos de teste obrigatórios, critérios de aceite
> - Plano de validação manual
> - Ao final: **prompt de brainstorm da Fase 4** (Arte adicional e polimento visual) seguindo este mesmo padrão de cascata
>
> **Não escreva código nesta etapa.** Foque em refinar o design, fazer perguntas críticas e produzir a spec.
