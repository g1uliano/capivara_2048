# Spec — Fase 2.6: Home Redesenhada + Coleção + Configurações

**Data:** 2026-05-01  
**Versão alvo:** v0.9.2  
**Fase anterior:** 2.5 ✅ (v0.9.1 — rebranding, GameTitleImage, ícone, launcher name)  
**Próxima fase:** 2.7 — Loja mock  

---

## Decisões tomadas no brainstorm

| Ponto aberto | Decisão |
|---|---|
| Layout da Home | Grid 2×N de cards + botão "Jogar" em destaque acima |
| Animação do logo | `flutter_animate` fade+scale (400ms) nesta fase |
| Como Jogar | Bottom sheet (`showModalBottomSheet`) |
| Grid da Coleção | 2 colunas — cards grandes com fun fact truncado visível |
| Card detalhado | Bottom sheet com `DraggableScrollableSheet` |
| Silhueta animal bloqueado | `Image.asset(..., color: Colors.black54, colorBlendMode: BlendMode.srcATop)` |
| Dados fun fact / scientificName | Preencher todos os 11 animais em `animals_data.dart` |
| Golden tests | Não — widget tests são suficientes |
| Navegação | `Navigator.push` + `MaterialPageRoute` — manter até Fase 3 |
| Transições | `MaterialPageRoute` padrão (slide) — animar na Fase 6 |
| Configurações | Tela dedicada via `Navigator.push` |
| Sobre | "Olha o Bichim! © Catraia Aplicativos" |

---

## Diagrama de navegação

```
HomeScreen
├── → GameScreen              (botão "Jogar" / "Continuar")    [existente]
├── → DailyRewardsScreen      (card Recompensa Diária)          [existente]
├── → CollectionScreen        (card Coleção)                    [NOVO]
├── → SettingsScreen          (card Configurações)              [NOVO]
├── → ShopScreen              (card Loja)                       [stub NOVO]
├── ✗ RankingScreen           (card Ranking — disabled)         [sem rota nesta fase]
├── ↓ HowToPlaySheet          (card Como Jogar)                 [bottom sheet NOVO]
├── → InviteFriendsScreen     (sem card na Home nesta fase)     [stub NOVO, não exposto]
└── → RedeemCodeScreen        (sem card na Home nesta fase)     [stub NOVO, não exposto]
```

Legenda: `→` rota navegável, `↓` bottom sheet, `✗` desabilitado sem rota, sem card = stub criado mas não acessível pela Home ainda.

---

## Sub-entrega A — Home Redesenhada

### Arquivos

| Arquivo | Ação |
|---|---|
| `lib/presentation/screens/home_screen.dart` | Reescrever in-place |

### Layout (de cima pra baixo, sem scroll em ≥ 360dp)

```
Scaffold (sem AppBar nativa)
└── Stack
    ├── GameBackground()                          // fundo.png, BoxFit.cover
    └── SafeArea
        └── Column
            ├── LivesIndicator()                  // topo, centralizado
            ├── GameTitleImage(
            │     asset: _titleAsset,
            │     height: 220,
            │   ).animate()
            │    .fadeIn(duration: 400.ms)
            │    .scale(begin: Offset(0.85, 0.85))
            ├── _PlayButton()                     // full width, primary #3FA968
            └── GridView.count(
                  crossAxisCount: 2,
                  shrinkWrap: true,
                  physics: NeverScrollableScrollPhysics(),
                  padding: EdgeInsets.symmetric(horizontal: 16, vertical: 8),
                )
                // cards em ordem:
                // [Loja] [Ranking (disabled)]
                // [Recompensa Diária (badge)] [Coleção]
                // [Configurações] [Como Jogar]
```

### Widgets privados

**`_PlayButton`**
```dart
// Detecta gameProvider — se GameState.isGameOver == false E board tem pelo menos 1 tile não-zero: label "Continuar"
// Caso contrário: "Novo Jogo"
// onTap: Navigator.push → GameScreen
// Estilo: botão largo full-width, Fredoka Bold 20, cor #3FA968, borderRadius 16, sombra inferior
```

**`_HomeCard`**
```dart
_HomeCard({
  required String label,
  required IconData icon,
  required VoidCallback? onTap,   // null = desabilitado
  bool showBadge = false,
  bool comingSoon = false,
})
// Container: borderRadius 16, sombra inferior, cor de fundo branca leve
// Conteúdo: ícone (Phosphor/Lucide, 32dp) + Text(label, Nunito 14)
// comingSoon: Opacity(0.5) + label "Em breve" sobreposto
// showBadge: círculo vermelho no canto superior direito (badge)
```

**`_HowToPlaySheet`** (bottom sheet)
```dart
// Conteúdo: título "Como Jogar" (Fredoka 24), regras básicas do 2048,
// explicação dos animais e vidas — texto em Nunito 14
// Chamado via: showModalBottomSheet(context, builder: (_) => _HowToPlaySheet())
```

### Providers consumidos

| Provider | Uso |
|---|---|
| `gameProvider` | Detectar partida salva: `GameState.isGameOver == false && board tem tile não-zero` |
| `dailyRewardsProvider` | Badge no card Recompensa Diária (recompensa disponível?) |

### `DailyRewardEntryTile` existente

Removido do layout da Home. O badge de recompensa diária passa para `_HomeCard("Recompensa Diária", showBadge: ...)`. O widget `DailyRewardEntryTile` pode ser mantido no codebase para uso eventual em outras telas, mas não aparece mais na Home.

### Testes obrigatórios

```dart
// home_screen_test.dart
testWidgets('6 _HomeCard presentes no widget tree (Loja, Ranking, Recompensa Diária, Coleção, Config, Como Jogar)', ...)
testWidgets('_PlayButton presente no widget tree', ...)
testWidgets('botão mostra "Continuar" quando GameState tem partida salva', ...)
testWidgets('botão mostra "Novo Jogo" quando não há partida salva', ...)
testWidgets('badge aparece no card Recompensa Diária quando recompensa disponível', ...)
testWidgets('card Ranking tem onTap null (desabilitado)', ...)
testWidgets('tap em "Como Jogar" abre bottom sheet', ...)
testWidgets('GameTitleImage presente no widget tree', ...)
```

### Critérios de aceite

- Home carrega sem overflow em 360dp, 390dp e 414dp
- Logo anima ao entrar na tela (fade + scale)
- Grid 2×3 visível sem scroll em dispositivos ≥ 5"
- Badge vermelho no card Recompensa Diária quando há recompensa pendente
- Card Ranking com visual desabilitado (opacidade 0.5, label "Em breve")
- `DailyRewardEntryTile` não aparece mais como widget flutuante na Home

---

## Sub-entrega B — Tela de Coleção

### Arquivos

| Arquivo | Ação |
|---|---|
| `lib/presentation/screens/collection_screen.dart` | Criar |
| `lib/data/animals_data.dart` | Preencher `funFact` e `scientificName` dos 11 animais |

### Layout

```
Scaffold
└── GameBackground()
    └── Column
        ├── AppBar customizada
        │   title: Text("Coleção", style: Fredoka 22)
        │   backgroundColor: #3FA968
        │   leading: BackButton (Navigator.pop)
        ├── Padding
        │   └── Column
        │       ├── Text("X/11 animais descobertos", Fredoka 16)
        │       └── LinearProgressIndicator(value: X / 11.0)
        └── Expanded
            └── GridView.count(
                  crossAxisCount: 2,
                  padding: EdgeInsets.all(12),
                  mainAxisSpacing: 12,
                  crossAxisSpacing: 12,
                )
                // _AnimalCard × 11, nível crescente 1→11
```

### `_AnimalCard` — desbloqueado (`highestLevelReached >= animal.level`)

```dart
GestureDetector(
  onTap: () => showModalBottomSheet(..., builder: (_) => _AnimalDetailSheet(animal)),
  child: Container(
    decoration: BoxDecoration(
      color: animal.backgroundBaseColor,
      borderRadius: BorderRadius.circular(16),
      boxShadow: [/* sombra inferior suave */],
    ),
    child: Column(
      children: [
        Image.asset(animal.tilePngPath, height: 80),
        Text(animal.name, style: Fredoka 16),
        Text(
          animal.funFact ?? '',
          style: Nunito 12,
          maxLines: 2,
          overflow: TextOverflow.ellipsis,
        ),
      ],
    ),
  ),
)
```

### `_AnimalCard` — bloqueado (`highestLevelReached < animal.level`)

```dart
Container(
  decoration: BoxDecoration(
    color: Colors.grey.shade300,
    borderRadius: BorderRadius.circular(16),
  ),
  child: Column(
    children: [
      Image.asset(
        animal.tilePngPath,
        height: 80,
        color: Colors.black54,
        colorBlendMode: BlendMode.srcATop,
      ),
      Text("???", style: Fredoka 16, color: Colors.black38),
    ],
  ),
)
// Sem onTap
```

### `_AnimalDetailSheet` (bottom sheet)

```dart
DraggableScrollableSheet(
  initialChildSize: 0.6,
  maxChildSize: 0.9,
  minChildSize: 0.4,
  builder: (context, scrollController) => SingleChildScrollView(
    controller: scrollController,
    child: Column(
      children: [
        Image.asset(animal.hostPngPath, height: 160),   // host 2×2
        Text(animal.name, style: Fredoka Bold 24),
        if (animal.scientificName != null)
          Text(animal.scientificName!, style: Nunito italic 14, color: Colors.grey),
        Text(animal.funFact!, style: Nunito 16),
      ],
    ),
  ),
)
```

### Dados a preencher em `animals_data.dart`

Todos os 11 animais devem ter `funFact` e `scientificName` não-nulos:

| Level | Nome | scientificName | funFact (sugestão) |
|---|---|---|---|
| 1 | Tanajura | *Atta laevigata* | "Pode carregar até 50× seu próprio peso!" |
| 2 | Lobo-guará | *Chrysocyon brachyurus* | "Tem as pernas longas para ver por cima do capim do cerrado." |
| 3 | Sapo-cururu | *Rhinella marina* | "Libera veneno pelas glândulas atrás dos olhos." |
| 4 | Tucano | *Ramphastos toco* | "O bico colorido ajuda a regular a temperatura do corpo." |
| 5 | Sagui | *Callithrix jacchus* | "Afia os dentes na casca das árvores para beber a seiva." |
| 6 | Preguiça | *Bradypus tridactylus* | "Desce da árvore apenas uma vez por semana — para fazer as necessidades!" |
| 7 | Mico-leão-dourado | *Leontopithecus rosalia* | "Espécie símbolo da Mata Atlântica, salvo da extinção nos anos 80." |
| 8 | Boto-cor-de-rosa | *Inia geoffrensis* | "O maior golfinho de rio do mundo, chega a 2,5 metros." |
| 9 | Onça-pintada | *Panthera onca* | "Mata mordendo o crânio — técnica única entre os grandes felinos." |
| 10 | Sucuri | *Eunectes murinus* | "A maior cobra do mundo por peso, podendo passar de 250 kg." |
| 11 | Capivara Lendária | *Hydrochoerus hydrochaeris* | "O maior roedor do planeta — e o mais querido da internet brasileira." |

> Nota: os textos acima são sugestões para o brainstorm. O implementador pode ajustar tom e extensão, mantendo cada fun fact em 1 frase curta e divertida.

### Providers consumidos

| Provider | Uso |
|---|---|
| `gameProvider.select((s) => s.highestLevelReached)` | Determina quais cards são desbloqueados |

### Testes obrigatórios

```dart
// collection_screen_test.dart
testWidgets('highestLevelReached=0 → 11 cards bloqueados, cabeçalho "0/11"', ...)
testWidgets('highestLevelReached=1 → 1 card colorido, 10 bloqueados', ...)
testWidgets('highestLevelReached=5 → 5 coloridos, 6 bloqueados', ...)
testWidgets('highestLevelReached=11 → 11 coloridos, cabeçalho "11/11 animais descobertos"', ...)
testWidgets('tap em card desbloqueado → bottom sheet com nome e fun fact', ...)
testWidgets('tap em card bloqueado → nenhum bottom sheet', ...)
testWidgets('LinearProgressIndicator.value == highestLevelReached / 11.0', ...)
```

### Critérios de aceite

- Grid 2 colunas visível sem overflow em 360dp
- Cards bloqueados mostram silhueta escura + "???" sem texto do animal
- Cards desbloqueados mostram fun fact truncado em 2 linhas
- Cabeçalho atualiza corretamente conforme `highestLevelReached`
- Bottom sheet do card detalhado abre com host 2×2, nome, nome científico, fun fact
- Nenhum animal tem `funFact == null` ou `scientificName == null` nos dados

---

## Sub-entrega C — Tela de Configurações

### Arquivos

| Arquivo | Ação |
|---|---|
| `lib/presentation/screens/settings_screen.dart` | Criar |
| `lib/presentation/controllers/settings_notifier.dart` | Criar |
| `pubspec.yaml` | Adicionar `package_info_plus` |

### Modelo de estado

```dart
// lib/presentation/controllers/settings_notifier.dart

class SettingsState {
  final bool hapticEnabled;
  final String locale; // "pt" | "en"

  const SettingsState({
    this.hapticEnabled = true,
    this.locale = 'pt',
  });

  SettingsState copyWith({bool? hapticEnabled, String? locale});
}

class SettingsNotifier extends StateNotifier<SettingsState> {
  SettingsNotifier(this._prefs) : super(_loadFromPrefs(_prefs));

  final SharedPreferences _prefs;

  static SettingsState _loadFromPrefs(SharedPreferences prefs) => SettingsState(
    hapticEnabled: prefs.getBool('settings.haptic_enabled') ?? true,
    locale: prefs.getString('settings.locale') ?? 'pt',
  );

  void setHaptic(bool value) {
    _prefs.setBool('settings.haptic_enabled', value);
    state = state.copyWith(hapticEnabled: value);
  }

  void setLocale(String locale) {
    _prefs.setString('settings.locale', locale);
    state = state.copyWith(locale: locale);
  }
}

final settingsProvider =
    StateNotifierProvider<SettingsNotifier, SettingsState>(...);
```

### Layout

```
Scaffold
└── GameBackground()
    └── Column
        ├── AppBar customizada
        │   title: Text("Configurações", Fredoka 22)
        │   backgroundColor: #3FA968
        │   leading: BackButton
        └── Expanded
            └── ListView
                ├── _SettingsSection("Geral")
                │   ├── SwitchListTile(
                │   │     title: Text("Vibração"),
                │   │     value: settingsProvider.hapticEnabled,
                │   │     onChanged: notifier.setHaptic,
                │   │   )
                │   └── ListTile(
                │         title: Text("Idioma"),
                │         trailing: SegmentedButton<String>(
                │           segments: [ButtonSegment("pt", "PT-BR"),
                │                      ButtonSegment("en", "EN")],
                │           selected: {settingsProvider.locale},
                │           onSelectionChanged: (s) => notifier.setLocale(s.first),
                │         ),
                │       )
                ├── _SettingsSection("Áudio")
                │   ├── ListTile(title: Text("Disponível na Fase 5",
                │   │           style: Nunito 13 italic, color: Colors.grey))
                │   ├── _DisabledSlider(label: "Volume SFX")
                │   └── _DisabledSlider(label: "Volume Música")
                └── _SettingsSection("Sobre")
                    ├── ListTile("Versão", trailing: Text(packageVersion))
                    └── ListTile("Olha o Bichim! © Catraia Aplicativos")
```

**`_DisabledSlider`**
```dart
// Slider(value: 1.0, onChanged: null) com Opacity(opacity: 0.4)
// Fase 5 remove o Opacity wrapper e conecta ao provider de áudio
```

**`_SettingsSection`**
```dart
// Padding + Text(title, Fredoka 14, cor primária) + Divider
```

**Versão do app**
```dart
// FutureProvider<String> packageVersionProvider via PackageInfo.fromPlatform()
// Exibido como "1.0.0+24" (version+buildNumber)
```

### Haptic — onde aplicar no app

Ao habilitar haptic, chamar `HapticFeedback.lightImpact()` em:
- Todos os botões da `HomeScreen` (onTap)
- Merge de tiles no `GameNotifier` (ao fundir dois tiles)
- Confirmação de uso de item no inventário

Implementação: helper global `maybeHaptic(WidgetRef ref)` que verifica `settingsProvider.hapticEnabled` antes de chamar.

```dart
// lib/core/utils/haptic_utils.dart
void maybeHaptic(WidgetRef ref) {
  if (ref.read(settingsProvider).hapticEnabled) {
    HapticFeedback.lightImpact();
  }
}
```

### Testes obrigatórios

```dart
// settings_screen_test.dart
testWidgets('toggle haptic → SharedPreferences persiste true/false', ...)
testWidgets('toggle idioma → SharedPreferences persiste "pt"/"en"', ...)
testWidgets('sliders de áudio presentes no widget tree com onChanged == null', ...)
testWidgets('versão do app presente no widget tree', ...) // mock PackageInfo
testWidgets('"Olha o Bichim! © Catraia Aplicativos" presente', ...)
```

### Critérios de aceite

- Toggle haptic persiste após hot restart
- Toggle idioma persiste após hot restart (não altera o locale do app nesta fase)
- Sliders de áudio visíveis mas não interativos, com label "Disponível na Fase 5"
- Versão exibida corretamente (lida de `PackageInfo`)
- Seção "Sobre" exibe "Olha o Bichim! © Catraia Aplicativos"

---

## Sub-entrega D — Navegação

### Arquivos

| Arquivo | Ação |
|---|---|
| `lib/presentation/screens/shop_screen.dart` | Criar (stub) |
| `lib/presentation/screens/invite_friends_screen.dart` | Criar (stub) |
| `lib/presentation/screens/redeem_code_screen.dart` | Criar (stub) |

### Template de stub

```dart
// Aplicar em ShopScreen, InviteFriendsScreen, RedeemCodeScreen
class XxxScreen extends StatelessWidget {
  const XxxScreen({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(
    body: Stack(
      children: [
        const GameBackground(),
        SafeArea(
          child: Column(
            children: [
              AppBar(
                title: Text("Título", style: Fredoka 22),
                backgroundColor: const Color(0xFF3FA968),
                leading: const BackButton(),
              ),
              const Expanded(
                child: Center(
                  child: Text("Em breve", style: /* Fredoka 28, cor primária */),
                ),
              ),
            ],
          ),
        ),
      ],
    ),
  );
}
```

### Padrão de navegação

```dart
// Uso em HomeScreen:
Navigator.push(context, MaterialPageRoute(builder: (_) => const CollectionScreen()));
Navigator.push(context, MaterialPageRoute(builder: (_) => const SettingsScreen()));
Navigator.push(context, MaterialPageRoute(builder: (_) => const ShopScreen()));
// Ranking: onTap: null (sem navegação)
// Como Jogar: showModalBottomSheet(...)
```

### Testes obrigatórios

```dart
// home_screen_test.dart (complemento ao Sub-entrega A)
testWidgets('tap Coleção → CollectionScreen no Navigator stack', ...)
testWidgets('tap Configurações → SettingsScreen no Navigator stack', ...)
testWidgets('tap Loja → ShopScreen no Navigator stack', ...)
testWidgets('tap Ranking → sem navegação', ...)
testWidgets('tap Como Jogar → bottom sheet visível', ...)
```

### Critérios de aceite

- Todas as telas secundárias têm AppBar com botão Voltar funcional
- Stubs exibem "Em breve" centralizado com visual consistente
- Ranking não navega; card visualmente desabilitado
- `InviteFriendsScreen` e `RedeemCodeScreen` existem no codebase mas sem card na Home

---

## Sub-entrega E — Documentação

### Arquivos a atualizar ao fechar a fase

| Arquivo | O que atualizar |
|---|---|
| `CHANGELOG.md` | Entrada v0.9.2 com sub-entregas A–D |
| `README.md` | Roadmap: Fase 2.6 ✅, Fase 2.7 como próxima |
| `CLAUDE.md` | Fase atual: "Fase 2.6 concluída — próximo: Fase 2.7" |
| `CAPIVARA_2048_DESIGN.md` §15 | Marcar Fase 2.6 como ✅ |
| `CAPIVARA_2048_DESIGN.md` §17 | Substituir prompt atual pelo prompt da Fase 2.7 |
| `CAPIVARA_2048_DESIGN.md` (nova seção) | Adicionar spec detalhada da Fase 2.7 (ver §15 expandido abaixo) |

---

## Sincronização com fases futuras

### Fase 2.7 — Loja mock

| Slot criado na 2.6 | O que a 2.7 faz |
|---|---|
| `ShopScreen` (stub) | Preenche com grid de 6 pacotes, cards De/Por, botão "Comprar" simulado |
| `_HomeCard("Loja")` | Sem alteração — rota já existe |

### Fase 3 — Backend / PlayerProfile

| Slot criado na 2.6 | O que a Fase 3 faz |
|---|---|
| `CollectionScreen` consome `gameProvider.highestLevelReached` | Trocar por `playerProfileProvider.highestLevelReached` sem alterar o widget |
| `SettingsScreen` locale persistido em SharedPreferences | Fase 6 lê e passa para `MaterialApp.locale` |
| Cards de Ranking, Convidar, Resgatar Código | Fase 3 habilita com dados reais |

### Fase 5 — Áudio

| Slot criado na 2.6 | O que a Fase 5 faz |
|---|---|
| `_DisabledSlider` SFX e Música em Configurações | Remove `Opacity` wrapper e conecta ao provider de áudio |
| `settingsProvider` com `hapticEnabled` | Sem alteração — provider já existe |

---

## Plano de validação manual

| Cenário | Plataforma |
|---|---|
| Home carrega sem erro, logo anima, grid 2×3 visível sem scroll | Android emulator + iOS simulator |
| Botão "Continuar" aparece com partida salva; "Novo Jogo" sem partida | Android emulator |
| Badge no card Recompensa Diária quando recompensa disponível | Android emulator |
| Card Ranking visualmente desabilitado, tap não navega | Android emulator |
| Coleção: 0 desbloqueados → tudo silhueta, cabeçalho "0/11" | Android emulator |
| Coleção: 5 desbloqueados → 5 cards coloridos + fun fact truncado | Android emulator |
| Coleção: tap em desbloqueado → bottom sheet com host, nome, scientificName, fun fact | Android emulator |
| Coleção: tap em bloqueado → sem reação | Android emulator |
| Coleção: 11/11 → cabeçalho "11/11 animais descobertos", progress completo | Android emulator |
| Configurações: toggle haptic persiste após hot restart | Android emulator |
| Configurações: toggle idioma persiste após hot restart | Android emulator |
| Configurações: sliders de áudio presentes mas não interativos | Android emulator + iOS simulator |
| Stubs (Loja, Como Jogar) navegam e voltam corretamente | Android + Web |
| Web: layout Home sem overflow em 360px (Chrome DevTools) | Web (Chrome) |
| iOS: animação fade+scale do logo sem jank | iOS simulator |

---

## Spec da Fase 2.7 — Loja Mock

> Esta seção expande o item "Fase 2.7 — Loja mock (3 dias)" do §15, seguindo o mesmo padrão de spec da Fase 2.6.

### Objetivo

Implementar a `ShopScreen` com os 6 pacotes da §7.1, cards com preços De/Por e badge de desconto, botão "Comprar" simulado que entrega os itens localmente, e tela de "Código para presentear" gerada após a compra simulada. Sem integração real de pagamento (IAP real entra na Fase 3).

### Sub-entregas

**A — ShopScreen (conteúdo)**

Substituir o stub criado na Fase 2.6 pelo conteúdo real.

Arquivos:
- `lib/presentation/screens/shop_screen.dart` — reescrever
- `lib/data/shop_data.dart` — criar (lista dos 6 `ShopPackage`)
- `lib/presentation/controllers/shop_notifier.dart` — criar

Layout:
```
Scaffold
└── GameBackground()
    └── Column
        ├── AppBar("Loja", Fredoka 22, #3FA968, BackButton)
        └── Expanded
            └── ListView
                // 6 _ShopPackageCard, ordem da tabela §7.1
```

**`_ShopPackageCard`**
```dart
_ShopPackageCard({required ShopPackage package})
// Container: borderRadius 16, sombra inferior, fundo branco leve
// Conteúdo:
//   Row: nome (Fredoka 18) + badge desconto (círculo laranja #FF8C42: "50%" ou "75%")
//   Text(description, Nunito 14, cor cinza)
//   Row: Text("De R$ X,XX", Nunito 14, riscado, cinza) + Text("Por R$ X,XX", Fredoka 20, #3FA968)
//   ElevatedButton("Comprar", onTap: _onBuy)
```

**`_onBuy` — fluxo de compra simulada**
1. Mostrar `AlertDialog` de confirmação: "Comprar [nome] por R$ X,XX?"
2. Ao confirmar: `inventoryProvider.add(package.contents)` + `livesProvider.add(package.contents.lives)`
3. Mostrar `_GiftCodeSheet` com o código gerado (`ShareCode` local, sem backend)
4. `ShareCode` gerado localmente com UUID, status `pending`, armazenado em `SharedPreferences` (lista de códigos gerados — Fase 3 migra para Firestore)

**B — `_GiftCodeSheet` (bottom sheet)**

```dart
// DraggableScrollableSheet, initialChildSize: 0.5
// Conteúdo:
//   Text("Presente gerado!", Fredoka 24)
//   Text("Compartilhe este código com um amigo:", Nunito 14)
//   Container com código em Fredoka Mono 28 (ou Fredoka Bold)
//   IconButton(Icons.copy) — copia para clipboard
//   Text("Seu amigo recebe: [conteúdo do giftContents]", Nunito 14)
//   ElevatedButton("Fechar")
```

**C — Dados `shop_data.dart`**

```dart
// Lista dos 6 ShopPackage conforme §7.1:
// [01] 4× Bomba 3 — De R$7,99 / Por R$3,99 / 50%
// [02] 4× Desfazer 3 — De R$3,99 / Por R$1,99 / 50%
// [03] 6 vidas — De R$9,99 / Por R$2,49 / 75%
// [04] 10 vidas — De R$19,99 / Por R$4,99 / 75%
// [05] Combo Mata Atlântica (6v+2b+2d) — De R$10,99 / Por R$4,99 / 50%
// [06] Combo Floresta Amazônica (10v+4b+4d) — De R$31,99 / Por R$9,99 / 50%
```

**D — Persistência local de `ShareCode`**

```dart
// SharedPreferences key: 'generated_share_codes'
// Valor: List<String> de JSON serializado dos ShareCode gerados
// Fase 3 migra para Firestore (sem quebra: mesma estrutura ShareCode)
```

**E — Documentação**
- `CHANGELOG.md`: entrada v0.9.3
- `README.md`: Fase 2.7 ✅
- `CLAUDE.md`: fase atual → "Fase 2.7 concluída — próximo: Fase 3"
- `CAPIVARA_2048_DESIGN.md` §15: marcar Fase 2.7 ✅
- `CAPIVARA_2048_DESIGN.md` §17: substituir pelo prompt da Fase 3

### Providers

| Provider | Tipo | Novo? |
|---|---|---|
| `shopPackagesProvider` | `Provider<List<ShopPackage>>` | Sim — lista estática de `shop_data.dart` |
| `generatedShareCodesProvider` | `StateNotifierProvider<...>` | Sim — lista local de `ShareCode` gerados |
| `inventoryProvider` | já existe | Reusar — `add()` com `package.contents` |
| `livesProvider` | já existe | Reusar — `add(package.contents.lives)` |

### Testes obrigatórios

```dart
// shop_screen_test.dart
testWidgets('6 cards de pacotes presentes no widget tree', ...)
testWidgets('cada card exibe nome, preço De, preço Por, badge desconto', ...)
testWidgets('tap Comprar → AlertDialog de confirmação', ...)
testWidgets('confirmar compra → inventoryProvider atualizado', ...)
testWidgets('confirmar compra → _GiftCodeSheet aparece com código', ...)
testWidgets('botão copiar → código no clipboard', ...)
```

### Critérios de aceite

- 6 pacotes visíveis em scroll sem overflow
- Badge de desconto correto (50% / 75%) em cada card
- Preço "De" riscado, preço "Por" em destaque verde
- Compra simulada entrega itens no inventário imediatamente
- Código de presente gerado e copiável
- Sem integração IAP real (Fase 3)

### Sincronização com Fase 3

| Slot criado na 2.7 | O que a Fase 3 faz |
|---|---|
| `_onBuy` mock (entrega local) | Substituir por `in_app_purchase` real |
| `ShareCode` em SharedPreferences | Migrar para Firestore |
| `_GiftCodeSheet` com código local | Conectar ao backend para validação |

---

## Prompt de brainstorm da Fase 2.7

> Use a skill `superpowers/brainstorming` pra refinar o design da próxima fase do projeto **Olha o Bichim!** (Flutter, codename `capivara_2048`).
>
> **Contexto:** Fase 2.6 concluída (v0.9.2) — Home redesenhada com grid 2×N de cards e animação do logo, Tela de Coleção (11 animais, grid 2 colunas, bottom sheet detalhado), Tela de Configurações (haptic, idioma, sliders de áudio desabilitados), stubs de navegação criados (`ShopScreen`, `InviteFriendsScreen`, `RedeemCodeScreen`). Use `CAPIVARA_2048_DESIGN.md` como spec geral (especialmente §7 — Loja de Itens, §12.4 — Tela Loja, §13.7 — `ShopPackage`, §13.8 — `ShareCode`, §15 — roadmap Fase 2.7).
>
> **Fases concluídas:** Fases 1 a 2.6 (v0.9.2). Áudio segue na **Fase 5**. Backend (Firebase, IAP real) entra na **Fase 3**.
>
> **Tópico do brainstorm:** **Fase 2.7 — Loja mock**. Esta fase preenche o stub `ShopScreen` com os 6 pacotes da §7.1, simula a compra localmente (sem IAP real), entrega os itens no inventário local, e gera um código de presente. Não há nova lógica de jogo nem integração com backend.
>
> **Sub-entregas (ver §15 — Fase 2.7 e spec em `docs/superpowers/specs/2026-05-01-fase-2-6-design.md`):**
>
> **A — ShopScreen (conteúdo):**
> - Substituir o stub criado na Fase 2.6 pelo conteúdo real — não criar nova tela.
> - `ListView` com os 6 `_ShopPackageCard` na ordem da tabela §7.1.
> - Cada card: nome do pacote, descrição do conteúdo (`RewardBundle`), preço "De" riscado, preço "Por" em destaque, badge de desconto (50% ou 75%), botão "Comprar".
> - Botão "Comprar" → `AlertDialog` de confirmação → entrega local via `inventoryProvider` + `livesProvider` → exibe `_GiftCodeSheet`.
> - `shop_data.dart`: lista estática dos 6 `ShopPackage` conforme §7.1 — sem backend.
>
> **B — `_GiftCodeSheet` (bottom sheet pós-compra):**
> - `DraggableScrollableSheet` com código de presente gerado localmente (UUID truncado).
> - Exibe: título "Presente gerado!", código formatado, conteúdo do `giftContents`, botão copiar para clipboard, botão fechar.
> - `ShareCode` armazenado em `SharedPreferences` — Fase 3 migra para Firestore sem alterar a estrutura.
>
> **C — `shop_notifier.dart` e `generatedShareCodesProvider`:**
> - `shopPackagesProvider`: `Provider<List<ShopPackage>>` — lista estática.
> - `generatedShareCodesProvider`: `StateNotifierProvider` — lista local de `ShareCode` gerados, persistida em `SharedPreferences`.
>
> **D — README, CHANGELOG, CLAUDE.md, design doc:**
> - Atualizar documentação ao fechar a fase.
> - Atualizar §17 do design doc com o prompt de brainstorm da **Fase 3**.
>
> **Pontos abertos pra explorar no brainstorm (elicitação esperada):**
>
> Sobre **A (ShopScreen — visual do card):**
> - Badge de desconto: chip no canto superior direito do card vs inline na linha de preço? Recomendação: canto superior direito (mais visível, padrão e-commerce).
> - Conteúdo do `RewardBundle` no card: texto compacto descritivo ("6 vidas + 2 bombas + 2 desfazer") vs lista com ícones dos itens? Recomendação: texto compacto — ícones exigem assets extras não planejados.
> - Scroll da loja: `ListView` simples vs `CustomScrollView` com `SliverAppBar` colapsável? Recomendação: `ListView` simples — 6 itens não justificam complexidade.
> - Header da `ShopScreen`: AppBar simples (padrão da 2.6) vs banner colorido com ilustração? Recomendação: AppBar padrão — arte adicional fica para a Fase 4.
>
> Sobre **B (`_GiftCodeSheet`):**
> - Formato do código: UUID truncado 8 chars uppercase com hífen (`A3F9-B2C1`) vs código alfanumérico de 6 chars sem hífen? Recomendação: 8 chars com hífen — legível e fácil de digitar para o amigo.
> - `_GiftCodeSheet` ou `AlertDialog` simples? Recomendação: bottom sheet — consistente com o padrão da 2.6 (Coleção, Como Jogar).
> - Confirmação de compra: `AlertDialog` simples ("Comprar [nome] por R$ X,XX?") vs bottom sheet estilizado? Recomendação: `AlertDialog` — fluxo transacional não precisa de visual elaborado.
> - Exibir o conteúdo do `giftContents` (o que o amigo recebe) separado do `contents` (o que o comprador recebe)?
>
> Sobre **C (providers e persistência):**
> - `generatedShareCodesProvider` precisa de notifier próprio ou basta um `StateProvider<List<ShareCode>>`? Recomendação: notifier próprio — encapsula a persistência em `SharedPreferences`.
> - Serialização do `ShareCode`: `jsonEncode`/`jsonDecode` manual vs `freezed`/`json_serializable`? Recomendação: manual — estrutura simples, sem dependência extra.
> - Limite de códigos armazenados localmente? Recomendação: últimos 20 — Fase 3 move para Firestore.
>
> Sobre **testes:**
> - Widget tests obrigatórios: 6 cards presentes, fluxo completo de compra (tap → dialog → confirmação → inventário atualizado → código gerado), botão copiar, código no clipboard.
> - `inventoryProvider` e `livesProvider` devem ser mockados nos testes da `ShopScreen`.
> - Golden tests: não — mesma decisão da Fase 2.6.
>
> Sobre **sincronização com Fase 3 (Backend/IAP):**
> - `_onBuy` mock entrega itens localmente — Fase 3 substitui pela chamada `in_app_purchase`.
> - `ShareCode` em `SharedPreferences` — Fase 3 migra para Firestore; mesma estrutura `ShareCode` já definida em §13.8.
> - `_GiftCodeSheet` exibe código local — Fase 3 conecta ao backend para validação do resgate.
> - `RedeemCodeScreen` (stub criado na 2.6) recebe conteúdo na Fase 3.
>
> **Output esperado do brainstorm:**
> Uma **spec detalhada da Fase 2.7** (`docs/superpowers/specs/YYYY-MM-DD-fase-2-7-design.md`) com:
> - Decisões tomadas em cada ponto aberto (visual do card, formato do código, confirmação, scroll).
> - Para cada sub-entrega (A–D): arquivos a criar/modificar, contratos exatos (assinaturas de widgets, modelos de dados usados, providers Riverpod necessários), casos de teste obrigatórios, critérios de aceite.
> - Diagrama de navegação textual (fluxo de compra completo: ShopScreen → AlertDialog → GiftCodeSheet).
> - Lista de pontos a sincronizar com Fase 3 (IAP real, Firestore, validação de código).
> - Plano de validação manual (emulador Android, simulador iOS — conferir layout da loja, fluxo de compra, código gerado e copiável).
> - Ao final do documento: **prompt de brainstorm da Fase 3** seguindo este mesmo padrão de cascata.
>
> Esse documento será depois consumido pela skill `superpowers/writing-plans` pra gerar o plano executável (TDD-friendly, com checkpoints).
>
> **Não escreva código nesta etapa.** Foque em refinar o design, fazer perguntas críticas e produzir a spec.
